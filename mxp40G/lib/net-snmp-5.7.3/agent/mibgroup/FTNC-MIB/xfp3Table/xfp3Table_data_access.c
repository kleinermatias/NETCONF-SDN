/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "../../xfp3Table.h"

#include <stdio.h>
#include <sys/mman.h>
#include <sys/types.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <stdlib.h>

#include "xfp3Table_data_access.h"

#include "../../../../lib/hl_configs/definitions.h"

void shm_handler_create(Monitor *pt_monitor_struct);
Monitor *pt_monitor_struct;

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table xfp3Table
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::xfp3Table is subid 5 of mxpMIBObjects.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.2.5, length: 9
*/

/**
 * initialization for xfp3Table data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param xfp3Table_reg
 *        Pointer to xfp3Table_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
xfp3Table_init_data(xfp3Table_registration * xfp3Table_reg)
{
    DEBUGMSGTL(("verbose:xfp3Table:xfp3Table_init_data","called\n"));

    /*
     * TODO:303:o: Initialize xfp3Table data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* xfp3Table_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
xfp3Table_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:xfp3Table:xfp3Table_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to xfp3Table_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to xfp3Table_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up xfp3Table cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = XFP3TABLE_CACHE_TIMEOUT; /* seconds */
} /* xfp3Table_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before xfp3Table_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
xfp3Table_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:xfp3Table:xfp3Table_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to xfp3Table_container_shutdown\n");
        return;
    }

} /* xfp3Table_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement xfp3Table data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  xfp3Table_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
xfp3Table_container_load(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:xfp3Table:xfp3Table_container_load","called\n"));

    xfp3Table_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * xfp3Index(1)/OCTETSTR/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h
         */
   
    char xfp3Index[32];
    strcpy (xfp3Index, "xfp3Index");
    size_t xfp3Index_len = strlen(xfp3Index);

    shm_handler_create(pt_monitor_struct);

        /*
         * TODO:352:M: |   |-> set indexes in new xfp3Table rowreq context.
         * data context will be set from the param (unless NULL,
         *      in which case a new data context will be allocated)
         */
        rowreq_ctx = xfp3Table_allocate_rowreq_ctx(NULL);
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            return MFD_RESOURCE_UNAVAILABLE;
        }
        if(MFD_SUCCESS != xfp3Table_indexes_set(rowreq_ctx
                               , xfp3Index, xfp3Index_len
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "xfp3Table data.\n");
            xfp3Table_release_rowreq_ctx(rowreq_ctx);
        }

        /*
         * TODO:352:r: |   |-> populate xfp3Table data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for xfp3Pres
     * xfp3Pres(2)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3Pres data
     */
    char xfp3Pres[65535];
	strcpy (xfp3Pres, general_status_no_yes[!pt_monitor_struct->xfp_struct.xfp_presence[2]]);
	size_t xfp3Pres_len = strlen(xfp3Pres);
     
    rowreq_ctx->data.xfp3Pres_len = xfp3Pres_len* sizeof(xfp3Pres[0]);
    if ((NULL == rowreq_ctx->data.xfp3Pres) ||
        (rowreq_ctx->data.xfp3Pres_len <
         (xfp3Pres_len* sizeof(xfp3Pres[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3Pres)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.xfp3Pres, xfp3Pres, xfp3Pres_len* sizeof(xfp3Pres[0]) );
    
    /*
     * setup/save data for xfp3Loss
     * xfp3Loss(3)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3Loss data
     */
     
    char xfp3Loss[65535];
	strcpy (xfp3Loss, general_status_no_yes[pt_monitor_struct->xfp_struct.xfp_rx_loss[2]]);
	size_t xfp3Loss_len = strlen(xfp3Loss);
     
    rowreq_ctx->data.xfp3Loss_len = xfp3Loss_len* sizeof(xfp3Loss[0]);
    if ((NULL == rowreq_ctx->data.xfp3Loss) ||
        (rowreq_ctx->data.xfp3Loss_len <
         (xfp3Loss_len* sizeof(xfp3Loss[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3Loss)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3Loss, xfp3Loss, xfp3Loss_len* sizeof(xfp3Loss[0]) );
    
    /*
     * setup/save data for xfp3Rdy
     * xfp3Rdy(4)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3Rdy data
     */

    char xfp3Rdy[65535];
	strcpy (xfp3Rdy, general_status_no_yes[!pt_monitor_struct->xfp_struct.xfp_ready[2]]);
	size_t xfp3Rdy_len = strlen(xfp3Rdy);
     
    rowreq_ctx->data.xfp3Rdy_len = xfp3Rdy_len* sizeof(xfp3Rdy[0]);
    if ((NULL == rowreq_ctx->data.xfp3Rdy) ||
        (rowreq_ctx->data.xfp3Rdy_len <
         (xfp3Rdy_len* sizeof(xfp3Rdy[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3Rdy)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3Rdy, xfp3Rdy, xfp3Rdy_len* sizeof(xfp3Rdy[0]) );
    
    /*
     * setup/save data for xfp3Int
     * xfp3Int(5)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3Int data
     */
     
    char xfp3Int[65535];
	strcpy (xfp3Int, general_status_no_yes[!pt_monitor_struct->xfp_struct.xfp_interrupt[2]]);
	size_t xfp3Int_len = strlen(xfp3Int);
     
    rowreq_ctx->data.xfp3Int_len = xfp3Int_len* sizeof(xfp3Int[0]);
    if ((NULL == rowreq_ctx->data.xfp3Int) ||
        (rowreq_ctx->data.xfp3Int_len <
         (xfp3Int_len* sizeof(xfp3Int[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3Int)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.xfp3Int, xfp3Int, xfp3Int_len* sizeof(xfp3Int[0]) );
    
    /*
     * setup/save data for xfp3TxPow
     * xfp3TxPow(6)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3TxPow data
     */
     
    char xfp3TxPow[65535];
	sprintf(output_char, "%.2f", pt_monitor_struct->xfp_struct.xfp_tx_power[2]);
	strcpy (xfp3TxPow, output_char);
	size_t xfp3TxPow_len = strlen(xfp3TxPow);
     
    rowreq_ctx->data.xfp3TxPow_len = xfp3TxPow_len* sizeof(xfp3TxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp3TxPow) ||
        (rowreq_ctx->data.xfp3TxPow_len <
         (xfp3TxPow_len* sizeof(xfp3TxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3TxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3TxPow, xfp3TxPow, xfp3TxPow_len* sizeof(xfp3TxPow[0]) );
    
    /*
     * setup/save data for xfp3RxPow
     * xfp3RxPow(7)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3RxPow data
     */
     
    char xfp3RxPow[65535];
	sprintf(output_char, "%.2f", pt_monitor_struct->xfp_struct.xfp_rx_power[2]);
	strcpy (xfp3RxPow, output_char);
	size_t xfp3RxPow_len = strlen(xfp3RxPow);
     
   rowreq_ctx->data.xfp3RxPow_len = xfp3RxPow_len* sizeof(xfp3RxPow[0]);
   if ((NULL == rowreq_ctx->data.xfp3RxPow) ||
        (rowreq_ctx->data.xfp3RxPow_len <
         (xfp3RxPow_len* sizeof(xfp3RxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3RxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3RxPow, xfp3RxPow, xfp3RxPow_len* sizeof(xfp3RxPow[0]) );
    
    /*
     * setup/save data for xfp3Temp
     * xfp3Temp(8)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3Temp data
     */
     
    char xfp3Temp[65535];
	sprintf(output_char, "%.2f", pt_monitor_struct->xfp_struct.xfp_temperature[2]);
	strcpy (xfp3Temp, output_char);
	size_t xfp3Temp_len = strlen(xfp3Temp);
     
    rowreq_ctx->data.xfp3Temp_len = xfp3Temp_len* sizeof(xfp3Temp[0]);
    if ((NULL == rowreq_ctx->data.xfp3Temp) ||
        (rowreq_ctx->data.xfp3Temp_len <
         (xfp3Temp_len* sizeof(xfp3Temp[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3Temp)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3Temp, xfp3Temp, xfp3Temp_len* sizeof(xfp3Temp[0]) );
    
    /*
     * setup/save data for xfp3LowTxPow
     * xfp3LowTxPow(9)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3LowTxPow data
     */
    
    char xfp3LowTxPow[65535];
	strcpy (xfp3LowTxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[2][0]]);	
	size_t xfp3LowTxPow_len = strlen(xfp3LowTxPow);
     
    rowreq_ctx->data.xfp3LowTxPow_len = xfp3LowTxPow_len* sizeof(xfp3LowTxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp3LowTxPow) ||
        (rowreq_ctx->data.xfp3LowTxPow_len <
         (xfp3LowTxPow_len* sizeof(xfp3LowTxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3LowTxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3LowTxPow, xfp3LowTxPow, xfp3LowTxPow_len* sizeof(xfp3LowTxPow[0]) );
    
    /*
     * setup/save data for xfp3HighTxPow
     * xfp3HighTxPow(10)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3HighTxPow data
     */
     
    char xfp3HighTxPow[65535];
	strcpy (xfp3HighTxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[2][1]]);
	size_t xfp3HighTxPow_len = strlen(xfp3HighTxPow);
    
    rowreq_ctx->data.xfp3HighTxPow_len = xfp3HighTxPow_len* sizeof(xfp3HighTxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp3HighTxPow) ||
        (rowreq_ctx->data.xfp3HighTxPow_len <
         (xfp3HighTxPow_len* sizeof(xfp3HighTxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3HighTxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3HighTxPow, xfp3HighTxPow, xfp3HighTxPow_len* sizeof(xfp3HighTxPow[0]) );
    
    /*
     * setup/save data for xfp3LowRxPow
     * xfp3LowRxPow(11)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3LowRxPow data
     */
     
    char xfp3LowRxPow[65535];
	strcpy (xfp3LowRxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[2][2]]);
	size_t xfp3LowRxPow_len = strlen(xfp3LowRxPow);
     
    rowreq_ctx->data.xfp3LowRxPow_len = xfp3LowRxPow_len* sizeof(xfp3LowRxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp3LowRxPow) ||
        (rowreq_ctx->data.xfp3LowRxPow_len <
         (xfp3LowRxPow_len* sizeof(xfp3LowRxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3LowRxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3LowRxPow, xfp3LowRxPow, xfp3LowRxPow_len* sizeof(xfp3LowRxPow[0]) );
    
    /*
     * setup/save data for xfp3HighRxPow
     * xfp3HighRxPow(12)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3HighRxPow data
     */
     
    char xfp3HighRxPow[65535];
	strcpy (xfp3HighRxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[2][3]]);
	size_t xfp3HighRxPow_len = strlen(xfp3HighRxPow);
     
    rowreq_ctx->data.xfp3HighRxPow_len = xfp3HighRxPow_len* sizeof(xfp3HighRxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp3HighRxPow) ||
        (rowreq_ctx->data.xfp3HighRxPow_len <
         (xfp3HighRxPow_len* sizeof(xfp3HighRxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3HighRxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3HighRxPow, xfp3HighRxPow, xfp3HighRxPow_len* sizeof(xfp3HighRxPow[0]) );
    
    /*
     * setup/save data for xfp3RxCdrLol
     * xfp3RxCdrLol(13)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3RxCdrLol data
     */
     
    char xfp3RxCdrLol[65535];
	strcpy (xfp3RxCdrLol, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[2][4]]);
	size_t xfp3RxCdrLol_len = strlen(xfp3RxCdrLol);
     
    rowreq_ctx->data.xfp3RxCdrLol_len = xfp3RxCdrLol_len* sizeof(xfp3RxCdrLol[0]);
    if ((NULL == rowreq_ctx->data.xfp3RxCdrLol) ||
        (rowreq_ctx->data.xfp3RxCdrLol_len <
         (xfp3RxCdrLol_len* sizeof(xfp3RxCdrLol[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3RxCdrLol)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3RxCdrLol, xfp3RxCdrLol, xfp3RxCdrLol_len* sizeof(xfp3RxCdrLol[0]) );
    
    /*
     * setup/save data for xfp3TxCdrLol
     * xfp3TxCdrLol(14)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3TxCdrLol data
     */
     
    char xfp3TxCdrLol[65535];
	strcpy (xfp3TxCdrLol, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[2][5]]);
	size_t xfp3TxCdrLol_len = strlen(xfp3TxCdrLol);
     
    rowreq_ctx->data.xfp3TxCdrLol_len = xfp3TxCdrLol_len* sizeof(xfp3TxCdrLol[0]);
    if ((NULL == rowreq_ctx->data.xfp3TxCdrLol) ||
        (rowreq_ctx->data.xfp3TxCdrLol_len <
         (xfp3TxCdrLol_len* sizeof(xfp3TxCdrLol[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3TxCdrLol)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp3TxCdrLol, xfp3TxCdrLol, xfp3TxCdrLol_len* sizeof(xfp3TxCdrLol[0]) );
    
    /*
     * setup/save data for xfp3LasFault
     * xfp3LasFault(15)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp3LasFault data
     */
     
    char xfp3LasFault[65535];
	strcpy (xfp3LasFault, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[2][6]]);
	size_t xfp3LasFault_len = strlen(xfp3LasFault);
     
    rowreq_ctx->data.xfp3LasFault_len = xfp3LasFault_len* sizeof(xfp3LasFault[0]);
    if ((NULL == rowreq_ctx->data.xfp3LasFault) ||
        (rowreq_ctx->data.xfp3LasFault_len <
         (xfp3LasFault_len* sizeof(xfp3LasFault[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp3LasFault)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.xfp3LasFault, xfp3LasFault, xfp3LasFault_len* sizeof(xfp3LasFault[0]) );
        
        /*
         * insert into table container
         */
        CONTAINER_INSERT(container, rowreq_ctx);
        ++count;

    DEBUGMSGT(("verbose:xfp3Table:xfp3Table_container_load",
               "inserted %d records\n", count));

    return MFD_SUCCESS;
} /* xfp3Table_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
xfp3Table_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:xfp3Table:xfp3Table_container_free","called\n"));

    /*
     * TODO:380:M: Free xfp3Table container data.
     */
} /* xfp3Table_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
xfp3Table_row_prep( xfp3Table_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:xfp3Table:xfp3Table_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* xfp3Table_row_prep */

/** @} */
