/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "tpClient2SeverityTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement tpClient2SeverityTable get routines.
 * TODO:240:M: Implement tpClient2SeverityTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table tpClient2SeverityTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::tpClient2SeverityTable is subid 3 of tpAlarms.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement tpClient2SeverityTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param tpClient2SeverityIndex_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
tpClient2SeverityTable_indexes_set_tbl_idx(tpClient2SeverityTable_mib_index *tbl_idx, long tpClient2SeverityIndex_val)
{
    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityTable_indexes_set_tbl_idx","called\n"));

    /* tpClient2SeverityIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/a/w/e/R/d/h */
    tbl_idx->tpClient2SeverityIndex = tpClient2SeverityIndex_val;
    

    return MFD_SUCCESS;
} /* tpClient2SeverityTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
tpClient2SeverityTable_indexes_set(tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient2SeverityIndex_val)
{
    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityTable_indexes_set","called\n"));

    if(MFD_SUCCESS != tpClient2SeverityTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , tpClient2SeverityIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != tpClient2SeverityTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* tpClient2SeverityTable_indexes_set */


/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityFramerFsmms
 * tpClient2SeverityFramerFsmms is subid 2 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.2
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityFramerFsmms data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityFramerFsmms_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityFramerFsmms_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityFramerFsmms_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityFramerFsmms_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityFramerFsmms_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityFramerFsmms data.
 * copy (* tpClient2SeverityFramerFsmms_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityFramerFsmms_val_ptr ) = rowreq_ctx->data.tpClient2SeverityFramerFsmms;

    return MFD_SUCCESS;
} /* tpClient2SeverityFramerFsmms_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityFramerFssusts
 * tpClient2SeverityFramerFssusts is subid 3 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.3
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityFramerFssusts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityFramerFssusts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityFramerFssusts_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityFramerFssusts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityFramerFssusts_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityFramerFssusts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityFramerFssusts data.
 * copy (* tpClient2SeverityFramerFssusts_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityFramerFssusts_val_ptr ) = rowreq_ctx->data.tpClient2SeverityFramerFssusts;

    return MFD_SUCCESS;
} /* tpClient2SeverityFramerFssusts_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityFramerSoof
 * tpClient2SeverityFramerSoof is subid 4 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.4
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityFramerSoof data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityFramerSoof_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityFramerSoof_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityFramerSoof_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityFramerSoof_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityFramerSoof_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityFramerSoof data.
 * copy (* tpClient2SeverityFramerSoof_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityFramerSoof_val_ptr ) = rowreq_ctx->data.tpClient2SeverityFramerSoof;

    return MFD_SUCCESS;
} /* tpClient2SeverityFramerSoof_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityFramerSlof
 * tpClient2SeverityFramerSlof is subid 5 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.5
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityFramerSlof data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityFramerSlof_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityFramerSlof_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityFramerSlof_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityFramerSlof_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityFramerSlof_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityFramerSlof data.
 * copy (* tpClient2SeverityFramerSlof_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityFramerSlof_val_ptr ) = rowreq_ctx->data.tpClient2SeverityFramerSlof;

    return MFD_SUCCESS;
} /* tpClient2SeverityFramerSlof_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityFramerSif
 * tpClient2SeverityFramerSif is subid 6 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.6
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityFramerSif data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityFramerSif_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityFramerSif_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityFramerSif_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityFramerSif_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityFramerSif_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityFramerSif data.
 * copy (* tpClient2SeverityFramerSif_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityFramerSif_val_ptr ) = rowreq_ctx->data.tpClient2SeverityFramerSif;

    return MFD_SUCCESS;
} /* tpClient2SeverityFramerSif_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityFramerFst
 * tpClient2SeverityFramerFst is subid 7 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.7
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityFramerFst data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityFramerFst_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityFramerFst_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityFramerFst_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityFramerFst_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityFramerFst_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityFramerFst data.
 * copy (* tpClient2SeverityFramerFst_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityFramerFst_val_ptr ) = rowreq_ctx->data.tpClient2SeverityFramerFst;

    return MFD_SUCCESS;
} /* tpClient2SeverityFramerFst_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityLosstatLosf
 * tpClient2SeverityLosstatLosf is subid 8 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.8
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityLosstatLosf data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityLosstatLosf_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityLosstatLosf_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityLosstatLosf_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityLosstatLosf_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityLosstatLosf_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityLosstatLosf data.
 * copy (* tpClient2SeverityLosstatLosf_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityLosstatLosf_val_ptr ) = rowreq_ctx->data.tpClient2SeverityLosstatLosf;

    return MFD_SUCCESS;
} /* tpClient2SeverityLosstatLosf_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityPathmonAccstat
 * tpClient2SeverityPathmonAccstat is subid 9 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.9
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityPathmonAccstat data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityPathmonAccstat_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityPathmonAccstat_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityPathmonAccstat_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityPathmonAccstat_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityPathmonAccstat_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityPathmonAccstat data.
 * copy (* tpClient2SeverityPathmonAccstat_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityPathmonAccstat_val_ptr ) = rowreq_ctx->data.tpClient2SeverityPathmonAccstat;

    return MFD_SUCCESS;
} /* tpClient2SeverityPathmonAccstat_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityPathmonDais
 * tpClient2SeverityPathmonDais is subid 10 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.10
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityPathmonDais data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityPathmonDais_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityPathmonDais_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityPathmonDais_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityPathmonDais_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityPathmonDais_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityPathmonDais data.
 * copy (* tpClient2SeverityPathmonDais_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityPathmonDais_val_ptr ) = rowreq_ctx->data.tpClient2SeverityPathmonDais;

    return MFD_SUCCESS;
} /* tpClient2SeverityPathmonDais_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityPathmonDbdi
 * tpClient2SeverityPathmonDbdi is subid 11 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.11
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityPathmonDbdi data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityPathmonDbdi_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityPathmonDbdi_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityPathmonDbdi_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityPathmonDbdi_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityPathmonDbdi_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityPathmonDbdi data.
 * copy (* tpClient2SeverityPathmonDbdi_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityPathmonDbdi_val_ptr ) = rowreq_ctx->data.tpClient2SeverityPathmonDbdi;

    return MFD_SUCCESS;
} /* tpClient2SeverityPathmonDbdi_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityPathmonDlck
 * tpClient2SeverityPathmonDlck is subid 12 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.12
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityPathmonDlck data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityPathmonDlck_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityPathmonDlck_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityPathmonDlck_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityPathmonDlck_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityPathmonDlck_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityPathmonDlck data.
 * copy (* tpClient2SeverityPathmonDlck_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityPathmonDlck_val_ptr ) = rowreq_ctx->data.tpClient2SeverityPathmonDlck;

    return MFD_SUCCESS;
} /* tpClient2SeverityPathmonDlck_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeverityPathmonMsiunst
 * tpClient2SeverityPathmonMsiunst is subid 13 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.13
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeverityPathmonMsiunst data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeverityPathmonMsiunst_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeverityPathmonMsiunst_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeverityPathmonMsiunst_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeverityPathmonMsiunst_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeverityPathmonMsiunst_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeverityPathmonMsiunst data.
 * copy (* tpClient2SeverityPathmonMsiunst_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeverityPathmonMsiunst_val_ptr ) = rowreq_ctx->data.tpClient2SeverityPathmonMsiunst;

    return MFD_SUCCESS;
} /* tpClient2SeverityPathmonMsiunst_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeveritySecmonBipsfs
 * tpClient2SeveritySecmonBipsfs is subid 14 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.14
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeveritySecmonBipsfs data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeveritySecmonBipsfs_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeveritySecmonBipsfs_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeveritySecmonBipsfs_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeveritySecmonBipsfs_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeveritySecmonBipsfs_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeveritySecmonBipsfs data.
 * copy (* tpClient2SeveritySecmonBipsfs_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeveritySecmonBipsfs_val_ptr ) = rowreq_ctx->data.tpClient2SeveritySecmonBipsfs;

    return MFD_SUCCESS;
} /* tpClient2SeveritySecmonBipsfs_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeveritySecmonDbdi
 * tpClient2SeveritySecmonDbdi is subid 15 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.15
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeveritySecmonDbdi data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeveritySecmonDbdi_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeveritySecmonDbdi_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeveritySecmonDbdi_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeveritySecmonDbdi_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeveritySecmonDbdi_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeveritySecmonDbdi data.
 * copy (* tpClient2SeveritySecmonDbdi_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeveritySecmonDbdi_val_ptr ) = rowreq_ctx->data.tpClient2SeveritySecmonDbdi;

    return MFD_SUCCESS;
} /* tpClient2SeveritySecmonDbdi_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeveritySecmonDiae
 * tpClient2SeveritySecmonDiae is subid 16 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.16
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeveritySecmonDiae data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeveritySecmonDiae_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeveritySecmonDiae_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeveritySecmonDiae_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeveritySecmonDiae_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeveritySecmonDiae_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeveritySecmonDiae data.
 * copy (* tpClient2SeveritySecmonDiae_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeveritySecmonDiae_val_ptr ) = rowreq_ctx->data.tpClient2SeveritySecmonDiae;

    return MFD_SUCCESS;
} /* tpClient2SeveritySecmonDiae_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeveritySecmonFlom
 * tpClient2SeveritySecmonFlom is subid 17 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.17
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeveritySecmonFlom data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeveritySecmonFlom_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeveritySecmonFlom_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeveritySecmonFlom_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeveritySecmonFlom_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeveritySecmonFlom_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeveritySecmonFlom data.
 * copy (* tpClient2SeveritySecmonFlom_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeveritySecmonFlom_val_ptr ) = rowreq_ctx->data.tpClient2SeveritySecmonFlom;

    return MFD_SUCCESS;
} /* tpClient2SeveritySecmonFlom_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient2SeverityEntry.tpClient2SeveritySecmonLom
 * tpClient2SeveritySecmonLom is subid 18 of tpClient2SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.3.1.18
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpClient2SeveritySecmonLom data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient2SeveritySecmonLom_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpClient2SeveritySecmonLom_get( tpClient2SeverityTable_rowreq_ctx *rowreq_ctx, long * tpClient2SeveritySecmonLom_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpClient2SeveritySecmonLom_val_ptr );


    DEBUGMSGTL(("verbose:tpClient2SeverityTable:tpClient2SeveritySecmonLom_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpClient2SeveritySecmonLom data.
 * copy (* tpClient2SeveritySecmonLom_val_ptr ) from rowreq_ctx->data
 */
    (* tpClient2SeveritySecmonLom_val_ptr ) = rowreq_ctx->data.tpClient2SeveritySecmonLom;

    return MFD_SUCCESS;
} /* tpClient2SeveritySecmonLom_get */



/** @} */
