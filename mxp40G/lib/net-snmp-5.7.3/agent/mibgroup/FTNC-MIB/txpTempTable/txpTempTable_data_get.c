/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "../../txpTempTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement txpTempTable get routines.
 * TODO:240:M: Implement txpTempTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table txpTempTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::txpTempTable is subid 1 of txpMiscellaneous.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.2.9.2.1, length: 11
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement txpTempTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param txpTempIndex_ptr
 * @param txpTempIndex_ptr_len
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
txpTempTable_indexes_set_tbl_idx(txpTempTable_mib_index *tbl_idx, char *txpTempIndex_val_ptr,  size_t txpTempIndex_val_ptr_len)
{
    DEBUGMSGTL(("verbose:txpTempTable:txpTempTable_indexes_set_tbl_idx","called\n"));

    /* txpTempIndex(1)/OCTETSTR/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h */
    tbl_idx->txpTempIndex_len = sizeof(tbl_idx->txpTempIndex)/sizeof(tbl_idx->txpTempIndex[0]); /* max length */
    /*
     * make sure there is enough space for txpTempIndex data
     */
    if ((NULL == tbl_idx->txpTempIndex) ||
        (tbl_idx->txpTempIndex_len <
         (txpTempIndex_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value (txpTempIndex_val_ptr)\n");
        return MFD_ERROR;
    }
    tbl_idx->txpTempIndex_len = txpTempIndex_val_ptr_len;
    memcpy( tbl_idx->txpTempIndex, txpTempIndex_val_ptr, txpTempIndex_val_ptr_len* sizeof(txpTempIndex_val_ptr[0]) );
    

    return MFD_SUCCESS;
} /* txpTempTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
txpTempTable_indexes_set(txpTempTable_rowreq_ctx *rowreq_ctx, char *txpTempIndex_val_ptr,  size_t txpTempIndex_val_ptr_len)
{
    DEBUGMSGTL(("verbose:txpTempTable:txpTempTable_indexes_set","called\n"));

    if(MFD_SUCCESS != txpTempTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , txpTempIndex_val_ptr, txpTempIndex_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != txpTempTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* txpTempTable_indexes_set */


/*---------------------------------------------------------------------
 * FTNC-MIB::txpTempEntry.txpTempAround
 * txpTempAround is subid 2 of txpTempEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.2.1.1.2
 * Description:
Temperature around txp40G.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpTempAround data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpTempAround_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpTempAround_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpTempAround.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpTempAround_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpTempAround_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpTempAround_get( txpTempTable_rowreq_ctx *rowreq_ctx, char **txpTempAround_val_ptr_ptr, size_t *txpTempAround_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpTempAround_val_ptr_ptr) && (NULL != *txpTempAround_val_ptr_ptr));
   netsnmp_assert( NULL != txpTempAround_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpTempTable:txpTempAround_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpTempAround data.
 * copy (* txpTempAround_val_ptr_ptr ) data and (* txpTempAround_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpTempAround data
     */
    if ((NULL == (* txpTempAround_val_ptr_ptr )) ||
        ((* txpTempAround_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpTempAround_len* sizeof(rowreq_ctx->data.txpTempAround[0])))) {
        /*
         * allocate space for txpTempAround data
         */
        (* txpTempAround_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpTempAround_len* sizeof(rowreq_ctx->data.txpTempAround[0]));
        if(NULL == (* txpTempAround_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpTempAround)\n");
            return MFD_ERROR;
        }
    }
    (* txpTempAround_val_ptr_len_ptr ) = rowreq_ctx->data.txpTempAround_len* sizeof(rowreq_ctx->data.txpTempAround[0]);
    memcpy( (* txpTempAround_val_ptr_ptr ), rowreq_ctx->data.txpTempAround, rowreq_ctx->data.txpTempAround_len* sizeof(rowreq_ctx->data.txpTempAround[0]) );

    return MFD_SUCCESS;
} /* txpTempAround_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::txpTempEntry.txpTempCase
 * txpTempCase is subid 3 of txpTempEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.2.1.1.3
 * Description:
Temperature case txp40G.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpTempCase data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpTempCase_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpTempCase_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpTempCase.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpTempCase_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpTempCase_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpTempCase_get( txpTempTable_rowreq_ctx *rowreq_ctx, char **txpTempCase_val_ptr_ptr, size_t *txpTempCase_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpTempCase_val_ptr_ptr) && (NULL != *txpTempCase_val_ptr_ptr));
   netsnmp_assert( NULL != txpTempCase_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpTempTable:txpTempCase_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpTempCase data.
 * copy (* txpTempCase_val_ptr_ptr ) data and (* txpTempCase_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpTempCase data
     */
    if ((NULL == (* txpTempCase_val_ptr_ptr )) ||
        ((* txpTempCase_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpTempCase_len* sizeof(rowreq_ctx->data.txpTempCase[0])))) {
        /*
         * allocate space for txpTempCase data
         */
        (* txpTempCase_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpTempCase_len* sizeof(rowreq_ctx->data.txpTempCase[0]));
        if(NULL == (* txpTempCase_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpTempCase)\n");
            return MFD_ERROR;
        }
    }
    (* txpTempCase_val_ptr_len_ptr ) = rowreq_ctx->data.txpTempCase_len* sizeof(rowreq_ctx->data.txpTempCase[0]);
    memcpy( (* txpTempCase_val_ptr_ptr ), rowreq_ctx->data.txpTempCase, rowreq_ctx->data.txpTempCase_len* sizeof(rowreq_ctx->data.txpTempCase[0]) );

    return MFD_SUCCESS;
} /* txpTempCase_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::txpTempEntry.txpTempTxLaser
 * txpTempTxLaser is subid 4 of txpTempEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.2.1.1.4
 * Description:
Temperature Tx laser txp40G.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpTempTxLaser data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpTempTxLaser_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpTempTxLaser_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpTempTxLaser.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpTempTxLaser_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpTempTxLaser_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpTempTxLaser_get( txpTempTable_rowreq_ctx *rowreq_ctx, char **txpTempTxLaser_val_ptr_ptr, size_t *txpTempTxLaser_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpTempTxLaser_val_ptr_ptr) && (NULL != *txpTempTxLaser_val_ptr_ptr));
   netsnmp_assert( NULL != txpTempTxLaser_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpTempTable:txpTempTxLaser_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpTempTxLaser data.
 * copy (* txpTempTxLaser_val_ptr_ptr ) data and (* txpTempTxLaser_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpTempTxLaser data
     */
    if ((NULL == (* txpTempTxLaser_val_ptr_ptr )) ||
        ((* txpTempTxLaser_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpTempTxLaser_len* sizeof(rowreq_ctx->data.txpTempTxLaser[0])))) {
        /*
         * allocate space for txpTempTxLaser data
         */
        (* txpTempTxLaser_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpTempTxLaser_len* sizeof(rowreq_ctx->data.txpTempTxLaser[0]));
        if(NULL == (* txpTempTxLaser_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpTempTxLaser)\n");
            return MFD_ERROR;
        }
    }
    (* txpTempTxLaser_val_ptr_len_ptr ) = rowreq_ctx->data.txpTempTxLaser_len* sizeof(rowreq_ctx->data.txpTempTxLaser[0]);
    memcpy( (* txpTempTxLaser_val_ptr_ptr ), rowreq_ctx->data.txpTempTxLaser, rowreq_ctx->data.txpTempTxLaser_len* sizeof(rowreq_ctx->data.txpTempTxLaser[0]) );

    return MFD_SUCCESS;
} /* txpTempTxLaser_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::txpTempEntry.txpTempRxLaser
 * txpTempRxLaser is subid 5 of txpTempEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.2.1.1.5
 * Description:
Temperature Rx laser txp40G.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpTempRxLaser data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpTempRxLaser_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpTempRxLaser_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpTempRxLaser.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpTempRxLaser_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpTempRxLaser_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpTempRxLaser_get( txpTempTable_rowreq_ctx *rowreq_ctx, char **txpTempRxLaser_val_ptr_ptr, size_t *txpTempRxLaser_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpTempRxLaser_val_ptr_ptr) && (NULL != *txpTempRxLaser_val_ptr_ptr));
   netsnmp_assert( NULL != txpTempRxLaser_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpTempTable:txpTempRxLaser_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpTempRxLaser data.
 * copy (* txpTempRxLaser_val_ptr_ptr ) data and (* txpTempRxLaser_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpTempRxLaser data
     */
    if ((NULL == (* txpTempRxLaser_val_ptr_ptr )) ||
        ((* txpTempRxLaser_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpTempRxLaser_len* sizeof(rowreq_ctx->data.txpTempRxLaser[0])))) {
        /*
         * allocate space for txpTempRxLaser data
         */
        (* txpTempRxLaser_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpTempRxLaser_len* sizeof(rowreq_ctx->data.txpTempRxLaser[0]));
        if(NULL == (* txpTempRxLaser_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpTempRxLaser)\n");
            return MFD_ERROR;
        }
    }
    (* txpTempRxLaser_val_ptr_len_ptr ) = rowreq_ctx->data.txpTempRxLaser_len* sizeof(rowreq_ctx->data.txpTempRxLaser[0]);
    memcpy( (* txpTempRxLaser_val_ptr_ptr ), rowreq_ctx->data.txpTempRxLaser, rowreq_ctx->data.txpTempRxLaser_len* sizeof(rowreq_ctx->data.txpTempRxLaser[0]) );

    return MFD_SUCCESS;
} /* txpTempRxLaser_get */



/** @} */
