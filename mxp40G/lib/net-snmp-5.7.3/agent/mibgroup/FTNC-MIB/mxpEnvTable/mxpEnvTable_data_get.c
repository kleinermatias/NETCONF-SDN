/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "../../mxpEnvTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement mxpEnvTable get routines.
 * TODO:240:M: Implement mxpEnvTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table mxpEnvTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::mxpEnvTable is subid 7 of mxpMIBObjects.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.2.7, length: 9
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement mxpEnvTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param mxpEnvIndex_ptr
 * @param mxpEnvIndex_ptr_len
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
mxpEnvTable_indexes_set_tbl_idx(mxpEnvTable_mib_index *tbl_idx, char *mxpEnvIndex_val_ptr,  size_t mxpEnvIndex_val_ptr_len)
{
    DEBUGMSGTL(("verbose:mxpEnvTable:mxpEnvTable_indexes_set_tbl_idx","called\n"));

    /* mxpEnvIndex(1)/OCTETSTR/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h */
    tbl_idx->mxpEnvIndex_len = sizeof(tbl_idx->mxpEnvIndex)/sizeof(tbl_idx->mxpEnvIndex[0]); /* max length */
    /*
     * make sure there is enough space for mxpEnvIndex data
     */
    if ((NULL == tbl_idx->mxpEnvIndex) ||
        (tbl_idx->mxpEnvIndex_len <
         (mxpEnvIndex_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value (mxpEnvIndex_val_ptr)\n");
        return MFD_ERROR;
    }
    tbl_idx->mxpEnvIndex_len = mxpEnvIndex_val_ptr_len;
    memcpy( tbl_idx->mxpEnvIndex, mxpEnvIndex_val_ptr, mxpEnvIndex_val_ptr_len* sizeof(mxpEnvIndex_val_ptr[0]) );
    

    return MFD_SUCCESS;
} /* mxpEnvTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
mxpEnvTable_indexes_set(mxpEnvTable_rowreq_ctx *rowreq_ctx, char *mxpEnvIndex_val_ptr,  size_t mxpEnvIndex_val_ptr_len)
{
    DEBUGMSGTL(("verbose:mxpEnvTable:mxpEnvTable_indexes_set","called\n"));

    if(MFD_SUCCESS != mxpEnvTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , mxpEnvIndex_val_ptr, mxpEnvIndex_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != mxpEnvTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* mxpEnvTable_indexes_set */


/*---------------------------------------------------------------------
 * FTNC-MIB::mxpEnvEntry.mxpTempT41Around
 * mxpTempT41Around is subid 2 of mxpEnvEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.7.1.2
 * Description:
Temperature around cs6041
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the mxpTempT41Around data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param mxpTempT41Around_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param mxpTempT41Around_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by mxpTempT41Around.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*mxpTempT41Around_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update mxpTempT41Around_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
mxpTempT41Around_get( mxpEnvTable_rowreq_ctx *rowreq_ctx, char **mxpTempT41Around_val_ptr_ptr, size_t *mxpTempT41Around_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != mxpTempT41Around_val_ptr_ptr) && (NULL != *mxpTempT41Around_val_ptr_ptr));
   netsnmp_assert( NULL != mxpTempT41Around_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:mxpEnvTable:mxpTempT41Around_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the mxpTempT41Around data.
 * copy (* mxpTempT41Around_val_ptr_ptr ) data and (* mxpTempT41Around_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for mxpTempT41Around data
     */
    if ((NULL == (* mxpTempT41Around_val_ptr_ptr )) ||
        ((* mxpTempT41Around_val_ptr_len_ptr ) <
         (rowreq_ctx->data.mxpTempT41Around_len* sizeof(rowreq_ctx->data.mxpTempT41Around[0])))) {
        /*
         * allocate space for mxpTempT41Around data
         */
        (* mxpTempT41Around_val_ptr_ptr ) = malloc(rowreq_ctx->data.mxpTempT41Around_len* sizeof(rowreq_ctx->data.mxpTempT41Around[0]));
        if(NULL == (* mxpTempT41Around_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.mxpTempT41Around)\n");
            return MFD_ERROR;
        }
    }
    (* mxpTempT41Around_val_ptr_len_ptr ) = rowreq_ctx->data.mxpTempT41Around_len* sizeof(rowreq_ctx->data.mxpTempT41Around[0]);
    memcpy( (* mxpTempT41Around_val_ptr_ptr ), rowreq_ctx->data.mxpTempT41Around, rowreq_ctx->data.mxpTempT41Around_len* sizeof(rowreq_ctx->data.mxpTempT41Around[0]) );

    return MFD_SUCCESS;
} /* mxpTempT41Around_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::mxpEnvEntry.mxpTempT41TS0
 * mxpTempT41TS0 is subid 3 of mxpEnvEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.7.1.3
 * Description:
Temperature cs6041 TS0
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the mxpTempT41TS0 data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param mxpTempT41TS0_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param mxpTempT41TS0_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by mxpTempT41TS0.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*mxpTempT41TS0_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update mxpTempT41TS0_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
mxpTempT41TS0_get( mxpEnvTable_rowreq_ctx *rowreq_ctx, char **mxpTempT41TS0_val_ptr_ptr, size_t *mxpTempT41TS0_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != mxpTempT41TS0_val_ptr_ptr) && (NULL != *mxpTempT41TS0_val_ptr_ptr));
   netsnmp_assert( NULL != mxpTempT41TS0_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:mxpEnvTable:mxpTempT41TS0_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the mxpTempT41TS0 data.
 * copy (* mxpTempT41TS0_val_ptr_ptr ) data and (* mxpTempT41TS0_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for mxpTempT41TS0 data
     */
    if ((NULL == (* mxpTempT41TS0_val_ptr_ptr )) ||
        ((* mxpTempT41TS0_val_ptr_len_ptr ) <
         (rowreq_ctx->data.mxpTempT41TS0_len* sizeof(rowreq_ctx->data.mxpTempT41TS0[0])))) {
        /*
         * allocate space for mxpTempT41TS0 data
         */
        (* mxpTempT41TS0_val_ptr_ptr ) = malloc(rowreq_ctx->data.mxpTempT41TS0_len* sizeof(rowreq_ctx->data.mxpTempT41TS0[0]));
        if(NULL == (* mxpTempT41TS0_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.mxpTempT41TS0)\n");
            return MFD_ERROR;
        }
    }
    (* mxpTempT41TS0_val_ptr_len_ptr ) = rowreq_ctx->data.mxpTempT41TS0_len* sizeof(rowreq_ctx->data.mxpTempT41TS0[0]);
    memcpy( (* mxpTempT41TS0_val_ptr_ptr ), rowreq_ctx->data.mxpTempT41TS0, rowreq_ctx->data.mxpTempT41TS0_len* sizeof(rowreq_ctx->data.mxpTempT41TS0[0]) );

    return MFD_SUCCESS;
} /* mxpTempT41TS0_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::mxpEnvEntry.mxpTempT41TS1
 * mxpTempT41TS1 is subid 4 of mxpEnvEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.7.1.4
 * Description:
Temperature cs6041 TS1
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the mxpTempT41TS1 data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param mxpTempT41TS1_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param mxpTempT41TS1_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by mxpTempT41TS1.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*mxpTempT41TS1_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update mxpTempT41TS1_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
mxpTempT41TS1_get( mxpEnvTable_rowreq_ctx *rowreq_ctx, char **mxpTempT41TS1_val_ptr_ptr, size_t *mxpTempT41TS1_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != mxpTempT41TS1_val_ptr_ptr) && (NULL != *mxpTempT41TS1_val_ptr_ptr));
   netsnmp_assert( NULL != mxpTempT41TS1_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:mxpEnvTable:mxpTempT41TS1_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the mxpTempT41TS1 data.
 * copy (* mxpTempT41TS1_val_ptr_ptr ) data and (* mxpTempT41TS1_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for mxpTempT41TS1 data
     */
    if ((NULL == (* mxpTempT41TS1_val_ptr_ptr )) ||
        ((* mxpTempT41TS1_val_ptr_len_ptr ) <
         (rowreq_ctx->data.mxpTempT41TS1_len* sizeof(rowreq_ctx->data.mxpTempT41TS1[0])))) {
        /*
         * allocate space for mxpTempT41TS1 data
         */
        (* mxpTempT41TS1_val_ptr_ptr ) = malloc(rowreq_ctx->data.mxpTempT41TS1_len* sizeof(rowreq_ctx->data.mxpTempT41TS1[0]));
        if(NULL == (* mxpTempT41TS1_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.mxpTempT41TS1)\n");
            return MFD_ERROR;
        }
    }
    (* mxpTempT41TS1_val_ptr_len_ptr ) = rowreq_ctx->data.mxpTempT41TS1_len* sizeof(rowreq_ctx->data.mxpTempT41TS1[0]);
    memcpy( (* mxpTempT41TS1_val_ptr_ptr ), rowreq_ctx->data.mxpTempT41TS1, rowreq_ctx->data.mxpTempT41TS1_len* sizeof(rowreq_ctx->data.mxpTempT41TS1[0]) );

    return MFD_SUCCESS;
} /* mxpTempT41TS1_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::mxpEnvEntry.mxpTempT41TS2
 * mxpTempT41TS2 is subid 5 of mxpEnvEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.7.1.5
 * Description:
Temperature cs6041 TS2
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the mxpTempT41TS2 data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param mxpTempT41TS2_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param mxpTempT41TS2_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by mxpTempT41TS2.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*mxpTempT41TS2_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update mxpTempT41TS2_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
mxpTempT41TS2_get( mxpEnvTable_rowreq_ctx *rowreq_ctx, char **mxpTempT41TS2_val_ptr_ptr, size_t *mxpTempT41TS2_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != mxpTempT41TS2_val_ptr_ptr) && (NULL != *mxpTempT41TS2_val_ptr_ptr));
   netsnmp_assert( NULL != mxpTempT41TS2_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:mxpEnvTable:mxpTempT41TS2_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the mxpTempT41TS2 data.
 * copy (* mxpTempT41TS2_val_ptr_ptr ) data and (* mxpTempT41TS2_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for mxpTempT41TS2 data
     */
    if ((NULL == (* mxpTempT41TS2_val_ptr_ptr )) ||
        ((* mxpTempT41TS2_val_ptr_len_ptr ) <
         (rowreq_ctx->data.mxpTempT41TS2_len* sizeof(rowreq_ctx->data.mxpTempT41TS2[0])))) {
        /*
         * allocate space for mxpTempT41TS2 data
         */
        (* mxpTempT41TS2_val_ptr_ptr ) = malloc(rowreq_ctx->data.mxpTempT41TS2_len* sizeof(rowreq_ctx->data.mxpTempT41TS2[0]));
        if(NULL == (* mxpTempT41TS2_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.mxpTempT41TS2)\n");
            return MFD_ERROR;
        }
    }
    (* mxpTempT41TS2_val_ptr_len_ptr ) = rowreq_ctx->data.mxpTempT41TS2_len* sizeof(rowreq_ctx->data.mxpTempT41TS2[0]);
    memcpy( (* mxpTempT41TS2_val_ptr_ptr ), rowreq_ctx->data.mxpTempT41TS2, rowreq_ctx->data.mxpTempT41TS2_len* sizeof(rowreq_ctx->data.mxpTempT41TS2[0]) );

    return MFD_SUCCESS;
} /* mxpTempT41TS2_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::mxpEnvEntry.mxpTempFPGA
 * mxpTempFPGA is subid 6 of mxpEnvEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.7.1.6
 * Description:
Temperature FPGA
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the mxpTempFPGA data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param mxpTempFPGA_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param mxpTempFPGA_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by mxpTempFPGA.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*mxpTempFPGA_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update mxpTempFPGA_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
mxpTempFPGA_get( mxpEnvTable_rowreq_ctx *rowreq_ctx, char **mxpTempFPGA_val_ptr_ptr, size_t *mxpTempFPGA_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != mxpTempFPGA_val_ptr_ptr) && (NULL != *mxpTempFPGA_val_ptr_ptr));
   netsnmp_assert( NULL != mxpTempFPGA_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:mxpEnvTable:mxpTempFPGA_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the mxpTempFPGA data.
 * copy (* mxpTempFPGA_val_ptr_ptr ) data and (* mxpTempFPGA_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for mxpTempFPGA data
     */
    if ((NULL == (* mxpTempFPGA_val_ptr_ptr )) ||
        ((* mxpTempFPGA_val_ptr_len_ptr ) <
         (rowreq_ctx->data.mxpTempFPGA_len* sizeof(rowreq_ctx->data.mxpTempFPGA[0])))) {
        /*
         * allocate space for mxpTempFPGA data
         */
        (* mxpTempFPGA_val_ptr_ptr ) = malloc(rowreq_ctx->data.mxpTempFPGA_len* sizeof(rowreq_ctx->data.mxpTempFPGA[0]));
        if(NULL == (* mxpTempFPGA_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.mxpTempFPGA)\n");
            return MFD_ERROR;
        }
    }
    (* mxpTempFPGA_val_ptr_len_ptr ) = rowreq_ctx->data.mxpTempFPGA_len* sizeof(rowreq_ctx->data.mxpTempFPGA[0]);
    memcpy( (* mxpTempFPGA_val_ptr_ptr ), rowreq_ctx->data.mxpTempFPGA, rowreq_ctx->data.mxpTempFPGA_len* sizeof(rowreq_ctx->data.mxpTempFPGA[0]) );

    return MFD_SUCCESS;
} /* mxpTempFPGA_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::mxpEnvEntry.mxpTempBOARD
 * mxpTempBOARD is subid 7 of mxpEnvEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.7.1.7
 * Description:
Temperature BOARD
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the mxpTempBOARD data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param mxpTempBOARD_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param mxpTempBOARD_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by mxpTempBOARD.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*mxpTempBOARD_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update mxpTempBOARD_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
mxpTempBOARD_get( mxpEnvTable_rowreq_ctx *rowreq_ctx, char **mxpTempBOARD_val_ptr_ptr, size_t *mxpTempBOARD_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != mxpTempBOARD_val_ptr_ptr) && (NULL != *mxpTempBOARD_val_ptr_ptr));
   netsnmp_assert( NULL != mxpTempBOARD_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:mxpEnvTable:mxpTempBOARD_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the mxpTempBOARD data.
 * copy (* mxpTempBOARD_val_ptr_ptr ) data and (* mxpTempBOARD_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for mxpTempBOARD data
     */
    if ((NULL == (* mxpTempBOARD_val_ptr_ptr )) ||
        ((* mxpTempBOARD_val_ptr_len_ptr ) <
         (rowreq_ctx->data.mxpTempBOARD_len* sizeof(rowreq_ctx->data.mxpTempBOARD[0])))) {
        /*
         * allocate space for mxpTempBOARD data
         */
        (* mxpTempBOARD_val_ptr_ptr ) = malloc(rowreq_ctx->data.mxpTempBOARD_len* sizeof(rowreq_ctx->data.mxpTempBOARD[0]));
        if(NULL == (* mxpTempBOARD_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.mxpTempBOARD)\n");
            return MFD_ERROR;
        }
    }
    (* mxpTempBOARD_val_ptr_len_ptr ) = rowreq_ctx->data.mxpTempBOARD_len* sizeof(rowreq_ctx->data.mxpTempBOARD[0]);
    memcpy( (* mxpTempBOARD_val_ptr_ptr ), rowreq_ctx->data.mxpTempBOARD, rowreq_ctx->data.mxpTempBOARD_len* sizeof(rowreq_ctx->data.mxpTempBOARD[0]) );

    return MFD_SUCCESS;
} /* mxpTempBOARD_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::mxpEnvEntry.mxpTempEdfa
 * mxpTempEdfa is subid 8 of mxpEnvEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.7.1.8
 * Description:
Temperature edfa
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the mxpTempEdfa data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param mxpTempEdfa_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param mxpTempEdfa_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by mxpTempEdfa.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*mxpTempEdfa_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update mxpTempEdfa_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
mxpTempEdfa_get( mxpEnvTable_rowreq_ctx *rowreq_ctx, char **mxpTempEdfa_val_ptr_ptr, size_t *mxpTempEdfa_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != mxpTempEdfa_val_ptr_ptr) && (NULL != *mxpTempEdfa_val_ptr_ptr));
   netsnmp_assert( NULL != mxpTempEdfa_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:mxpEnvTable:mxpTempEdfa_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the mxpTempEdfa data.
 * copy (* mxpTempEdfa_val_ptr_ptr ) data and (* mxpTempEdfa_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for mxpTempEdfa data
     */
    if ((NULL == (* mxpTempEdfa_val_ptr_ptr )) ||
        ((* mxpTempEdfa_val_ptr_len_ptr ) <
         (rowreq_ctx->data.mxpTempEdfa_len* sizeof(rowreq_ctx->data.mxpTempEdfa[0])))) {
        /*
         * allocate space for mxpTempEdfa data
         */
        (* mxpTempEdfa_val_ptr_ptr ) = malloc(rowreq_ctx->data.mxpTempEdfa_len* sizeof(rowreq_ctx->data.mxpTempEdfa[0]));
        if(NULL == (* mxpTempEdfa_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.mxpTempEdfa)\n");
            return MFD_ERROR;
        }
    }
    (* mxpTempEdfa_val_ptr_len_ptr ) = rowreq_ctx->data.mxpTempEdfa_len* sizeof(rowreq_ctx->data.mxpTempEdfa[0]);
    memcpy( (* mxpTempEdfa_val_ptr_ptr ), rowreq_ctx->data.mxpTempEdfa, rowreq_ctx->data.mxpTempEdfa_len* sizeof(rowreq_ctx->data.mxpTempEdfa[0]) );

    return MFD_SUCCESS;
} /* mxpTempEdfa_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::mxpEnvEntry.mxpHumedity
 * mxpHumedity is subid 9 of mxpEnvEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.7.1.9
 * Description:
Muxponder humidity.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the mxpHumedity data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param mxpHumedity_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param mxpHumedity_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by mxpHumedity.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*mxpHumedity_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update mxpHumedity_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
mxpHumedity_get( mxpEnvTable_rowreq_ctx *rowreq_ctx, char **mxpHumedity_val_ptr_ptr, size_t *mxpHumedity_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != mxpHumedity_val_ptr_ptr) && (NULL != *mxpHumedity_val_ptr_ptr));
   netsnmp_assert( NULL != mxpHumedity_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:mxpEnvTable:mxpHumedity_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the mxpHumedity data.
 * copy (* mxpHumedity_val_ptr_ptr ) data and (* mxpHumedity_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for mxpHumedity data
     */
    if ((NULL == (* mxpHumedity_val_ptr_ptr )) ||
        ((* mxpHumedity_val_ptr_len_ptr ) <
         (rowreq_ctx->data.mxpHumedity_len* sizeof(rowreq_ctx->data.mxpHumedity[0])))) {
        /*
         * allocate space for mxpHumedity data
         */
        (* mxpHumedity_val_ptr_ptr ) = malloc(rowreq_ctx->data.mxpHumedity_len* sizeof(rowreq_ctx->data.mxpHumedity[0]));
        if(NULL == (* mxpHumedity_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.mxpHumedity)\n");
            return MFD_ERROR;
        }
    }
    (* mxpHumedity_val_ptr_len_ptr ) = rowreq_ctx->data.mxpHumedity_len* sizeof(rowreq_ctx->data.mxpHumedity[0]);
    memcpy( (* mxpHumedity_val_ptr_ptr ), rowreq_ctx->data.mxpHumedity, rowreq_ctx->data.mxpHumedity_len* sizeof(rowreq_ctx->data.mxpHumedity[0]) );

    return MFD_SUCCESS;
} /* mxpHumedity_get */



/** @} */
