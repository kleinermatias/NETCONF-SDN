/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "../../xfp2Table.h"

#include <stdio.h>
#include <sys/mman.h>
#include <sys/types.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <stdlib.h>

#include "xfp2Table_data_access.h"

#include "../../../../lib/hl_configs/definitions.h"

void shm_handler_create(Monitor *pt_monitor_struct);
Monitor *pt_monitor_struct;

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table xfp2Table
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::xfp2Table is subid 4 of mxpMIBObjects.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.2.4, length: 9
*/

/**
 * initialization for xfp2Table data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param xfp2Table_reg
 *        Pointer to xfp2Table_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
xfp2Table_init_data(xfp2Table_registration * xfp2Table_reg)
{
    DEBUGMSGTL(("verbose:xfp2Table:xfp2Table_init_data","called\n"));

    /*
     * TODO:303:o: Initialize xfp2Table data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* xfp2Table_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
xfp2Table_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:xfp2Table:xfp2Table_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to xfp2Table_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to xfp2Table_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up xfp2Table cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = XFP2TABLE_CACHE_TIMEOUT; /* seconds */
} /* xfp2Table_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before xfp2Table_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
xfp2Table_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:xfp2Table:xfp2Table_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to xfp2Table_container_shutdown\n");
        return;
    }

} /* xfp2Table_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement xfp2Table data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  xfp2Table_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
xfp2Table_container_load(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:xfp2Table:xfp2Table_container_load","called\n"));

    xfp2Table_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * xfp2Index(1)/OCTETSTR/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h
         */
   
    char xfp2Index[32];
    strcpy (xfp2Index, "xfp2Index");
    size_t xfp2Index_len = strlen(xfp2Index);

    shm_handler_create(pt_monitor_struct);

        /*
         * TODO:352:M: |   |-> set indexes in new xfp2Table rowreq context.
         * data context will be set from the param (unless NULL,
         *      in which case a new data context will be allocated)
         */
        rowreq_ctx = xfp2Table_allocate_rowreq_ctx(NULL);
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            return MFD_RESOURCE_UNAVAILABLE;
        }
        if(MFD_SUCCESS != xfp2Table_indexes_set(rowreq_ctx
                               , xfp2Index, xfp2Index_len
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "xfp2Table data.\n");
            xfp2Table_release_rowreq_ctx(rowreq_ctx);
        }

        /*
         * TODO:352:r: |   |-> populate xfp2Table data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for xfp2Pres
     * xfp2Pres(2)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2Pres data
     */
    char xfp2Pres[65535];
	strcpy (xfp2Pres, general_status_no_yes[!pt_monitor_struct->xfp_struct.xfp_presence[1]]);
	size_t xfp2Pres_len = strlen(xfp2Pres);
     
    rowreq_ctx->data.xfp2Pres_len = xfp2Pres_len* sizeof(xfp2Pres[0]);
    if ((NULL == rowreq_ctx->data.xfp2Pres) ||
        (rowreq_ctx->data.xfp2Pres_len <
         (xfp2Pres_len* sizeof(xfp2Pres[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2Pres)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.xfp2Pres, xfp2Pres, xfp2Pres_len* sizeof(xfp2Pres[0]) );
    
    /*
     * setup/save data for xfp2Loss
     * xfp2Loss(3)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2Loss data
     */
     
    char xfp2Loss[65535];
	strcpy (xfp2Loss, general_status_no_yes[pt_monitor_struct->xfp_struct.xfp_rx_loss[1]]);
	size_t xfp2Loss_len = strlen(xfp2Loss);
     
    rowreq_ctx->data.xfp2Loss_len = xfp2Loss_len* sizeof(xfp2Loss[0]);
    if ((NULL == rowreq_ctx->data.xfp2Loss) ||
        (rowreq_ctx->data.xfp2Loss_len <
         (xfp2Loss_len* sizeof(xfp2Loss[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2Loss)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2Loss, xfp2Loss, xfp2Loss_len* sizeof(xfp2Loss[0]) );
    
    /*
     * setup/save data for xfp2Rdy
     * xfp2Rdy(4)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2Rdy data
     */

    char xfp2Rdy[65535];
	strcpy (xfp2Rdy, general_status_no_yes[!pt_monitor_struct->xfp_struct.xfp_ready[1]]);
	size_t xfp2Rdy_len = strlen(xfp2Rdy);
     
    rowreq_ctx->data.xfp2Rdy_len = xfp2Rdy_len* sizeof(xfp2Rdy[0]);
    if ((NULL == rowreq_ctx->data.xfp2Rdy) ||
        (rowreq_ctx->data.xfp2Rdy_len <
         (xfp2Rdy_len* sizeof(xfp2Rdy[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2Rdy)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2Rdy, xfp2Rdy, xfp2Rdy_len* sizeof(xfp2Rdy[0]) );
    
    /*
     * setup/save data for xfp2Int
     * xfp2Int(5)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2Int data
     */
     
    char xfp2Int[65535];
	strcpy (xfp2Int, general_status_no_yes[!pt_monitor_struct->xfp_struct.xfp_interrupt[1]]);
	size_t xfp2Int_len = strlen(xfp2Int);
     
    rowreq_ctx->data.xfp2Int_len = xfp2Int_len* sizeof(xfp2Int[0]);
    if ((NULL == rowreq_ctx->data.xfp2Int) ||
        (rowreq_ctx->data.xfp2Int_len <
         (xfp2Int_len* sizeof(xfp2Int[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2Int)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.xfp2Int, xfp2Int, xfp2Int_len* sizeof(xfp2Int[0]) );
    
    /*
     * setup/save data for xfp2TxPow
     * xfp2TxPow(6)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2TxPow data
     */
     
    char xfp2TxPow[65535];
	sprintf(output_char, "%.2f", pt_monitor_struct->xfp_struct.xfp_tx_power[1]);
	strcpy (xfp2TxPow, output_char);
	size_t xfp2TxPow_len = strlen(xfp2TxPow);
     
    rowreq_ctx->data.xfp2TxPow_len = xfp2TxPow_len* sizeof(xfp2TxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp2TxPow) ||
        (rowreq_ctx->data.xfp2TxPow_len <
         (xfp2TxPow_len* sizeof(xfp2TxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2TxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2TxPow, xfp2TxPow, xfp2TxPow_len* sizeof(xfp2TxPow[0]) );
    
    /*
     * setup/save data for xfp2RxPow
     * xfp2RxPow(7)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2RxPow data
     */
     
    char xfp2RxPow[65535];
	sprintf(output_char, "%.2f", pt_monitor_struct->xfp_struct.xfp_rx_power[1]);
	strcpy (xfp2RxPow, output_char);
	size_t xfp2RxPow_len = strlen(xfp2RxPow);
     
   rowreq_ctx->data.xfp2RxPow_len = xfp2RxPow_len* sizeof(xfp2RxPow[0]);
   if ((NULL == rowreq_ctx->data.xfp2RxPow) ||
        (rowreq_ctx->data.xfp2RxPow_len <
         (xfp2RxPow_len* sizeof(xfp2RxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2RxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2RxPow, xfp2RxPow, xfp2RxPow_len* sizeof(xfp2RxPow[0]) );
    
    /*
     * setup/save data for xfp2Temp
     * xfp2Temp(8)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2Temp data
     */
     
    char xfp2Temp[65535];
	sprintf(output_char, "%.2f", pt_monitor_struct->xfp_struct.xfp_temperature[1]);
	strcpy (xfp2Temp, output_char);
	size_t xfp2Temp_len = strlen(xfp2Temp);
     
    rowreq_ctx->data.xfp2Temp_len = xfp2Temp_len* sizeof(xfp2Temp[0]);
    if ((NULL == rowreq_ctx->data.xfp2Temp) ||
        (rowreq_ctx->data.xfp2Temp_len <
         (xfp2Temp_len* sizeof(xfp2Temp[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2Temp)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2Temp, xfp2Temp, xfp2Temp_len* sizeof(xfp2Temp[0]) );
    
    /*
     * setup/save data for xfp2LowTxPow
     * xfp2LowTxPow(9)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2LowTxPow data
     */
    
    char xfp2LowTxPow[65535];
	strcpy (xfp2LowTxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[1][0]]);
	size_t xfp2LowTxPow_len = strlen(xfp2LowTxPow);
     
    rowreq_ctx->data.xfp2LowTxPow_len = xfp2LowTxPow_len* sizeof(xfp2LowTxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp2LowTxPow) ||
        (rowreq_ctx->data.xfp2LowTxPow_len <
         (xfp2LowTxPow_len* sizeof(xfp2LowTxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2LowTxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2LowTxPow, xfp2LowTxPow, xfp2LowTxPow_len* sizeof(xfp2LowTxPow[0]) );
    
    /*
     * setup/save data for xfp2HighTxPow
     * xfp2HighTxPow(10)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2HighTxPow data
     */
     
    char xfp2HighTxPow[65535];
	strcpy (xfp2HighTxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[1][1]]);
	size_t xfp2HighTxPow_len = strlen(xfp2HighTxPow);
    
    rowreq_ctx->data.xfp2HighTxPow_len = xfp2HighTxPow_len* sizeof(xfp2HighTxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp2HighTxPow) ||
        (rowreq_ctx->data.xfp2HighTxPow_len <
         (xfp2HighTxPow_len* sizeof(xfp2HighTxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2HighTxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2HighTxPow, xfp2HighTxPow, xfp2HighTxPow_len* sizeof(xfp2HighTxPow[0]) );
    
    /*
     * setup/save data for xfp2LowRxPow
     * xfp2LowRxPow(11)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2LowRxPow data
     */
     
    char xfp2LowRxPow[65535];
	strcpy (xfp2LowRxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[1][2]]);
	size_t xfp2LowRxPow_len = strlen(xfp2LowRxPow);
     
    rowreq_ctx->data.xfp2LowRxPow_len = xfp2LowRxPow_len* sizeof(xfp2LowRxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp2LowRxPow) ||
        (rowreq_ctx->data.xfp2LowRxPow_len <
         (xfp2LowRxPow_len* sizeof(xfp2LowRxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2LowRxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2LowRxPow, xfp2LowRxPow, xfp2LowRxPow_len* sizeof(xfp2LowRxPow[0]) );
    
    /*
     * setup/save data for xfp2HighRxPow
     * xfp2HighRxPow(12)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2HighRxPow data
     */
     
    char xfp2HighRxPow[65535];
	strcpy (xfp2HighRxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[1][3]]);
	size_t xfp2HighRxPow_len = strlen(xfp2HighRxPow);
     
    rowreq_ctx->data.xfp2HighRxPow_len = xfp2HighRxPow_len* sizeof(xfp2HighRxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp2HighRxPow) ||
        (rowreq_ctx->data.xfp2HighRxPow_len <
         (xfp2HighRxPow_len* sizeof(xfp2HighRxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2HighRxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2HighRxPow, xfp2HighRxPow, xfp2HighRxPow_len* sizeof(xfp2HighRxPow[0]) );
    
    /*
     * setup/save data for xfp2RxCdrLol
     * xfp2RxCdrLol(13)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2RxCdrLol data
     */
     
    char xfp2RxCdrLol[65535];
	strcpy (xfp2RxCdrLol, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[1][4]]);
	size_t xfp2RxCdrLol_len = strlen(xfp2RxCdrLol);
     
    rowreq_ctx->data.xfp2RxCdrLol_len = xfp2RxCdrLol_len* sizeof(xfp2RxCdrLol[0]);
    if ((NULL == rowreq_ctx->data.xfp2RxCdrLol) ||
        (rowreq_ctx->data.xfp2RxCdrLol_len <
         (xfp2RxCdrLol_len* sizeof(xfp2RxCdrLol[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2RxCdrLol)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2RxCdrLol, xfp2RxCdrLol, xfp2RxCdrLol_len* sizeof(xfp2RxCdrLol[0]) );
    
    /*
     * setup/save data for xfp2TxCdrLol
     * xfp2TxCdrLol(14)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2TxCdrLol data
     */
     
    char xfp2TxCdrLol[65535];
	strcpy (xfp2TxCdrLol, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[1][5]]);
	size_t xfp2TxCdrLol_len = strlen(xfp2TxCdrLol);
     
    rowreq_ctx->data.xfp2TxCdrLol_len = xfp2TxCdrLol_len* sizeof(xfp2TxCdrLol[0]);
    if ((NULL == rowreq_ctx->data.xfp2TxCdrLol) ||
        (rowreq_ctx->data.xfp2TxCdrLol_len <
         (xfp2TxCdrLol_len* sizeof(xfp2TxCdrLol[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2TxCdrLol)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp2TxCdrLol, xfp2TxCdrLol, xfp2TxCdrLol_len* sizeof(xfp2TxCdrLol[0]) );
    
    /*
     * setup/save data for xfp2LasFault
     * xfp2LasFault(15)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp2LasFault data
     */
     
    char xfp2LasFault[65535];
	strcpy (xfp2LasFault, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[1][6]]);
	size_t xfp2LasFault_len = strlen(xfp2LasFault);
     
    rowreq_ctx->data.xfp2LasFault_len = xfp2LasFault_len* sizeof(xfp2LasFault[0]);
    if ((NULL == rowreq_ctx->data.xfp2LasFault) ||
        (rowreq_ctx->data.xfp2LasFault_len <
         (xfp2LasFault_len* sizeof(xfp2LasFault[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp2LasFault)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.xfp2LasFault, xfp2LasFault, xfp2LasFault_len* sizeof(xfp2LasFault[0]) );
        
        /*
         * insert into table container
         */
        CONTAINER_INSERT(container, rowreq_ctx);
        ++count;

    DEBUGMSGT(("verbose:xfp2Table:xfp2Table_container_load",
               "inserted %d records\n", count));

    return MFD_SUCCESS;
} /* xfp2Table_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
xfp2Table_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:xfp2Table:xfp2Table_container_free","called\n"));

    /*
     * TODO:380:M: Free xfp2Table container data.
     */
} /* xfp2Table_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
xfp2Table_row_prep( xfp2Table_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:xfp2Table:xfp2Table_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* xfp2Table_row_prep */

/** @} */
