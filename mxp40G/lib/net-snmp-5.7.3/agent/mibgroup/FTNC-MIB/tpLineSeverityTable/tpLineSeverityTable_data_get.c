/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "tpLineSeverityTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement tpLineSeverityTable get routines.
 * TODO:240:M: Implement tpLineSeverityTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table tpLineSeverityTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::tpLineSeverityTable is subid 1 of tpAlarms.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1, length: 12
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement tpLineSeverityTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param tpLineSeverityIndex_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
tpLineSeverityTable_indexes_set_tbl_idx(tpLineSeverityTable_mib_index *tbl_idx, long tpLineSeverityIndex_val)
{
    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityTable_indexes_set_tbl_idx","called\n"));

    /* tpLineSeverityIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/a/w/e/R/d/h */
    tbl_idx->tpLineSeverityIndex = tpLineSeverityIndex_val;
    

    return MFD_SUCCESS;
} /* tpLineSeverityTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
tpLineSeverityTable_indexes_set(tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long tpLineSeverityIndex_val)
{
    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityTable_indexes_set","called\n"));

    if(MFD_SUCCESS != tpLineSeverityTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , tpLineSeverityIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != tpLineSeverityTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* tpLineSeverityTable_indexes_set */


/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityFramerFsmms
 * tpLineSeverityFramerFsmms is subid 2 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.2
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityFramerFsmms data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityFramerFsmms_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityFramerFsmms_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityFramerFsmms_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityFramerFsmms_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityFramerFsmms_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityFramerFsmms data.
 * copy (* tpLineSeverityFramerFsmms_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityFramerFsmms_val_ptr ) = rowreq_ctx->data.tpLineSeverityFramerFsmms;

    return MFD_SUCCESS;
} /* tpLineSeverityFramerFsmms_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityFramerFsmmtrb1s
 * tpLineSeverityFramerFsmmtrb1s is subid 3 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.3
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityFramerFsmmtrb1s data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityFramerFsmmtrb1s_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityFramerFsmmtrb1s_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityFramerFsmmtrb1s_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityFramerFsmmtrb1s_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityFramerFsmmtrb1s_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityFramerFsmmtrb1s data.
 * copy (* tpLineSeverityFramerFsmmtrb1s_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityFramerFsmmtrb1s_val_ptr ) = rowreq_ctx->data.tpLineSeverityFramerFsmmtrb1s;

    return MFD_SUCCESS;
} /* tpLineSeverityFramerFsmmtrb1s_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityFramerFsmmtrb2s
 * tpLineSeverityFramerFsmmtrb2s is subid 4 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.4
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityFramerFsmmtrb2s data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityFramerFsmmtrb2s_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityFramerFsmmtrb2s_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityFramerFsmmtrb2s_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityFramerFsmmtrb2s_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityFramerFsmmtrb2s_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityFramerFsmmtrb2s data.
 * copy (* tpLineSeverityFramerFsmmtrb2s_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityFramerFsmmtrb2s_val_ptr ) = rowreq_ctx->data.tpLineSeverityFramerFsmmtrb2s;

    return MFD_SUCCESS;
} /* tpLineSeverityFramerFsmmtrb2s_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityFramerFsmmtrb3s
 * tpLineSeverityFramerFsmmtrb3s is subid 5 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.5
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityFramerFsmmtrb3s data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityFramerFsmmtrb3s_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityFramerFsmmtrb3s_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityFramerFsmmtrb3s_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityFramerFsmmtrb3s_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityFramerFsmmtrb3s_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityFramerFsmmtrb3s data.
 * copy (* tpLineSeverityFramerFsmmtrb3s_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityFramerFsmmtrb3s_val_ptr ) = rowreq_ctx->data.tpLineSeverityFramerFsmmtrb3s;

    return MFD_SUCCESS;
} /* tpLineSeverityFramerFsmmtrb3s_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityFramerSoof
 * tpLineSeverityFramerSoof is subid 6 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.6
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityFramerSoof data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityFramerSoof_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityFramerSoof_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityFramerSoof_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityFramerSoof_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityFramerSoof_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityFramerSoof data.
 * copy (* tpLineSeverityFramerSoof_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityFramerSoof_val_ptr ) = rowreq_ctx->data.tpLineSeverityFramerSoof;

    return MFD_SUCCESS;
} /* tpLineSeverityFramerSoof_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityFramerSlof
 * tpLineSeverityFramerSlof is subid 7 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.7
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityFramerSlof data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityFramerSlof_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityFramerSlof_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityFramerSlof_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityFramerSlof_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityFramerSlof_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityFramerSlof data.
 * copy (* tpLineSeverityFramerSlof_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityFramerSlof_val_ptr ) = rowreq_ctx->data.tpLineSeverityFramerSlof;

    return MFD_SUCCESS;
} /* tpLineSeverityFramerSlof_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityFramerSif
 * tpLineSeverityFramerSif is subid 8 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.8
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityFramerSif data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityFramerSif_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityFramerSif_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityFramerSif_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityFramerSif_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityFramerSif_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityFramerSif data.
 * copy (* tpLineSeverityFramerSif_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityFramerSif_val_ptr ) = rowreq_ctx->data.tpLineSeverityFramerSif;

    return MFD_SUCCESS;
} /* tpLineSeverityFramerSif_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityFramerFst
 * tpLineSeverityFramerFst is subid 9 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.9
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityFramerFst data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityFramerFst_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityFramerFst_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityFramerFst_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityFramerFst_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityFramerFst_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityFramerFst data.
 * copy (* tpLineSeverityFramerFst_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityFramerFst_val_ptr ) = rowreq_ctx->data.tpLineSeverityFramerFst;

    return MFD_SUCCESS;
} /* tpLineSeverityFramerFst_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityLosstatLosf
 * tpLineSeverityLosstatLosf is subid 10 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.10
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityLosstatLosf data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityLosstatLosf_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityLosstatLosf_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityLosstatLosf_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityLosstatLosf_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityLosstatLosf_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityLosstatLosf data.
 * copy (* tpLineSeverityLosstatLosf_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityLosstatLosf_val_ptr ) = rowreq_ctx->data.tpLineSeverityLosstatLosf;

    return MFD_SUCCESS;
} /* tpLineSeverityLosstatLosf_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityPathmonAccstat
 * tpLineSeverityPathmonAccstat is subid 11 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.11
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityPathmonAccstat data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityPathmonAccstat_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityPathmonAccstat_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityPathmonAccstat_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityPathmonAccstat_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityPathmonAccstat_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityPathmonAccstat data.
 * copy (* tpLineSeverityPathmonAccstat_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityPathmonAccstat_val_ptr ) = rowreq_ctx->data.tpLineSeverityPathmonAccstat;

    return MFD_SUCCESS;
} /* tpLineSeverityPathmonAccstat_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityPathmonDais
 * tpLineSeverityPathmonDais is subid 12 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.12
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityPathmonDais data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityPathmonDais_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityPathmonDais_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityPathmonDais_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityPathmonDais_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityPathmonDais_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityPathmonDais data.
 * copy (* tpLineSeverityPathmonDais_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityPathmonDais_val_ptr ) = rowreq_ctx->data.tpLineSeverityPathmonDais;

    return MFD_SUCCESS;
} /* tpLineSeverityPathmonDais_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityPathmonDbdi
 * tpLineSeverityPathmonDbdi is subid 13 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.13
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityPathmonDbdi data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityPathmonDbdi_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityPathmonDbdi_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityPathmonDbdi_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityPathmonDbdi_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityPathmonDbdi_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityPathmonDbdi data.
 * copy (* tpLineSeverityPathmonDbdi_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityPathmonDbdi_val_ptr ) = rowreq_ctx->data.tpLineSeverityPathmonDbdi;

    return MFD_SUCCESS;
} /* tpLineSeverityPathmonDbdi_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityPathmonDlck
 * tpLineSeverityPathmonDlck is subid 14 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.14
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityPathmonDlck data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityPathmonDlck_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityPathmonDlck_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityPathmonDlck_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityPathmonDlck_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityPathmonDlck_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityPathmonDlck data.
 * copy (* tpLineSeverityPathmonDlck_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityPathmonDlck_val_ptr ) = rowreq_ctx->data.tpLineSeverityPathmonDlck;

    return MFD_SUCCESS;
} /* tpLineSeverityPathmonDlck_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeverityPathmonMsiunst
 * tpLineSeverityPathmonMsiunst is subid 15 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.15
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeverityPathmonMsiunst data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeverityPathmonMsiunst_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeverityPathmonMsiunst_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeverityPathmonMsiunst_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeverityPathmonMsiunst_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeverityPathmonMsiunst_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeverityPathmonMsiunst data.
 * copy (* tpLineSeverityPathmonMsiunst_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeverityPathmonMsiunst_val_ptr ) = rowreq_ctx->data.tpLineSeverityPathmonMsiunst;

    return MFD_SUCCESS;
} /* tpLineSeverityPathmonMsiunst_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeveritySecmonBipsfs
 * tpLineSeveritySecmonBipsfs is subid 16 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.16
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeveritySecmonBipsfs data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeveritySecmonBipsfs_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeveritySecmonBipsfs_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeveritySecmonBipsfs_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeveritySecmonBipsfs_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeveritySecmonBipsfs_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeveritySecmonBipsfs data.
 * copy (* tpLineSeveritySecmonBipsfs_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeveritySecmonBipsfs_val_ptr ) = rowreq_ctx->data.tpLineSeveritySecmonBipsfs;

    return MFD_SUCCESS;
} /* tpLineSeveritySecmonBipsfs_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeveritySecmonDbdi
 * tpLineSeveritySecmonDbdi is subid 17 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.17
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeveritySecmonDbdi data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeveritySecmonDbdi_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeveritySecmonDbdi_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeveritySecmonDbdi_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeveritySecmonDbdi_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeveritySecmonDbdi_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeveritySecmonDbdi data.
 * copy (* tpLineSeveritySecmonDbdi_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeveritySecmonDbdi_val_ptr ) = rowreq_ctx->data.tpLineSeveritySecmonDbdi;

    return MFD_SUCCESS;
} /* tpLineSeveritySecmonDbdi_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeveritySecmonDiae
 * tpLineSeveritySecmonDiae is subid 18 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.18
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeveritySecmonDiae data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeveritySecmonDiae_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeveritySecmonDiae_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeveritySecmonDiae_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeveritySecmonDiae_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeveritySecmonDiae_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeveritySecmonDiae data.
 * copy (* tpLineSeveritySecmonDiae_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeveritySecmonDiae_val_ptr ) = rowreq_ctx->data.tpLineSeveritySecmonDiae;

    return MFD_SUCCESS;
} /* tpLineSeveritySecmonDiae_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeveritySecmonFlom
 * tpLineSeveritySecmonFlom is subid 19 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.19
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeveritySecmonFlom data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeveritySecmonFlom_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeveritySecmonFlom_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeveritySecmonFlom_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeveritySecmonFlom_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeveritySecmonFlom_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeveritySecmonFlom data.
 * copy (* tpLineSeveritySecmonFlom_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeveritySecmonFlom_val_ptr ) = rowreq_ctx->data.tpLineSeveritySecmonFlom;

    return MFD_SUCCESS;
} /* tpLineSeveritySecmonFlom_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpLineSeverityEntry.tpLineSeveritySecmonLom
 * tpLineSeveritySecmonLom is subid 20 of tpLineSeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.1.1.20
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the tpLineSeveritySecmonLom data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpLineSeveritySecmonLom_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
tpLineSeveritySecmonLom_get( tpLineSeverityTable_rowreq_ctx *rowreq_ctx, long * tpLineSeveritySecmonLom_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != tpLineSeveritySecmonLom_val_ptr );


    DEBUGMSGTL(("verbose:tpLineSeverityTable:tpLineSeveritySecmonLom_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the tpLineSeveritySecmonLom data.
 * copy (* tpLineSeveritySecmonLom_val_ptr ) from rowreq_ctx->data
 */
    (* tpLineSeveritySecmonLom_val_ptr ) = rowreq_ctx->data.tpLineSeveritySecmonLom;

    return MFD_SUCCESS;
} /* tpLineSeveritySecmonLom_get */



/** @} */
