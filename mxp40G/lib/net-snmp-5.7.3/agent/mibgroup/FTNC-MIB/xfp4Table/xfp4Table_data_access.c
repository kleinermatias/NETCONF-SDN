/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "../../xfp4Table.h"

#include <stdio.h>
#include <sys/mman.h>
#include <sys/types.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <stdlib.h>

#include "xfp4Table_data_access.h"

#include "../../../../lib/hl_configs/definitions.h"

void shm_handler_create(Monitor *pt_monitor_struct);
Monitor *pt_monitor_struct;

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table xfp4Table
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::xfp4Table is subid 6 of mxpMIBObjects.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.2.6, length: 9
*/

/**
 * initialization for xfp4Table data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param xfp4Table_reg
 *        Pointer to xfp4Table_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
xfp4Table_init_data(xfp4Table_registration * xfp4Table_reg)
{
    DEBUGMSGTL(("verbose:xfp4Table:xfp4Table_init_data","called\n"));

    /*
     * TODO:303:o: Initialize xfp4Table data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* xfp4Table_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
xfp4Table_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:xfp4Table:xfp4Table_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to xfp4Table_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to xfp4Table_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up xfp4Table cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = XFP4TABLE_CACHE_TIMEOUT; /* seconds */
} /* xfp4Table_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before xfp4Table_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
xfp4Table_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:xfp4Table:xfp4Table_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to xfp4Table_container_shutdown\n");
        return;
    }

} /* xfp4Table_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement xfp4Table data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  xfp4Table_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
xfp4Table_container_load(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:xfp4Table:xfp4Table_container_load","called\n"));
    
    xfp4Table_rowreq_ctx *rowreq_ctx;
    size_t count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * xfp4Index(1)/OCTETSTR/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h
         */
   
    char xfp4Index[32];
    strcpy (xfp4Index, "xfp4Index");
    size_t xfp4Index_len = strlen(xfp4Index);

    shm_handler_create(pt_monitor_struct);

        /*
         * TODO:352:M: |   |-> set indexes in new xfp4Table rowreq context.
         * data context will be set from the param (unless NULL,
         *      in which case a new data context will be allocated)
         */
        rowreq_ctx = xfp4Table_allocate_rowreq_ctx(NULL);
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            return MFD_RESOURCE_UNAVAILABLE;
        }
        if(MFD_SUCCESS != xfp4Table_indexes_set(rowreq_ctx
                               , xfp4Index, xfp4Index_len
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "xfp4Table data.\n");
            xfp4Table_release_rowreq_ctx(rowreq_ctx);
        }

        /*
         * TODO:352:r: |   |-> populate xfp4Table data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for xfp4Pres
     * xfp4Pres(2)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4Pres data
     */
    char xfp4Pres[65535];
	strcpy (xfp4Pres, general_status_no_yes[!pt_monitor_struct->xfp_struct.xfp_presence[3]]);
	size_t xfp4Pres_len = strlen(xfp4Pres);
     
    rowreq_ctx->data.xfp4Pres_len = xfp4Pres_len* sizeof(xfp4Pres[0]);
    if ((NULL == rowreq_ctx->data.xfp4Pres) ||
        (rowreq_ctx->data.xfp4Pres_len <
         (xfp4Pres_len* sizeof(xfp4Pres[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4Pres)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.xfp4Pres, xfp4Pres, xfp4Pres_len* sizeof(xfp4Pres[0]) );
    
    /*
     * setup/save data for xfp4Loss
     * xfp4Loss(3)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4Loss data
     */
     
    char xfp4Loss[65535];
	strcpy (xfp4Loss, general_status_no_yes[pt_monitor_struct->xfp_struct.xfp_rx_loss[3]]);
	size_t xfp4Loss_len = strlen(xfp4Loss);
     
    rowreq_ctx->data.xfp4Loss_len = xfp4Loss_len* sizeof(xfp4Loss[0]);
    if ((NULL == rowreq_ctx->data.xfp4Loss) ||
        (rowreq_ctx->data.xfp4Loss_len <
         (xfp4Loss_len* sizeof(xfp4Loss[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4Loss)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4Loss, xfp4Loss, xfp4Loss_len* sizeof(xfp4Loss[0]) );
    
    /*
     * setup/save data for xfp4Rdy
     * xfp4Rdy(4)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4Rdy data
     */

    char xfp4Rdy[65535];
	strcpy (xfp4Rdy, general_status_no_yes[!pt_monitor_struct->xfp_struct.xfp_ready[3]]);
	size_t xfp4Rdy_len = strlen(xfp4Rdy);
     
    rowreq_ctx->data.xfp4Rdy_len = xfp4Rdy_len* sizeof(xfp4Rdy[0]);
    if ((NULL == rowreq_ctx->data.xfp4Rdy) ||
        (rowreq_ctx->data.xfp4Rdy_len <
         (xfp4Rdy_len* sizeof(xfp4Rdy[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4Rdy)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4Rdy, xfp4Rdy, xfp4Rdy_len* sizeof(xfp4Rdy[0]) );
    
    /*
     * setup/save data for xfp4Int
     * xfp4Int(5)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4Int data
     */
     
    char xfp4Int[65535];
	strcpy (xfp4Int, general_status_no_yes[!pt_monitor_struct->xfp_struct.xfp_interrupt[3]]);
	size_t xfp4Int_len = strlen(xfp4Int);
     
    rowreq_ctx->data.xfp4Int_len = xfp4Int_len* sizeof(xfp4Int[0]);
    if ((NULL == rowreq_ctx->data.xfp4Int) ||
        (rowreq_ctx->data.xfp4Int_len <
         (xfp4Int_len* sizeof(xfp4Int[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4Int)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.xfp4Int, xfp4Int, xfp4Int_len* sizeof(xfp4Int[0]) );
    
    /*
     * setup/save data for xfp4TxPow
     * xfp4TxPow(6)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4TxPow data
     */
     
    char xfp4TxPow[65535];
	sprintf(output_char, "%.2f", pt_monitor_struct->xfp_struct.xfp_tx_power[3]);
	strcpy (xfp4TxPow, output_char);
	size_t xfp4TxPow_len = strlen(xfp4TxPow);
     
    rowreq_ctx->data.xfp4TxPow_len = xfp4TxPow_len* sizeof(xfp4TxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp4TxPow) ||
        (rowreq_ctx->data.xfp4TxPow_len <
         (xfp4TxPow_len* sizeof(xfp4TxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4TxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4TxPow, xfp4TxPow, xfp4TxPow_len* sizeof(xfp4TxPow[0]) );
    
    /*
     * setup/save data for xfp4RxPow
     * xfp4RxPow(7)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4RxPow data
     */
     
    char xfp4RxPow[65535];
	sprintf(output_char, "%.2f", pt_monitor_struct->xfp_struct.xfp_rx_power[3]);
	strcpy (xfp4RxPow, output_char);
	size_t xfp4RxPow_len = strlen(xfp4RxPow);
     
   rowreq_ctx->data.xfp4RxPow_len = xfp4RxPow_len* sizeof(xfp4RxPow[0]);
   if ((NULL == rowreq_ctx->data.xfp4RxPow) ||
        (rowreq_ctx->data.xfp4RxPow_len <
         (xfp4RxPow_len* sizeof(xfp4RxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4RxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4RxPow, xfp4RxPow, xfp4RxPow_len* sizeof(xfp4RxPow[0]) );
    
    /*
     * setup/save data for xfp4Temp
     * xfp4Temp(8)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4Temp data
     */
     
    char xfp4Temp[65535];
	sprintf(output_char, "%.2f", pt_monitor_struct->xfp_struct.xfp_temperature[3]);
	strcpy (xfp4Temp, output_char);
	size_t xfp4Temp_len = strlen(xfp4Temp);
     
    rowreq_ctx->data.xfp4Temp_len = xfp4Temp_len* sizeof(xfp4Temp[0]);
    if ((NULL == rowreq_ctx->data.xfp4Temp) ||
        (rowreq_ctx->data.xfp4Temp_len <
         (xfp4Temp_len* sizeof(xfp4Temp[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4Temp)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4Temp, xfp4Temp, xfp4Temp_len* sizeof(xfp4Temp[0]) );
    
    /*
     * setup/save data for xfp4LowTxPow
     * xfp4LowTxPow(9)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4LowTxPow data
     */
    
    char xfp4LowTxPow[65535];
	strcpy (xfp4LowTxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[3][0]]);	
	size_t xfp4LowTxPow_len = strlen(xfp4LowTxPow);
     
    rowreq_ctx->data.xfp4LowTxPow_len = xfp4LowTxPow_len* sizeof(xfp4LowTxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp4LowTxPow) ||
        (rowreq_ctx->data.xfp4LowTxPow_len <
         (xfp4LowTxPow_len* sizeof(xfp4LowTxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4LowTxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4LowTxPow, xfp4LowTxPow, xfp4LowTxPow_len* sizeof(xfp4LowTxPow[0]) );
    
    /*
     * setup/save data for xfp4HighTxPow
     * xfp4HighTxPow(10)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4HighTxPow data
     */
     
    char xfp4HighTxPow[65535];
	strcpy (xfp4HighTxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[3][1]]);
	size_t xfp4HighTxPow_len = strlen(xfp4HighTxPow);
    
    rowreq_ctx->data.xfp4HighTxPow_len = xfp4HighTxPow_len* sizeof(xfp4HighTxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp4HighTxPow) ||
        (rowreq_ctx->data.xfp4HighTxPow_len <
         (xfp4HighTxPow_len* sizeof(xfp4HighTxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4HighTxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4HighTxPow, xfp4HighTxPow, xfp4HighTxPow_len* sizeof(xfp4HighTxPow[0]) );
    
    /*
     * setup/save data for xfp4LowRxPow
     * xfp4LowRxPow(11)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4LowRxPow data
     */
     
    char xfp4LowRxPow[65535];
	strcpy (xfp4LowRxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[3][2]]);
	size_t xfp4LowRxPow_len = strlen(xfp4LowRxPow);
     
    rowreq_ctx->data.xfp4LowRxPow_len = xfp4LowRxPow_len* sizeof(xfp4LowRxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp4LowRxPow) ||
        (rowreq_ctx->data.xfp4LowRxPow_len <
         (xfp4LowRxPow_len* sizeof(xfp4LowRxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4LowRxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4LowRxPow, xfp4LowRxPow, xfp4LowRxPow_len* sizeof(xfp4LowRxPow[0]) );
    
    /*
     * setup/save data for xfp4HighRxPow
     * xfp4HighRxPow(12)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4HighRxPow data
     */
     
    char xfp4HighRxPow[65535];
	strcpy (xfp4HighRxPow, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[3][3]]);
	size_t xfp4HighRxPow_len = strlen(xfp4HighRxPow);
     
    rowreq_ctx->data.xfp4HighRxPow_len = xfp4HighRxPow_len* sizeof(xfp4HighRxPow[0]);
    if ((NULL == rowreq_ctx->data.xfp4HighRxPow) ||
        (rowreq_ctx->data.xfp4HighRxPow_len <
         (xfp4HighRxPow_len* sizeof(xfp4HighRxPow[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4HighRxPow)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4HighRxPow, xfp4HighRxPow, xfp4HighRxPow_len* sizeof(xfp4HighRxPow[0]) );
    
    /*
     * setup/save data for xfp4RxCdrLol
     * xfp4RxCdrLol(13)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4RxCdrLol data
     */
     
    char xfp4RxCdrLol[65535];
	strcpy (xfp4RxCdrLol, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[3][4]]);
	size_t xfp4RxCdrLol_len = strlen(xfp4RxCdrLol);
     
    rowreq_ctx->data.xfp4RxCdrLol_len = xfp4RxCdrLol_len* sizeof(xfp4RxCdrLol[0]);
    if ((NULL == rowreq_ctx->data.xfp4RxCdrLol) ||
        (rowreq_ctx->data.xfp4RxCdrLol_len <
         (xfp4RxCdrLol_len* sizeof(xfp4RxCdrLol[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4RxCdrLol)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4RxCdrLol, xfp4RxCdrLol, xfp4RxCdrLol_len* sizeof(xfp4RxCdrLol[0]) );
    
    /*
     * setup/save data for xfp4TxCdrLol
     * xfp4TxCdrLol(14)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4TxCdrLol data
     */
     
    char xfp4TxCdrLol[65535];
	strcpy (xfp4TxCdrLol, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[3][5]]);
	size_t xfp4TxCdrLol_len = strlen(xfp4TxCdrLol);
     
    rowreq_ctx->data.xfp4TxCdrLol_len = xfp4TxCdrLol_len* sizeof(xfp4TxCdrLol[0]);
    if ((NULL == rowreq_ctx->data.xfp4TxCdrLol) ||
        (rowreq_ctx->data.xfp4TxCdrLol_len <
         (xfp4TxCdrLol_len* sizeof(xfp4TxCdrLol[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4TxCdrLol)\n");
        return MFD_ERROR;
    }    
    memcpy( rowreq_ctx->data.xfp4TxCdrLol, xfp4TxCdrLol, xfp4TxCdrLol_len* sizeof(xfp4TxCdrLol[0]) );
    
    /*
     * setup/save data for xfp4LasFault
     * xfp4LasFault(15)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for xfp4LasFault data
     */
     
    char xfp4LasFault[65535];
	strcpy (xfp4LasFault, alarm_no_Alarm[pt_monitor_struct->xfp_struct.xfp_interruption_flags[3][6]]);
	size_t xfp4LasFault_len = strlen(xfp4LasFault);
     
    rowreq_ctx->data.xfp4LasFault_len = xfp4LasFault_len* sizeof(xfp4LasFault[0]);
    if ((NULL == rowreq_ctx->data.xfp4LasFault) ||
        (rowreq_ctx->data.xfp4LasFault_len <
         (xfp4LasFault_len* sizeof(xfp4LasFault[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4LasFault)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.xfp4LasFault, xfp4LasFault, xfp4LasFault_len* sizeof(xfp4LasFault[0]) );
        
        /*
         * insert into table container
         */
        CONTAINER_INSERT(container, rowreq_ctx);
        ++count;

    DEBUGMSGT(("verbose:xfp4Table:xfp4Table_container_load",
               "inserted %d records\n", count));

    return MFD_SUCCESS;
} /* xfp4Table_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
xfp4Table_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:xfp4Table:xfp4Table_container_free","called\n"));

    /*
     * TODO:380:M: Free xfp4Table container data.
     */
} /* xfp4Table_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
xfp4Table_row_prep( xfp4Table_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:xfp4Table:xfp4Table_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* xfp4Table_row_prep */

/** @} */
