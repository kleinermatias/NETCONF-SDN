/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "../../xfp4Table.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement xfp4Table get routines.
 * TODO:240:M: Implement xfp4Table mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table xfp4Table
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::xfp4Table is subid 6 of mxpMIBObjects.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.2.6, length: 9
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement xfp4Table data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param xfp4Index_ptr
 * @param xfp4Index_ptr_len
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
xfp4Table_indexes_set_tbl_idx(xfp4Table_mib_index *tbl_idx, char *xfp4Index_val_ptr,  size_t xfp4Index_val_ptr_len)
{
    DEBUGMSGTL(("verbose:xfp4Table:xfp4Table_indexes_set_tbl_idx","called\n"));

    /* xfp4Index(1)/OCTETSTR/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h */
    tbl_idx->xfp4Index_len = sizeof(tbl_idx->xfp4Index)/sizeof(tbl_idx->xfp4Index[0]); /* max length */
    /*
     * make sure there is enough space for xfp4Index data
     */
    if ((NULL == tbl_idx->xfp4Index) ||
        (tbl_idx->xfp4Index_len <
         (xfp4Index_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value (xfp4Index_val_ptr)\n");
        return MFD_ERROR;
    }
    tbl_idx->xfp4Index_len = xfp4Index_val_ptr_len;
    memcpy( tbl_idx->xfp4Index, xfp4Index_val_ptr, xfp4Index_val_ptr_len* sizeof(xfp4Index_val_ptr[0]) );
    

    return MFD_SUCCESS;
} /* xfp4Table_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
xfp4Table_indexes_set(xfp4Table_rowreq_ctx *rowreq_ctx, char *xfp4Index_val_ptr,  size_t xfp4Index_val_ptr_len)
{
    DEBUGMSGTL(("verbose:xfp4Table:xfp4Table_indexes_set","called\n"));

    if(MFD_SUCCESS != xfp4Table_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , xfp4Index_val_ptr, xfp4Index_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != xfp4Table_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* xfp4Table_indexes_set */


/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4Pres
 * xfp4Pres is subid 2 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.2
 * Description:
XFP4 Presence
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4Pres data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4Pres_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4Pres_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4Pres.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4Pres_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4Pres_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4Pres_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4Pres_val_ptr_ptr, size_t *xfp4Pres_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4Pres_val_ptr_ptr) && (NULL != *xfp4Pres_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4Pres_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4Pres_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4Pres data.
 * copy (* xfp4Pres_val_ptr_ptr ) data and (* xfp4Pres_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4Pres data
     */
    if ((NULL == (* xfp4Pres_val_ptr_ptr )) ||
        ((* xfp4Pres_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4Pres_len* sizeof(rowreq_ctx->data.xfp4Pres[0])))) {
        /*
         * allocate space for xfp4Pres data
         */
        (* xfp4Pres_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4Pres_len* sizeof(rowreq_ctx->data.xfp4Pres[0]));
        if(NULL == (* xfp4Pres_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4Pres)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4Pres_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4Pres_len* sizeof(rowreq_ctx->data.xfp4Pres[0]);
    memcpy( (* xfp4Pres_val_ptr_ptr ), rowreq_ctx->data.xfp4Pres, rowreq_ctx->data.xfp4Pres_len* sizeof(rowreq_ctx->data.xfp4Pres[0]) );

    return MFD_SUCCESS;
} /* xfp4Pres_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4Loss
 * xfp4Loss is subid 3 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.3
 * Description:
XFP4 Loss of Signal Indicator
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4Loss data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4Loss_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4Loss_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4Loss.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4Loss_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4Loss_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4Loss_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4Loss_val_ptr_ptr, size_t *xfp4Loss_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4Loss_val_ptr_ptr) && (NULL != *xfp4Loss_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4Loss_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4Loss_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4Loss data.
 * copy (* xfp4Loss_val_ptr_ptr ) data and (* xfp4Loss_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4Loss data
     */
    if ((NULL == (* xfp4Loss_val_ptr_ptr )) ||
        ((* xfp4Loss_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4Loss_len* sizeof(rowreq_ctx->data.xfp4Loss[0])))) {
        /*
         * allocate space for xfp4Loss data
         */
        (* xfp4Loss_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4Loss_len* sizeof(rowreq_ctx->data.xfp4Loss[0]));
        if(NULL == (* xfp4Loss_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4Loss)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4Loss_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4Loss_len* sizeof(rowreq_ctx->data.xfp4Loss[0]);
    memcpy( (* xfp4Loss_val_ptr_ptr ), rowreq_ctx->data.xfp4Loss, rowreq_ctx->data.xfp4Loss_len* sizeof(rowreq_ctx->data.xfp4Loss[0]) );

    return MFD_SUCCESS;
} /* xfp4Loss_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4Rdy
 * xfp4Rdy is subid 4 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.4
 * Description:
XFP4 Ready
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4Rdy data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4Rdy_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4Rdy_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4Rdy.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4Rdy_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4Rdy_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4Rdy_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4Rdy_val_ptr_ptr, size_t *xfp4Rdy_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4Rdy_val_ptr_ptr) && (NULL != *xfp4Rdy_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4Rdy_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4Rdy_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4Rdy data.
 * copy (* xfp4Rdy_val_ptr_ptr ) data and (* xfp4Rdy_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4Rdy data
     */
    if ((NULL == (* xfp4Rdy_val_ptr_ptr )) ||
        ((* xfp4Rdy_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4Rdy_len* sizeof(rowreq_ctx->data.xfp4Rdy[0])))) {
        /*
         * allocate space for xfp4Rdy data
         */
        (* xfp4Rdy_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4Rdy_len* sizeof(rowreq_ctx->data.xfp4Rdy[0]));
        if(NULL == (* xfp4Rdy_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4Rdy)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4Rdy_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4Rdy_len* sizeof(rowreq_ctx->data.xfp4Rdy[0]);
    memcpy( (* xfp4Rdy_val_ptr_ptr ), rowreq_ctx->data.xfp4Rdy, rowreq_ctx->data.xfp4Rdy_len* sizeof(rowreq_ctx->data.xfp4Rdy[0]) );

    return MFD_SUCCESS;
} /* xfp4Rdy_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4Int
 * xfp4Int is subid 5 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.5
 * Description:
XFP4 Interruption signal
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4Int data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4Int_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4Int_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4Int.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4Int_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4Int_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4Int_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4Int_val_ptr_ptr, size_t *xfp4Int_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4Int_val_ptr_ptr) && (NULL != *xfp4Int_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4Int_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4Int_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4Int data.
 * copy (* xfp4Int_val_ptr_ptr ) data and (* xfp4Int_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4Int data
     */
    if ((NULL == (* xfp4Int_val_ptr_ptr )) ||
        ((* xfp4Int_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4Int_len* sizeof(rowreq_ctx->data.xfp4Int[0])))) {
        /*
         * allocate space for xfp4Int data
         */
        (* xfp4Int_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4Int_len* sizeof(rowreq_ctx->data.xfp4Int[0]));
        if(NULL == (* xfp4Int_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4Int)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4Int_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4Int_len* sizeof(rowreq_ctx->data.xfp4Int[0]);
    memcpy( (* xfp4Int_val_ptr_ptr ), rowreq_ctx->data.xfp4Int, rowreq_ctx->data.xfp4Int_len* sizeof(rowreq_ctx->data.xfp4Int[0]) );

    return MFD_SUCCESS;
} /* xfp4Int_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4TxPow
 * xfp4TxPow is subid 6 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.6
 * Description:
XFP4 TX Power
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4TxPow data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4TxPow_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4TxPow_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4TxPow.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4TxPow_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4TxPow_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4TxPow_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4TxPow_val_ptr_ptr, size_t *xfp4TxPow_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4TxPow_val_ptr_ptr) && (NULL != *xfp4TxPow_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4TxPow_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4TxPow_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4TxPow data.
 * copy (* xfp4TxPow_val_ptr_ptr ) data and (* xfp4TxPow_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4TxPow data
     */
    if ((NULL == (* xfp4TxPow_val_ptr_ptr )) ||
        ((* xfp4TxPow_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4TxPow_len* sizeof(rowreq_ctx->data.xfp4TxPow[0])))) {
        /*
         * allocate space for xfp4TxPow data
         */
        (* xfp4TxPow_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4TxPow_len* sizeof(rowreq_ctx->data.xfp4TxPow[0]));
        if(NULL == (* xfp4TxPow_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4TxPow)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4TxPow_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4TxPow_len* sizeof(rowreq_ctx->data.xfp4TxPow[0]);
    memcpy( (* xfp4TxPow_val_ptr_ptr ), rowreq_ctx->data.xfp4TxPow, rowreq_ctx->data.xfp4TxPow_len* sizeof(rowreq_ctx->data.xfp4TxPow[0]) );

    return MFD_SUCCESS;
} /* xfp4TxPow_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4RxPow
 * xfp4RxPow is subid 7 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.7
 * Description:
XFP4 RX Power
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4RxPow data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4RxPow_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4RxPow_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4RxPow.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4RxPow_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4RxPow_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4RxPow_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4RxPow_val_ptr_ptr, size_t *xfp4RxPow_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4RxPow_val_ptr_ptr) && (NULL != *xfp4RxPow_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4RxPow_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4RxPow_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4RxPow data.
 * copy (* xfp4RxPow_val_ptr_ptr ) data and (* xfp4RxPow_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4RxPow data
     */
    if ((NULL == (* xfp4RxPow_val_ptr_ptr )) ||
        ((* xfp4RxPow_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4RxPow_len* sizeof(rowreq_ctx->data.xfp4RxPow[0])))) {
        /*
         * allocate space for xfp4RxPow data
         */
        (* xfp4RxPow_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4RxPow_len* sizeof(rowreq_ctx->data.xfp4RxPow[0]));
        if(NULL == (* xfp4RxPow_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4RxPow)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4RxPow_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4RxPow_len* sizeof(rowreq_ctx->data.xfp4RxPow[0]);
    memcpy( (* xfp4RxPow_val_ptr_ptr ), rowreq_ctx->data.xfp4RxPow, rowreq_ctx->data.xfp4RxPow_len* sizeof(rowreq_ctx->data.xfp4RxPow[0]) );

    return MFD_SUCCESS;
} /* xfp4RxPow_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4Temp
 * xfp4Temp is subid 8 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.8
 * Description:
XFP4 Temperature
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4Temp data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4Temp_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4Temp_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4Temp.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4Temp_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4Temp_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4Temp_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4Temp_val_ptr_ptr, size_t *xfp4Temp_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4Temp_val_ptr_ptr) && (NULL != *xfp4Temp_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4Temp_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4Temp_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4Temp data.
 * copy (* xfp4Temp_val_ptr_ptr ) data and (* xfp4Temp_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4Temp data
     */
    if ((NULL == (* xfp4Temp_val_ptr_ptr )) ||
        ((* xfp4Temp_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4Temp_len* sizeof(rowreq_ctx->data.xfp4Temp[0])))) {
        /*
         * allocate space for xfp4Temp data
         */
        (* xfp4Temp_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4Temp_len* sizeof(rowreq_ctx->data.xfp4Temp[0]));
        if(NULL == (* xfp4Temp_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4Temp)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4Temp_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4Temp_len* sizeof(rowreq_ctx->data.xfp4Temp[0]);
    memcpy( (* xfp4Temp_val_ptr_ptr ), rowreq_ctx->data.xfp4Temp, rowreq_ctx->data.xfp4Temp_len* sizeof(rowreq_ctx->data.xfp4Temp[0]) );

    return MFD_SUCCESS;
} /* xfp4Temp_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4LowTxPow
 * xfp4LowTxPow is subid 9 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.9
 * Description:
XFP4 Low TX Power Alarm
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4LowTxPow data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4LowTxPow_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4LowTxPow_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4LowTxPow.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4LowTxPow_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4LowTxPow_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4LowTxPow_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4LowTxPow_val_ptr_ptr, size_t *xfp4LowTxPow_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4LowTxPow_val_ptr_ptr) && (NULL != *xfp4LowTxPow_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4LowTxPow_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4LowTxPow_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4LowTxPow data.
 * copy (* xfp4LowTxPow_val_ptr_ptr ) data and (* xfp4LowTxPow_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4LowTxPow data
     */
    if ((NULL == (* xfp4LowTxPow_val_ptr_ptr )) ||
        ((* xfp4LowTxPow_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4LowTxPow_len* sizeof(rowreq_ctx->data.xfp4LowTxPow[0])))) {
        /*
         * allocate space for xfp4LowTxPow data
         */
        (* xfp4LowTxPow_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4LowTxPow_len* sizeof(rowreq_ctx->data.xfp4LowTxPow[0]));
        if(NULL == (* xfp4LowTxPow_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4LowTxPow)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4LowTxPow_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4LowTxPow_len* sizeof(rowreq_ctx->data.xfp4LowTxPow[0]);
    memcpy( (* xfp4LowTxPow_val_ptr_ptr ), rowreq_ctx->data.xfp4LowTxPow, rowreq_ctx->data.xfp4LowTxPow_len* sizeof(rowreq_ctx->data.xfp4LowTxPow[0]) );

    return MFD_SUCCESS;
} /* xfp4LowTxPow_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4HighTxPow
 * xfp4HighTxPow is subid 10 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.10
 * Description:
XFP4 High TX Power Alarm
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4HighTxPow data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4HighTxPow_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4HighTxPow_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4HighTxPow.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4HighTxPow_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4HighTxPow_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4HighTxPow_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4HighTxPow_val_ptr_ptr, size_t *xfp4HighTxPow_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4HighTxPow_val_ptr_ptr) && (NULL != *xfp4HighTxPow_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4HighTxPow_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4HighTxPow_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4HighTxPow data.
 * copy (* xfp4HighTxPow_val_ptr_ptr ) data and (* xfp4HighTxPow_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4HighTxPow data
     */
    if ((NULL == (* xfp4HighTxPow_val_ptr_ptr )) ||
        ((* xfp4HighTxPow_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4HighTxPow_len* sizeof(rowreq_ctx->data.xfp4HighTxPow[0])))) {
        /*
         * allocate space for xfp4HighTxPow data
         */
        (* xfp4HighTxPow_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4HighTxPow_len* sizeof(rowreq_ctx->data.xfp4HighTxPow[0]));
        if(NULL == (* xfp4HighTxPow_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4HighTxPow)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4HighTxPow_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4HighTxPow_len* sizeof(rowreq_ctx->data.xfp4HighTxPow[0]);
    memcpy( (* xfp4HighTxPow_val_ptr_ptr ), rowreq_ctx->data.xfp4HighTxPow, rowreq_ctx->data.xfp4HighTxPow_len* sizeof(rowreq_ctx->data.xfp4HighTxPow[0]) );

    return MFD_SUCCESS;
} /* xfp4HighTxPow_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4LowRxPow
 * xfp4LowRxPow is subid 11 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.11
 * Description:
XFP4 High RX Power Alarm
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4LowRxPow data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4LowRxPow_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4LowRxPow_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4LowRxPow.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4LowRxPow_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4LowRxPow_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4LowRxPow_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4LowRxPow_val_ptr_ptr, size_t *xfp4LowRxPow_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4LowRxPow_val_ptr_ptr) && (NULL != *xfp4LowRxPow_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4LowRxPow_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4LowRxPow_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4LowRxPow data.
 * copy (* xfp4LowRxPow_val_ptr_ptr ) data and (* xfp4LowRxPow_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4LowRxPow data
     */
    if ((NULL == (* xfp4LowRxPow_val_ptr_ptr )) ||
        ((* xfp4LowRxPow_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4LowRxPow_len* sizeof(rowreq_ctx->data.xfp4LowRxPow[0])))) {
        /*
         * allocate space for xfp4LowRxPow data
         */
        (* xfp4LowRxPow_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4LowRxPow_len* sizeof(rowreq_ctx->data.xfp4LowRxPow[0]));
        if(NULL == (* xfp4LowRxPow_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4LowRxPow)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4LowRxPow_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4LowRxPow_len* sizeof(rowreq_ctx->data.xfp4LowRxPow[0]);
    memcpy( (* xfp4LowRxPow_val_ptr_ptr ), rowreq_ctx->data.xfp4LowRxPow, rowreq_ctx->data.xfp4LowRxPow_len* sizeof(rowreq_ctx->data.xfp4LowRxPow[0]) );

    return MFD_SUCCESS;
} /* xfp4LowRxPow_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4HighRxPow
 * xfp4HighRxPow is subid 12 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.12
 * Description:
XFP4 High RX Power Alarm
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4HighRxPow data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4HighRxPow_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4HighRxPow_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4HighRxPow.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4HighRxPow_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4HighRxPow_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4HighRxPow_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4HighRxPow_val_ptr_ptr, size_t *xfp4HighRxPow_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4HighRxPow_val_ptr_ptr) && (NULL != *xfp4HighRxPow_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4HighRxPow_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4HighRxPow_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4HighRxPow data.
 * copy (* xfp4HighRxPow_val_ptr_ptr ) data and (* xfp4HighRxPow_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4HighRxPow data
     */
    if ((NULL == (* xfp4HighRxPow_val_ptr_ptr )) ||
        ((* xfp4HighRxPow_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4HighRxPow_len* sizeof(rowreq_ctx->data.xfp4HighRxPow[0])))) {
        /*
         * allocate space for xfp4HighRxPow data
         */
        (* xfp4HighRxPow_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4HighRxPow_len* sizeof(rowreq_ctx->data.xfp4HighRxPow[0]));
        if(NULL == (* xfp4HighRxPow_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4HighRxPow)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4HighRxPow_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4HighRxPow_len* sizeof(rowreq_ctx->data.xfp4HighRxPow[0]);
    memcpy( (* xfp4HighRxPow_val_ptr_ptr ), rowreq_ctx->data.xfp4HighRxPow, rowreq_ctx->data.xfp4HighRxPow_len* sizeof(rowreq_ctx->data.xfp4HighRxPow[0]) );

    return MFD_SUCCESS;
} /* xfp4HighRxPow_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4RxCdrLol
 * xfp4RxCdrLol is subid 13 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.13
 * Description:
XFP4 RX Loss of Lock Alarm
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4RxCdrLol data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4RxCdrLol_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4RxCdrLol_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4RxCdrLol.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4RxCdrLol_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4RxCdrLol_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4RxCdrLol_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4RxCdrLol_val_ptr_ptr, size_t *xfp4RxCdrLol_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4RxCdrLol_val_ptr_ptr) && (NULL != *xfp4RxCdrLol_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4RxCdrLol_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4RxCdrLol_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4RxCdrLol data.
 * copy (* xfp4RxCdrLol_val_ptr_ptr ) data and (* xfp4RxCdrLol_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4RxCdrLol data
     */
    if ((NULL == (* xfp4RxCdrLol_val_ptr_ptr )) ||
        ((* xfp4RxCdrLol_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4RxCdrLol_len* sizeof(rowreq_ctx->data.xfp4RxCdrLol[0])))) {
        /*
         * allocate space for xfp4RxCdrLol data
         */
        (* xfp4RxCdrLol_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4RxCdrLol_len* sizeof(rowreq_ctx->data.xfp4RxCdrLol[0]));
        if(NULL == (* xfp4RxCdrLol_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4RxCdrLol)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4RxCdrLol_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4RxCdrLol_len* sizeof(rowreq_ctx->data.xfp4RxCdrLol[0]);
    memcpy( (* xfp4RxCdrLol_val_ptr_ptr ), rowreq_ctx->data.xfp4RxCdrLol, rowreq_ctx->data.xfp4RxCdrLol_len* sizeof(rowreq_ctx->data.xfp4RxCdrLol[0]) );

    return MFD_SUCCESS;
} /* xfp4RxCdrLol_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4TxCdrLol
 * xfp4TxCdrLol is subid 14 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.14
 * Description:
XFP4 TX Loss of Lock Alarm
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4TxCdrLol data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4TxCdrLol_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4TxCdrLol_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4TxCdrLol.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4TxCdrLol_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4TxCdrLol_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4TxCdrLol_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4TxCdrLol_val_ptr_ptr, size_t *xfp4TxCdrLol_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4TxCdrLol_val_ptr_ptr) && (NULL != *xfp4TxCdrLol_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4TxCdrLol_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4TxCdrLol_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4TxCdrLol data.
 * copy (* xfp4TxCdrLol_val_ptr_ptr ) data and (* xfp4TxCdrLol_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4TxCdrLol data
     */
    if ((NULL == (* xfp4TxCdrLol_val_ptr_ptr )) ||
        ((* xfp4TxCdrLol_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4TxCdrLol_len* sizeof(rowreq_ctx->data.xfp4TxCdrLol[0])))) {
        /*
         * allocate space for xfp4TxCdrLol data
         */
        (* xfp4TxCdrLol_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4TxCdrLol_len* sizeof(rowreq_ctx->data.xfp4TxCdrLol[0]));
        if(NULL == (* xfp4TxCdrLol_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4TxCdrLol)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4TxCdrLol_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4TxCdrLol_len* sizeof(rowreq_ctx->data.xfp4TxCdrLol[0]);
    memcpy( (* xfp4TxCdrLol_val_ptr_ptr ), rowreq_ctx->data.xfp4TxCdrLol, rowreq_ctx->data.xfp4TxCdrLol_len* sizeof(rowreq_ctx->data.xfp4TxCdrLol[0]) );

    return MFD_SUCCESS;
} /* xfp4TxCdrLol_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::xfp4Entry.xfp4LasFault
 * xfp4LasFault is subid 15 of xfp4Entry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.6.1.15
 * Description:
XFP4 Laser Fault Alarm
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the xfp4LasFault data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param xfp4LasFault_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param xfp4LasFault_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by xfp4LasFault.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*xfp4LasFault_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update xfp4LasFault_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
xfp4LasFault_get( xfp4Table_rowreq_ctx *rowreq_ctx, char **xfp4LasFault_val_ptr_ptr, size_t *xfp4LasFault_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != xfp4LasFault_val_ptr_ptr) && (NULL != *xfp4LasFault_val_ptr_ptr));
   netsnmp_assert( NULL != xfp4LasFault_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:xfp4Table:xfp4LasFault_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the xfp4LasFault data.
 * copy (* xfp4LasFault_val_ptr_ptr ) data and (* xfp4LasFault_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for xfp4LasFault data
     */
    if ((NULL == (* xfp4LasFault_val_ptr_ptr )) ||
        ((* xfp4LasFault_val_ptr_len_ptr ) <
         (rowreq_ctx->data.xfp4LasFault_len* sizeof(rowreq_ctx->data.xfp4LasFault[0])))) {
        /*
         * allocate space for xfp4LasFault data
         */
        (* xfp4LasFault_val_ptr_ptr ) = malloc(rowreq_ctx->data.xfp4LasFault_len* sizeof(rowreq_ctx->data.xfp4LasFault[0]));
        if(NULL == (* xfp4LasFault_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.xfp4LasFault)\n");
            return MFD_ERROR;
        }
    }
    (* xfp4LasFault_val_ptr_len_ptr ) = rowreq_ctx->data.xfp4LasFault_len* sizeof(rowreq_ctx->data.xfp4LasFault[0]);
    memcpy( (* xfp4LasFault_val_ptr_ptr ), rowreq_ctx->data.xfp4LasFault, rowreq_ctx->data.xfp4LasFault_len* sizeof(rowreq_ctx->data.xfp4LasFault[0]) );

    return MFD_SUCCESS;
} /* xfp4LasFault_get */



/** @} */
