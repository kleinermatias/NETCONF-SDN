/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "tpClient1SeverityTable.h"

#include <stdio.h>
#include <sys/mman.h>
#include <sys/types.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <stdlib.h>
#include "../../../../lib/hl_configs/definitions.h"

void shm_handler_create(Monitor *pt_monitor_struct);
Monitor *pt_monitor_struct;

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table tpClient1SeverityTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::tpClient1SeverityTable is subid 2 of tpAlarms.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * tpClient1SeverityTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * tpClient1SeverityTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
tpClient1SeverityTable_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup tpClient1SeverityTable undo.
     * set up tpClient1SeverityTable undo information, in preparation for a set.
     * Undo storage is in (* tpClient1SeveritySecmonLom_val_ptr )*
     */

    return rc;
} /* tpClient1SeverityTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
tpClient1SeverityTable_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> tpClient1SeverityTable undo.
     * tpClient1SeverityTable undo information, in response to a failed set.
     * Undo storage is in (* tpClient1SeveritySecmonLom_val_ptr )*
     */

    return rc;
} /* tpClient1SeverityTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
tpClient1SeverityTable_undo_cleanup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup tpClient1SeverityTable undo.
     * Undo storage is in (* tpClient1SeveritySecmonLom_val_ptr )*
     */

    return rc;
} /* tpClient1SeverityTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * tpClient1SeverityTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param tpClient1SeverityTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
tpClient1SeverityTable_commit( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit tpClient1SeverityTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_TPCLIENT1SEVERITYFRAMERFSMMS_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYFRAMERFSMMS_FLAG; /* clear tpClient1SeverityFramerFsmms */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityFramerFsmms.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityFramerFsmms commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityFramerFsmms
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYFRAMERFSMMS_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYFRAMERFSSUSTS_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYFRAMERFSSUSTS_FLAG; /* clear tpClient1SeverityFramerFssusts */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityFramerFssusts.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityFramerFssusts commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityFramerFssusts
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYFRAMERFSSUSTS_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYFRAMERSOOF_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYFRAMERSOOF_FLAG; /* clear tpClient1SeverityFramerSoof */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityFramerSoof.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityFramerSoof commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityFramerSoof
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYFRAMERSOOF_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYFRAMERSLOF_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYFRAMERSLOF_FLAG; /* clear tpClient1SeverityFramerSlof */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityFramerSlof.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityFramerSlof commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityFramerSlof
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYFRAMERSLOF_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYFRAMERSIF_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYFRAMERSIF_FLAG; /* clear tpClient1SeverityFramerSif */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityFramerSif.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityFramerSif commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityFramerSif
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYFRAMERSIF_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYFRAMERFST_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYFRAMERFST_FLAG; /* clear tpClient1SeverityFramerFst */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityFramerFst.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityFramerFst commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityFramerFst
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYFRAMERFST_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYLOSSTATLOSF_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYLOSSTATLOSF_FLAG; /* clear tpClient1SeverityLosstatLosf */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityLosstatLosf.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityLosstatLosf commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityLosstatLosf
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYLOSSTATLOSF_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYPATHMONACCSTAT_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYPATHMONACCSTAT_FLAG; /* clear tpClient1SeverityPathmonAccstat */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityPathmonAccstat.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityPathmonAccstat commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityPathmonAccstat
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYPATHMONACCSTAT_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYPATHMONDAIS_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYPATHMONDAIS_FLAG; /* clear tpClient1SeverityPathmonDais */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityPathmonDais.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityPathmonDais commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityPathmonDais
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYPATHMONDAIS_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYPATHMONDBDI_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYPATHMONDBDI_FLAG; /* clear tpClient1SeverityPathmonDbdi */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityPathmonDbdi.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityPathmonDbdi commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityPathmonDbdi
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYPATHMONDBDI_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYPATHMONDLCK_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYPATHMONDLCK_FLAG; /* clear tpClient1SeverityPathmonDlck */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityPathmonDlck.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityPathmonDlck commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityPathmonDlck
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYPATHMONDLCK_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYPATHMONMSIUNST_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYPATHMONMSIUNST_FLAG; /* clear tpClient1SeverityPathmonMsiunst */
       /*
        * TODO:482:o: |-> commit column tpClient1SeverityPathmonMsiunst.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeverityPathmonMsiunst commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeverityPathmonMsiunst
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYPATHMONMSIUNST_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYSECMONBIPSFS_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYSECMONBIPSFS_FLAG; /* clear tpClient1SeveritySecmonBipsfs */
       /*
        * TODO:482:o: |-> commit column tpClient1SeveritySecmonBipsfs.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeveritySecmonBipsfs commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeveritySecmonBipsfs
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYSECMONBIPSFS_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYSECMONDBDI_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYSECMONDBDI_FLAG; /* clear tpClient1SeveritySecmonDbdi */
       /*
        * TODO:482:o: |-> commit column tpClient1SeveritySecmonDbdi.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeveritySecmonDbdi commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeveritySecmonDbdi
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYSECMONDBDI_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYSECMONDIAE_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYSECMONDIAE_FLAG; /* clear tpClient1SeveritySecmonDiae */
       /*
        * TODO:482:o: |-> commit column tpClient1SeveritySecmonDiae.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeveritySecmonDiae commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeveritySecmonDiae
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYSECMONDIAE_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYSECMONFLOM_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYSECMONFLOM_FLAG; /* clear tpClient1SeveritySecmonFlom */
       /*
        * TODO:482:o: |-> commit column tpClient1SeveritySecmonFlom.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeveritySecmonFlom commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeveritySecmonFlom
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYSECMONFLOM_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT1SEVERITYSECMONLOM_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT1SEVERITYSECMONLOM_FLAG; /* clear tpClient1SeveritySecmonLom */
       /*
        * TODO:482:o: |-> commit column tpClient1SeveritySecmonLom.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient1SeverityTable column tpClient1SeveritySecmonLom commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient1SeveritySecmonLom
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT1SEVERITYSECMONLOM_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* tpClient1SeverityTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * tpClient1SeverityTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param tpClient1SeverityTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
tpClient1SeverityTable_undo_commit( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo tpClient1SeverityTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* tpClient1SeverityTable_undo_commit */

/*
 * TODO:440:M: Implement tpClient1SeverityTable node value checks.
 * TODO:450:M: Implement tpClient1SeverityTable undo functions.
 * TODO:460:M: Implement tpClient1SeverityTable set functions.
 * TODO:480:M: Implement tpClient1SeverityTable commit functions.
 */
/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityFramerFsmms
 * tpClient1SeverityFramerFsmms is subid 2 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.2
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityFramerFsmms_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityFramerFsmms_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerFsmms_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFsmms_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityFramerFsmms value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityFramerFsmms value not illegal */
} /* tpClient1SeverityFramerFsmms_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityFramerFsmms_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFsmms_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityFramerFsmms undo.
     */
    /*
     * copy tpClient1SeverityFramerFsmms data
     * set rowreq_ctx->undo->tpClient1SeverityFramerFsmms from rowreq_ctx->data.tpClient1SeverityFramerFsmms
     */
    rowreq_ctx->undo->tpClient1SeverityFramerFsmms = rowreq_ctx->data.tpClient1SeverityFramerFsmms;


    return MFD_SUCCESS;
} /* tpClient1SeverityFramerFsmms_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityFramerFsmms_val
 *        A long containing the new value.
 */
int
tpClient1SeverityFramerFsmms_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerFsmms_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFsmms_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityFramerFsmms value.
     * set tpClient1SeverityFramerFsmms value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityFramerFsmms = tpClient1SeverityFramerFsmms_val;

    // Set severity on shared memory
    setTpClient1Severity("FramerFsmms", tpClient1SeverityFramerFsmms_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityFramerFsmms_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityFramerFsmms_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFsmms_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityFramerFsmms undo.
     */
    /*
     * copy tpClient1SeverityFramerFsmms data
     * set rowreq_ctx->data.tpClient1SeverityFramerFsmms from rowreq_ctx->undo->tpClient1SeverityFramerFsmms
     */
    rowreq_ctx->data.tpClient1SeverityFramerFsmms = rowreq_ctx->undo->tpClient1SeverityFramerFsmms;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityFramerFsmms_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityFramerFssusts
 * tpClient1SeverityFramerFssusts is subid 3 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.3
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityFramerFssusts_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityFramerFssusts_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerFssusts_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFssusts_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityFramerFssusts value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityFramerFssusts value not illegal */
} /* tpClient1SeverityFramerFssusts_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityFramerFssusts_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFssusts_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityFramerFssusts undo.
     */
    /*
     * copy tpClient1SeverityFramerFssusts data
     * set rowreq_ctx->undo->tpClient1SeverityFramerFssusts from rowreq_ctx->data.tpClient1SeverityFramerFssusts
     */
    rowreq_ctx->undo->tpClient1SeverityFramerFssusts = rowreq_ctx->data.tpClient1SeverityFramerFssusts;


    return MFD_SUCCESS;
} /* tpClient1SeverityFramerFssusts_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityFramerFssusts_val
 *        A long containing the new value.
 */
int
tpClient1SeverityFramerFssusts_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerFssusts_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFssusts_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityFramerFssusts value.
     * set tpClient1SeverityFramerFssusts value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityFramerFssusts = tpClient1SeverityFramerFssusts_val;

    // Set severity on shared memory
    setTpClient1Severity("FramerFssusts", tpClient1SeverityFramerFssusts_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityFramerFssusts_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityFramerFssusts_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFssusts_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityFramerFssusts undo.
     */
    /*
     * copy tpClient1SeverityFramerFssusts data
     * set rowreq_ctx->data.tpClient1SeverityFramerFssusts from rowreq_ctx->undo->tpClient1SeverityFramerFssusts
     */
    rowreq_ctx->data.tpClient1SeverityFramerFssusts = rowreq_ctx->undo->tpClient1SeverityFramerFssusts;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityFramerFssusts_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityFramerSoof
 * tpClient1SeverityFramerSoof is subid 4 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.4
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityFramerSoof_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityFramerSoof_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerSoof_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSoof_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityFramerSoof value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityFramerSoof value not illegal */
} /* tpClient1SeverityFramerSoof_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityFramerSoof_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSoof_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityFramerSoof undo.
     */
    /*
     * copy tpClient1SeverityFramerSoof data
     * set rowreq_ctx->undo->tpClient1SeverityFramerSoof from rowreq_ctx->data.tpClient1SeverityFramerSoof
     */
    rowreq_ctx->undo->tpClient1SeverityFramerSoof = rowreq_ctx->data.tpClient1SeverityFramerSoof;


    return MFD_SUCCESS;
} /* tpClient1SeverityFramerSoof_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityFramerSoof_val
 *        A long containing the new value.
 */
int
tpClient1SeverityFramerSoof_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerSoof_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSoof_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityFramerSoof value.
     * set tpClient1SeverityFramerSoof value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityFramerSoof = tpClient1SeverityFramerSoof_val;

    // Set severity on shared memory
    setTpClient1Severity("FramerSoof", tpClient1SeverityFramerSoof_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityFramerSoof_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityFramerSoof_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSoof_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityFramerSoof undo.
     */
    /*
     * copy tpClient1SeverityFramerSoof data
     * set rowreq_ctx->data.tpClient1SeverityFramerSoof from rowreq_ctx->undo->tpClient1SeverityFramerSoof
     */
    rowreq_ctx->data.tpClient1SeverityFramerSoof = rowreq_ctx->undo->tpClient1SeverityFramerSoof;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityFramerSoof_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityFramerSlof
 * tpClient1SeverityFramerSlof is subid 5 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.5
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityFramerSlof_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityFramerSlof_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerSlof_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSlof_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityFramerSlof value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityFramerSlof value not illegal */
} /* tpClient1SeverityFramerSlof_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityFramerSlof_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSlof_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityFramerSlof undo.
     */
    /*
     * copy tpClient1SeverityFramerSlof data
     * set rowreq_ctx->undo->tpClient1SeverityFramerSlof from rowreq_ctx->data.tpClient1SeverityFramerSlof
     */
    rowreq_ctx->undo->tpClient1SeverityFramerSlof = rowreq_ctx->data.tpClient1SeverityFramerSlof;


    return MFD_SUCCESS;
} /* tpClient1SeverityFramerSlof_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityFramerSlof_val
 *        A long containing the new value.
 */
int
tpClient1SeverityFramerSlof_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerSlof_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSlof_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityFramerSlof value.
     * set tpClient1SeverityFramerSlof value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityFramerSlof = tpClient1SeverityFramerSlof_val;

    // Set severity on shared memory
    setTpClient1Severity("FramerSlof", tpClient1SeverityFramerSlof_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityFramerSlof_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityFramerSlof_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSlof_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityFramerSlof undo.
     */
    /*
     * copy tpClient1SeverityFramerSlof data
     * set rowreq_ctx->data.tpClient1SeverityFramerSlof from rowreq_ctx->undo->tpClient1SeverityFramerSlof
     */
    rowreq_ctx->data.tpClient1SeverityFramerSlof = rowreq_ctx->undo->tpClient1SeverityFramerSlof;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityFramerSlof_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityFramerSif
 * tpClient1SeverityFramerSif is subid 6 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.6
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityFramerSif_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityFramerSif_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerSif_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSif_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityFramerSif value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityFramerSif value not illegal */
} /* tpClient1SeverityFramerSif_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityFramerSif_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSif_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityFramerSif undo.
     */
    /*
     * copy tpClient1SeverityFramerSif data
     * set rowreq_ctx->undo->tpClient1SeverityFramerSif from rowreq_ctx->data.tpClient1SeverityFramerSif
     */
    rowreq_ctx->undo->tpClient1SeverityFramerSif = rowreq_ctx->data.tpClient1SeverityFramerSif;


    return MFD_SUCCESS;
} /* tpClient1SeverityFramerSif_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityFramerSif_val
 *        A long containing the new value.
 */
int
tpClient1SeverityFramerSif_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerSif_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSif_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityFramerSif value.
     * set tpClient1SeverityFramerSif value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityFramerSif = tpClient1SeverityFramerSif_val;

    // Set severity on shared memory
    setTpClient1Severity("FramerSif", tpClient1SeverityFramerSif_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityFramerSif_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityFramerSif_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerSif_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityFramerSif undo.
     */
    /*
     * copy tpClient1SeverityFramerSif data
     * set rowreq_ctx->data.tpClient1SeverityFramerSif from rowreq_ctx->undo->tpClient1SeverityFramerSif
     */
    rowreq_ctx->data.tpClient1SeverityFramerSif = rowreq_ctx->undo->tpClient1SeverityFramerSif;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityFramerSif_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityFramerFst
 * tpClient1SeverityFramerFst is subid 7 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.7
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityFramerFst_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityFramerFst_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerFst_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFst_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityFramerFst value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityFramerFst value not illegal */
} /* tpClient1SeverityFramerFst_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityFramerFst_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFst_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityFramerFst undo.
     */
    /*
     * copy tpClient1SeverityFramerFst data
     * set rowreq_ctx->undo->tpClient1SeverityFramerFst from rowreq_ctx->data.tpClient1SeverityFramerFst
     */
    rowreq_ctx->undo->tpClient1SeverityFramerFst = rowreq_ctx->data.tpClient1SeverityFramerFst;


    return MFD_SUCCESS;
} /* tpClient1SeverityFramerFst_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityFramerFst_val
 *        A long containing the new value.
 */
int
tpClient1SeverityFramerFst_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityFramerFst_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFst_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityFramerFst value.
     * set tpClient1SeverityFramerFst value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityFramerFst = tpClient1SeverityFramerFst_val;

    // Set severity on shared memory
    setTpClient1Severity("FramerFst", tpClient1SeverityFramerFst_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityFramerFst_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityFramerFst_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityFramerFst_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityFramerFst undo.
     */
    /*
     * copy tpClient1SeverityFramerFst data
     * set rowreq_ctx->data.tpClient1SeverityFramerFst from rowreq_ctx->undo->tpClient1SeverityFramerFst
     */
    rowreq_ctx->data.tpClient1SeverityFramerFst = rowreq_ctx->undo->tpClient1SeverityFramerFst;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityFramerFst_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityLosstatLosf
 * tpClient1SeverityLosstatLosf is subid 8 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.8
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityLosstatLosf_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityLosstatLosf_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityLosstatLosf_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityLosstatLosf_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityLosstatLosf value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityLosstatLosf value not illegal */
} /* tpClient1SeverityLosstatLosf_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityLosstatLosf_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityLosstatLosf_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityLosstatLosf undo.
     */
    /*
     * copy tpClient1SeverityLosstatLosf data
     * set rowreq_ctx->undo->tpClient1SeverityLosstatLosf from rowreq_ctx->data.tpClient1SeverityLosstatLosf
     */
    rowreq_ctx->undo->tpClient1SeverityLosstatLosf = rowreq_ctx->data.tpClient1SeverityLosstatLosf;


    return MFD_SUCCESS;
} /* tpClient1SeverityLosstatLosf_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityLosstatLosf_val
 *        A long containing the new value.
 */
int
tpClient1SeverityLosstatLosf_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityLosstatLosf_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityLosstatLosf_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityLosstatLosf value.
     * set tpClient1SeverityLosstatLosf value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityLosstatLosf = tpClient1SeverityLosstatLosf_val;

    // Set severity on shared memory
    setTpClient1Severity("LosstatLosf", tpClient1SeverityLosstatLosf_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityLosstatLosf_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityLosstatLosf_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityLosstatLosf_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityLosstatLosf undo.
     */
    /*
     * copy tpClient1SeverityLosstatLosf data
     * set rowreq_ctx->data.tpClient1SeverityLosstatLosf from rowreq_ctx->undo->tpClient1SeverityLosstatLosf
     */
    rowreq_ctx->data.tpClient1SeverityLosstatLosf = rowreq_ctx->undo->tpClient1SeverityLosstatLosf;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityLosstatLosf_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityPathmonAccstat
 * tpClient1SeverityPathmonAccstat is subid 9 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.9
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityPathmonAccstat_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityPathmonAccstat_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityPathmonAccstat_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonAccstat_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityPathmonAccstat value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityPathmonAccstat value not illegal */
} /* tpClient1SeverityPathmonAccstat_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityPathmonAccstat_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonAccstat_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityPathmonAccstat undo.
     */
    /*
     * copy tpClient1SeverityPathmonAccstat data
     * set rowreq_ctx->undo->tpClient1SeverityPathmonAccstat from rowreq_ctx->data.tpClient1SeverityPathmonAccstat
     */
    rowreq_ctx->undo->tpClient1SeverityPathmonAccstat = rowreq_ctx->data.tpClient1SeverityPathmonAccstat;


    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonAccstat_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityPathmonAccstat_val
 *        A long containing the new value.
 */
int
tpClient1SeverityPathmonAccstat_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityPathmonAccstat_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonAccstat_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityPathmonAccstat value.
     * set tpClient1SeverityPathmonAccstat value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityPathmonAccstat = tpClient1SeverityPathmonAccstat_val;

    // Set severity on shared memory
    setTpClient1Severity("PathmonAccstat", tpClient1SeverityPathmonAccstat_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonAccstat_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityPathmonAccstat_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonAccstat_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityPathmonAccstat undo.
     */
    /*
     * copy tpClient1SeverityPathmonAccstat data
     * set rowreq_ctx->data.tpClient1SeverityPathmonAccstat from rowreq_ctx->undo->tpClient1SeverityPathmonAccstat
     */
    rowreq_ctx->data.tpClient1SeverityPathmonAccstat = rowreq_ctx->undo->tpClient1SeverityPathmonAccstat;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonAccstat_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityPathmonDais
 * tpClient1SeverityPathmonDais is subid 10 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.10
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityPathmonDais_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityPathmonDais_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityPathmonDais_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDais_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityPathmonDais value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityPathmonDais value not illegal */
} /* tpClient1SeverityPathmonDais_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityPathmonDais_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDais_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityPathmonDais undo.
     */
    /*
     * copy tpClient1SeverityPathmonDais data
     * set rowreq_ctx->undo->tpClient1SeverityPathmonDais from rowreq_ctx->data.tpClient1SeverityPathmonDais
     */
    rowreq_ctx->undo->tpClient1SeverityPathmonDais = rowreq_ctx->data.tpClient1SeverityPathmonDais;


    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonDais_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityPathmonDais_val
 *        A long containing the new value.
 */
int
tpClient1SeverityPathmonDais_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityPathmonDais_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDais_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityPathmonDais value.
     * set tpClient1SeverityPathmonDais value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityPathmonDais = tpClient1SeverityPathmonDais_val;

    // Set severity on shared memory
    setTpClient1Severity("PathmonDais", tpClient1SeverityPathmonDais_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonDais_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityPathmonDais_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDais_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityPathmonDais undo.
     */
    /*
     * copy tpClient1SeverityPathmonDais data
     * set rowreq_ctx->data.tpClient1SeverityPathmonDais from rowreq_ctx->undo->tpClient1SeverityPathmonDais
     */
    rowreq_ctx->data.tpClient1SeverityPathmonDais = rowreq_ctx->undo->tpClient1SeverityPathmonDais;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonDais_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityPathmonDbdi
 * tpClient1SeverityPathmonDbdi is subid 11 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.11
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityPathmonDbdi_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityPathmonDbdi_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityPathmonDbdi_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDbdi_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityPathmonDbdi value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityPathmonDbdi value not illegal */
} /* tpClient1SeverityPathmonDbdi_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityPathmonDbdi_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDbdi_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityPathmonDbdi undo.
     */
    /*
     * copy tpClient1SeverityPathmonDbdi data
     * set rowreq_ctx->undo->tpClient1SeverityPathmonDbdi from rowreq_ctx->data.tpClient1SeverityPathmonDbdi
     */
    rowreq_ctx->undo->tpClient1SeverityPathmonDbdi = rowreq_ctx->data.tpClient1SeverityPathmonDbdi;


    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonDbdi_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityPathmonDbdi_val
 *        A long containing the new value.
 */
int
tpClient1SeverityPathmonDbdi_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityPathmonDbdi_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDbdi_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityPathmonDbdi value.
     * set tpClient1SeverityPathmonDbdi value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityPathmonDbdi = tpClient1SeverityPathmonDbdi_val;

    // Set severity on shared memory
    setTpClient1Severity("PathmonDbdi", tpClient1SeverityPathmonDbdi_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonDbdi_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityPathmonDbdi_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDbdi_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityPathmonDbdi undo.
     */
    /*
     * copy tpClient1SeverityPathmonDbdi data
     * set rowreq_ctx->data.tpClient1SeverityPathmonDbdi from rowreq_ctx->undo->tpClient1SeverityPathmonDbdi
     */
    rowreq_ctx->data.tpClient1SeverityPathmonDbdi = rowreq_ctx->undo->tpClient1SeverityPathmonDbdi;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonDbdi_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityPathmonDlck
 * tpClient1SeverityPathmonDlck is subid 12 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.12
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityPathmonDlck_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityPathmonDlck_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityPathmonDlck_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDlck_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityPathmonDlck value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityPathmonDlck value not illegal */
} /* tpClient1SeverityPathmonDlck_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityPathmonDlck_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDlck_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityPathmonDlck undo.
     */
    /*
     * copy tpClient1SeverityPathmonDlck data
     * set rowreq_ctx->undo->tpClient1SeverityPathmonDlck from rowreq_ctx->data.tpClient1SeverityPathmonDlck
     */
    rowreq_ctx->undo->tpClient1SeverityPathmonDlck = rowreq_ctx->data.tpClient1SeverityPathmonDlck;


    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonDlck_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityPathmonDlck_val
 *        A long containing the new value.
 */
int
tpClient1SeverityPathmonDlck_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityPathmonDlck_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDlck_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityPathmonDlck value.
     * set tpClient1SeverityPathmonDlck value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityPathmonDlck = tpClient1SeverityPathmonDlck_val;

    // Set severity on shared memory
    setTpClient1Severity("PathmonDlck", tpClient1SeverityPathmonDlck_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonDlck_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityPathmonDlck_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonDlck_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityPathmonDlck undo.
     */
    /*
     * copy tpClient1SeverityPathmonDlck data
     * set rowreq_ctx->data.tpClient1SeverityPathmonDlck from rowreq_ctx->undo->tpClient1SeverityPathmonDlck
     */
    rowreq_ctx->data.tpClient1SeverityPathmonDlck = rowreq_ctx->undo->tpClient1SeverityPathmonDlck;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonDlck_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeverityPathmonMsiunst
 * tpClient1SeverityPathmonMsiunst is subid 13 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.13
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeverityPathmonMsiunst_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeverityPathmonMsiunst_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityPathmonMsiunst_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonMsiunst_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeverityPathmonMsiunst value.
     */

    return MFD_SUCCESS; /* tpClient1SeverityPathmonMsiunst value not illegal */
} /* tpClient1SeverityPathmonMsiunst_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeverityPathmonMsiunst_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonMsiunst_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeverityPathmonMsiunst undo.
     */
    /*
     * copy tpClient1SeverityPathmonMsiunst data
     * set rowreq_ctx->undo->tpClient1SeverityPathmonMsiunst from rowreq_ctx->data.tpClient1SeverityPathmonMsiunst
     */
    rowreq_ctx->undo->tpClient1SeverityPathmonMsiunst = rowreq_ctx->data.tpClient1SeverityPathmonMsiunst;


    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonMsiunst_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeverityPathmonMsiunst_val
 *        A long containing the new value.
 */
int
tpClient1SeverityPathmonMsiunst_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeverityPathmonMsiunst_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonMsiunst_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeverityPathmonMsiunst value.
     * set tpClient1SeverityPathmonMsiunst value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeverityPathmonMsiunst = tpClient1SeverityPathmonMsiunst_val;

    // Set severity on shared memory
    setTpClient1Severity("PathmonMsiunst", tpClient1SeverityPathmonMsiunst_val);

    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonMsiunst_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeverityPathmonMsiunst_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeverityPathmonMsiunst_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeverityPathmonMsiunst undo.
     */
    /*
     * copy tpClient1SeverityPathmonMsiunst data
     * set rowreq_ctx->data.tpClient1SeverityPathmonMsiunst from rowreq_ctx->undo->tpClient1SeverityPathmonMsiunst
     */
    rowreq_ctx->data.tpClient1SeverityPathmonMsiunst = rowreq_ctx->undo->tpClient1SeverityPathmonMsiunst;

    
    return MFD_SUCCESS;
} /* tpClient1SeverityPathmonMsiunst_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeveritySecmonBipsfs
 * tpClient1SeveritySecmonBipsfs is subid 14 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.14
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeveritySecmonBipsfs_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeveritySecmonBipsfs_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeveritySecmonBipsfs_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonBipsfs_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeveritySecmonBipsfs value.
     */

    return MFD_SUCCESS; /* tpClient1SeveritySecmonBipsfs value not illegal */
} /* tpClient1SeveritySecmonBipsfs_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeveritySecmonBipsfs_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonBipsfs_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeveritySecmonBipsfs undo.
     */
    /*
     * copy tpClient1SeveritySecmonBipsfs data
     * set rowreq_ctx->undo->tpClient1SeveritySecmonBipsfs from rowreq_ctx->data.tpClient1SeveritySecmonBipsfs
     */
    rowreq_ctx->undo->tpClient1SeveritySecmonBipsfs = rowreq_ctx->data.tpClient1SeveritySecmonBipsfs;


    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonBipsfs_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeveritySecmonBipsfs_val
 *        A long containing the new value.
 */
int
tpClient1SeveritySecmonBipsfs_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeveritySecmonBipsfs_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonBipsfs_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeveritySecmonBipsfs value.
     * set tpClient1SeveritySecmonBipsfs value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeveritySecmonBipsfs = tpClient1SeveritySecmonBipsfs_val;

    // Set severity on shared memory
    setTpClient1Severity("SecmonBipsfs", tpClient1SeveritySecmonBipsfs_val);

    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonBipsfs_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeveritySecmonBipsfs_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonBipsfs_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeveritySecmonBipsfs undo.
     */
    /*
     * copy tpClient1SeveritySecmonBipsfs data
     * set rowreq_ctx->data.tpClient1SeveritySecmonBipsfs from rowreq_ctx->undo->tpClient1SeveritySecmonBipsfs
     */
    rowreq_ctx->data.tpClient1SeveritySecmonBipsfs = rowreq_ctx->undo->tpClient1SeveritySecmonBipsfs;

    
    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonBipsfs_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeveritySecmonDbdi
 * tpClient1SeveritySecmonDbdi is subid 15 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.15
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeveritySecmonDbdi_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeveritySecmonDbdi_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeveritySecmonDbdi_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonDbdi_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeveritySecmonDbdi value.
     */

    return MFD_SUCCESS; /* tpClient1SeveritySecmonDbdi value not illegal */
} /* tpClient1SeveritySecmonDbdi_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeveritySecmonDbdi_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonDbdi_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeveritySecmonDbdi undo.
     */
    /*
     * copy tpClient1SeveritySecmonDbdi data
     * set rowreq_ctx->undo->tpClient1SeveritySecmonDbdi from rowreq_ctx->data.tpClient1SeveritySecmonDbdi
     */
    rowreq_ctx->undo->tpClient1SeveritySecmonDbdi = rowreq_ctx->data.tpClient1SeveritySecmonDbdi;


    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonDbdi_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeveritySecmonDbdi_val
 *        A long containing the new value.
 */
int
tpClient1SeveritySecmonDbdi_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeveritySecmonDbdi_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonDbdi_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeveritySecmonDbdi value.
     * set tpClient1SeveritySecmonDbdi value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeveritySecmonDbdi = tpClient1SeveritySecmonDbdi_val;

    // Set severity on shared memory
    setTpClient1Severity("SecmonDbdi", tpClient1SeveritySecmonDbdi_val);

    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonDbdi_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeveritySecmonDbdi_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonDbdi_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeveritySecmonDbdi undo.
     */
    /*
     * copy tpClient1SeveritySecmonDbdi data
     * set rowreq_ctx->data.tpClient1SeveritySecmonDbdi from rowreq_ctx->undo->tpClient1SeveritySecmonDbdi
     */
    rowreq_ctx->data.tpClient1SeveritySecmonDbdi = rowreq_ctx->undo->tpClient1SeveritySecmonDbdi;

    
    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonDbdi_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeveritySecmonDiae
 * tpClient1SeveritySecmonDiae is subid 16 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.16
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeveritySecmonDiae_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeveritySecmonDiae_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeveritySecmonDiae_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonDiae_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeveritySecmonDiae value.
     */

    return MFD_SUCCESS; /* tpClient1SeveritySecmonDiae value not illegal */
} /* tpClient1SeveritySecmonDiae_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeveritySecmonDiae_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonDiae_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeveritySecmonDiae undo.
     */
    /*
     * copy tpClient1SeveritySecmonDiae data
     * set rowreq_ctx->undo->tpClient1SeveritySecmonDiae from rowreq_ctx->data.tpClient1SeveritySecmonDiae
     */
    rowreq_ctx->undo->tpClient1SeveritySecmonDiae = rowreq_ctx->data.tpClient1SeveritySecmonDiae;


    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonDiae_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeveritySecmonDiae_val
 *        A long containing the new value.
 */
int
tpClient1SeveritySecmonDiae_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeveritySecmonDiae_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonDiae_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeveritySecmonDiae value.
     * set tpClient1SeveritySecmonDiae value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeveritySecmonDiae = tpClient1SeveritySecmonDiae_val;

    // Set severity on shared memory
    setTpClient1Severity("SecmonDiae", tpClient1SeveritySecmonDiae_val);

    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonDiae_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeveritySecmonDiae_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonDiae_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeveritySecmonDiae undo.
     */
    /*
     * copy tpClient1SeveritySecmonDiae data
     * set rowreq_ctx->data.tpClient1SeveritySecmonDiae from rowreq_ctx->undo->tpClient1SeveritySecmonDiae
     */
    rowreq_ctx->data.tpClient1SeveritySecmonDiae = rowreq_ctx->undo->tpClient1SeveritySecmonDiae;

    
    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonDiae_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeveritySecmonFlom
 * tpClient1SeveritySecmonFlom is subid 17 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.17
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeveritySecmonFlom_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeveritySecmonFlom_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeveritySecmonFlom_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonFlom_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeveritySecmonFlom value.
     */

    return MFD_SUCCESS; /* tpClient1SeveritySecmonFlom value not illegal */
} /* tpClient1SeveritySecmonFlom_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeveritySecmonFlom_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonFlom_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeveritySecmonFlom undo.
     */
    /*
     * copy tpClient1SeveritySecmonFlom data
     * set rowreq_ctx->undo->tpClient1SeveritySecmonFlom from rowreq_ctx->data.tpClient1SeveritySecmonFlom
     */
    rowreq_ctx->undo->tpClient1SeveritySecmonFlom = rowreq_ctx->data.tpClient1SeveritySecmonFlom;


    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonFlom_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeveritySecmonFlom_val
 *        A long containing the new value.
 */
int
tpClient1SeveritySecmonFlom_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeveritySecmonFlom_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonFlom_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeveritySecmonFlom value.
     * set tpClient1SeveritySecmonFlom value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeveritySecmonFlom = tpClient1SeveritySecmonFlom_val;

    // Set severity on shared memory
    setTpClient1Severity("SecmonFlom", tpClient1SeveritySecmonFlom_val);

    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonFlom_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeveritySecmonFlom_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonFlom_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeveritySecmonFlom undo.
     */
    /*
     * copy tpClient1SeveritySecmonFlom data
     * set rowreq_ctx->data.tpClient1SeveritySecmonFlom from rowreq_ctx->undo->tpClient1SeveritySecmonFlom
     */
    rowreq_ctx->data.tpClient1SeveritySecmonFlom = rowreq_ctx->undo->tpClient1SeveritySecmonFlom;

    
    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonFlom_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient1SeverityEntry.tpClient1SeveritySecmonLom
 * tpClient1SeveritySecmonLom is subid 18 of tpClient1SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.2.1.18
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient1SeveritySecmonLom_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient1SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient1SeveritySecmonLom_check_value( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeveritySecmonLom_val)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonLom_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient1SeveritySecmonLom value.
     */

    return MFD_SUCCESS; /* tpClient1SeveritySecmonLom value not illegal */
} /* tpClient1SeveritySecmonLom_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient1SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient1SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient1SeveritySecmonLom_undo_setup( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonLom_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient1SeveritySecmonLom undo.
     */
    /*
     * copy tpClient1SeveritySecmonLom data
     * set rowreq_ctx->undo->tpClient1SeveritySecmonLom from rowreq_ctx->data.tpClient1SeveritySecmonLom
     */
    rowreq_ctx->undo->tpClient1SeveritySecmonLom = rowreq_ctx->data.tpClient1SeveritySecmonLom;


    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonLom_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient1SeveritySecmonLom_val
 *        A long containing the new value.
 */
int
tpClient1SeveritySecmonLom_set( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient1SeveritySecmonLom_val )
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonLom_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient1SeveritySecmonLom value.
     * set tpClient1SeveritySecmonLom value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient1SeveritySecmonLom = tpClient1SeveritySecmonLom_val;

    // Set severity on shared memory
    setTpClient1Severity("SecmonLom", tpClient1SeveritySecmonLom_val);

    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonLom_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient1SeveritySecmonLom_undo( tpClient1SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient1SeverityTable:tpClient1SeveritySecmonLom_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient1SeveritySecmonLom undo.
     */
    /*
     * copy tpClient1SeveritySecmonLom data
     * set rowreq_ctx->data.tpClient1SeveritySecmonLom from rowreq_ctx->undo->tpClient1SeveritySecmonLom
     */
    rowreq_ctx->data.tpClient1SeveritySecmonLom = rowreq_ctx->undo->tpClient1SeveritySecmonLom;

    
    return MFD_SUCCESS;
} /* tpClient1SeveritySecmonLom_undo */

void setTpClient1Severity(char *alarm, long severity_value)
{
	DEBUGMSGTL(("verbose:setTpClient1Severity:setTpClient1Severity","called\n"));

	shm_handler_create(pt_monitor_struct);

	if (strcmp(alarm, "FramerFsmms") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].FramerFsmms = severity_value;
	}
	else if (strcmp(alarm, "FramerFssusts") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].FramerFssusts = severity_value;
	}
	else if (strcmp(alarm, "FramerFst") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].FramerFst = severity_value;
	}
	else if (strcmp(alarm, "FramerSif") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].FramerSif = severity_value;
	}
	else if (strcmp(alarm, "FramerSlof") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].FramerSlof = severity_value;
	}
	else if (strcmp(alarm, "FramerSoof") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].FramerSoof = severity_value;
	}
	else if (strcmp(alarm, "LosstatLosf") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].LosstatLosf = severity_value;
	}
	else if (strcmp(alarm, "PathmonAccstat") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].PathmonAccstat = severity_value;
	}
	else if (strcmp(alarm, "PathmonDais") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].PathmonDais = severity_value;
	}
	else if (strcmp(alarm, "PathmonDbdi") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].PathmonDbdi = severity_value;
	}
	else if (strcmp(alarm, "PathmonDlck") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].PathmonDlck = severity_value;
	}
	else if (strcmp(alarm, "PathmonMsiunst") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].PathmonMsiunst = severity_value;
	}
	else if (strcmp(alarm, "SecmonBipsfs") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].SecmonBipsfs = severity_value;
	}
	else if (strcmp(alarm, "SecmonDbdi") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].SecmonDbdi = severity_value;
	}
	else if (strcmp(alarm, "SecmonDiae") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].SecmonDiae = severity_value;
	}
	else if (strcmp(alarm, "SecmonFlom") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].SecmonFlom = severity_value;
	}
	else if (strcmp(alarm, "SecmonLom") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[0].SecmonLom = severity_value;
	}

	// Set dirty flag
	pt_monitor_struct->sev_dirty_flag = 1;
}

/** @} */
