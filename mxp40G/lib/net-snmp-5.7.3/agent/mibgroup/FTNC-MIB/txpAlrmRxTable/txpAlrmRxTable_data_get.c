/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "../../txpAlrmRxTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement txpAlrmRxTable get routines.
 * TODO:240:M: Implement txpAlrmRxTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table txpAlrmRxTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::txpAlrmRxTable is subid 2 of txpAlrm.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.2.9.1.2, length: 11
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement txpAlrmRxTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param txpAlrmRxIndex_ptr
 * @param txpAlrmRxIndex_ptr_len
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
txpAlrmRxTable_indexes_set_tbl_idx(txpAlrmRxTable_mib_index *tbl_idx, char *txpAlrmRxIndex_val_ptr,  size_t txpAlrmRxIndex_val_ptr_len)
{
    DEBUGMSGTL(("verbose:txpAlrmRxTable:txpAlrmRxTable_indexes_set_tbl_idx","called\n"));

    /* txpAlrmRxIndex(1)/OCTETSTR/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h */
    tbl_idx->txpAlrmRxIndex_len = sizeof(tbl_idx->txpAlrmRxIndex)/sizeof(tbl_idx->txpAlrmRxIndex[0]); /* max length */
    /*
     * make sure there is enough space for txpAlrmRxIndex data
     */
    if ((NULL == tbl_idx->txpAlrmRxIndex) ||
        (tbl_idx->txpAlrmRxIndex_len <
         (txpAlrmRxIndex_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value (txpAlrmRxIndex_val_ptr)\n");
        return MFD_ERROR;
    }
    tbl_idx->txpAlrmRxIndex_len = txpAlrmRxIndex_val_ptr_len;
    memcpy( tbl_idx->txpAlrmRxIndex, txpAlrmRxIndex_val_ptr, txpAlrmRxIndex_val_ptr_len* sizeof(txpAlrmRxIndex_val_ptr[0]) );
    

    return MFD_SUCCESS;
} /* txpAlrmRxTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
txpAlrmRxTable_indexes_set(txpAlrmRxTable_rowreq_ctx *rowreq_ctx, char *txpAlrmRxIndex_val_ptr,  size_t txpAlrmRxIndex_val_ptr_len)
{
    DEBUGMSGTL(("verbose:txpAlrmRxTable:txpAlrmRxTable_indexes_set","called\n"));

    if(MFD_SUCCESS != txpAlrmRxTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , txpAlrmRxIndex_val_ptr, txpAlrmRxIndex_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != txpAlrmRxTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* txpAlrmRxTable_indexes_set */


/*---------------------------------------------------------------------
 * FTNC-MIB::txpAlrmRxEntry.txpRxALMINT
 * txpRxALMINT is subid 2 of txpAlrmRxEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.1.2.1.2
 * Description:
Line receiver Rx summary alarm.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpRxALMINT data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpRxALMINT_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpRxALMINT_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpRxALMINT.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpRxALMINT_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpRxALMINT_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpRxALMINT_get( txpAlrmRxTable_rowreq_ctx *rowreq_ctx, char **txpRxALMINT_val_ptr_ptr, size_t *txpRxALMINT_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpRxALMINT_val_ptr_ptr) && (NULL != *txpRxALMINT_val_ptr_ptr));
   netsnmp_assert( NULL != txpRxALMINT_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpAlrmRxTable:txpRxALMINT_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpRxALMINT data.
 * copy (* txpRxALMINT_val_ptr_ptr ) data and (* txpRxALMINT_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpRxALMINT data
     */
    if ((NULL == (* txpRxALMINT_val_ptr_ptr )) ||
        ((* txpRxALMINT_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpRxALMINT_len* sizeof(rowreq_ctx->data.txpRxALMINT[0])))) {
        /*
         * allocate space for txpRxALMINT data
         */
        (* txpRxALMINT_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpRxALMINT_len* sizeof(rowreq_ctx->data.txpRxALMINT[0]));
        if(NULL == (* txpRxALMINT_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpRxALMINT)\n");
            return MFD_ERROR;
        }
    }
    (* txpRxALMINT_val_ptr_len_ptr ) = rowreq_ctx->data.txpRxALMINT_len* sizeof(rowreq_ctx->data.txpRxALMINT[0]);
    memcpy( (* txpRxALMINT_val_ptr_ptr ), rowreq_ctx->data.txpRxALMINT, rowreq_ctx->data.txpRxALMINT_len* sizeof(rowreq_ctx->data.txpRxALMINT[0]) );

    return MFD_SUCCESS;
} /* txpRxALMINT_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::txpAlrmRxEntry.txpRxPOWALM
 * txpRxPOWALM is subid 3 of txpAlrmRxEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.1.2.1.3
 * Description:
Line receiver loss average optical power alarm.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpRxPOWALM data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpRxPOWALM_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpRxPOWALM_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpRxPOWALM.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpRxPOWALM_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpRxPOWALM_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpRxPOWALM_get( txpAlrmRxTable_rowreq_ctx *rowreq_ctx, char **txpRxPOWALM_val_ptr_ptr, size_t *txpRxPOWALM_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpRxPOWALM_val_ptr_ptr) && (NULL != *txpRxPOWALM_val_ptr_ptr));
   netsnmp_assert( NULL != txpRxPOWALM_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpAlrmRxTable:txpRxPOWALM_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpRxPOWALM data.
 * copy (* txpRxPOWALM_val_ptr_ptr ) data and (* txpRxPOWALM_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpRxPOWALM data
     */
    if ((NULL == (* txpRxPOWALM_val_ptr_ptr )) ||
        ((* txpRxPOWALM_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpRxPOWALM_len* sizeof(rowreq_ctx->data.txpRxPOWALM[0])))) {
        /*
         * allocate space for txpRxPOWALM data
         */
        (* txpRxPOWALM_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpRxPOWALM_len* sizeof(rowreq_ctx->data.txpRxPOWALM[0]));
        if(NULL == (* txpRxPOWALM_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpRxPOWALM)\n");
            return MFD_ERROR;
        }
    }
    (* txpRxPOWALM_val_ptr_len_ptr ) = rowreq_ctx->data.txpRxPOWALM_len* sizeof(rowreq_ctx->data.txpRxPOWALM[0]);
    memcpy( (* txpRxPOWALM_val_ptr_ptr ), rowreq_ctx->data.txpRxPOWALM, rowreq_ctx->data.txpRxPOWALM_len* sizeof(rowreq_ctx->data.txpRxPOWALM[0]) );

    return MFD_SUCCESS;
} /* txpRxPOWALM_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::txpAlrmRxEntry.txpRxLOS
 * txpRxLOS is subid 4 of txpAlrmRxEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.1.2.1.4
 * Description:
Line receiver loss AC (modulated) power alarm.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpRxLOS data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpRxLOS_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpRxLOS_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpRxLOS.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpRxLOS_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpRxLOS_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpRxLOS_get( txpAlrmRxTable_rowreq_ctx *rowreq_ctx, char **txpRxLOS_val_ptr_ptr, size_t *txpRxLOS_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpRxLOS_val_ptr_ptr) && (NULL != *txpRxLOS_val_ptr_ptr));
   netsnmp_assert( NULL != txpRxLOS_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpAlrmRxTable:txpRxLOS_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpRxLOS data.
 * copy (* txpRxLOS_val_ptr_ptr ) data and (* txpRxLOS_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpRxLOS data
     */
    if ((NULL == (* txpRxLOS_val_ptr_ptr )) ||
        ((* txpRxLOS_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpRxLOS_len* sizeof(rowreq_ctx->data.txpRxLOS[0])))) {
        /*
         * allocate space for txpRxLOS data
         */
        (* txpRxLOS_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpRxLOS_len* sizeof(rowreq_ctx->data.txpRxLOS[0]));
        if(NULL == (* txpRxLOS_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpRxLOS)\n");
            return MFD_ERROR;
        }
    }
    (* txpRxLOS_val_ptr_len_ptr ) = rowreq_ctx->data.txpRxLOS_len* sizeof(rowreq_ctx->data.txpRxLOS[0]);
    memcpy( (* txpRxLOS_val_ptr_ptr ), rowreq_ctx->data.txpRxLOS, rowreq_ctx->data.txpRxLOS_len* sizeof(rowreq_ctx->data.txpRxLOS[0]) );

    return MFD_SUCCESS;
} /* txpRxLOS_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::txpAlrmRxEntry.txpRxLOCKERR
 * txpRxLOCKERR is subid 5 of txpAlrmRxEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.1.2.1.5
 * Description:
Line receiver loss of RxPLL lock indicator.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpRxLOCKERR data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpRxLOCKERR_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpRxLOCKERR_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpRxLOCKERR.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpRxLOCKERR_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpRxLOCKERR_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpRxLOCKERR_get( txpAlrmRxTable_rowreq_ctx *rowreq_ctx, char **txpRxLOCKERR_val_ptr_ptr, size_t *txpRxLOCKERR_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpRxLOCKERR_val_ptr_ptr) && (NULL != *txpRxLOCKERR_val_ptr_ptr));
   netsnmp_assert( NULL != txpRxLOCKERR_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpAlrmRxTable:txpRxLOCKERR_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpRxLOCKERR data.
 * copy (* txpRxLOCKERR_val_ptr_ptr ) data and (* txpRxLOCKERR_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpRxLOCKERR data
     */
    if ((NULL == (* txpRxLOCKERR_val_ptr_ptr )) ||
        ((* txpRxLOCKERR_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpRxLOCKERR_len* sizeof(rowreq_ctx->data.txpRxLOCKERR[0])))) {
        /*
         * allocate space for txpRxLOCKERR data
         */
        (* txpRxLOCKERR_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpRxLOCKERR_len* sizeof(rowreq_ctx->data.txpRxLOCKERR[0]));
        if(NULL == (* txpRxLOCKERR_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpRxLOCKERR)\n");
            return MFD_ERROR;
        }
    }
    (* txpRxLOCKERR_val_ptr_len_ptr ) = rowreq_ctx->data.txpRxLOCKERR_len* sizeof(rowreq_ctx->data.txpRxLOCKERR[0]);
    memcpy( (* txpRxLOCKERR_val_ptr_ptr ), rowreq_ctx->data.txpRxLOCKERR, rowreq_ctx->data.txpRxLOCKERR_len* sizeof(rowreq_ctx->data.txpRxLOCKERR[0]) );

    return MFD_SUCCESS;
} /* txpRxLOCKERR_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::txpAlrmRxEntry.txpRXS
 * txpRXS is subid 6 of txpAlrmRxEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.1.2.1.6
 * Description:
Line receiver SFI-5 DEMUX status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpRXS data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpRXS_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpRXS_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpRXS.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpRXS_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpRXS_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpRXS_get( txpAlrmRxTable_rowreq_ctx *rowreq_ctx, char **txpRXS_val_ptr_ptr, size_t *txpRXS_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpRXS_val_ptr_ptr) && (NULL != *txpRXS_val_ptr_ptr));
   netsnmp_assert( NULL != txpRXS_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpAlrmRxTable:txpRXS_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpRXS data.
 * copy (* txpRXS_val_ptr_ptr ) data and (* txpRXS_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpRXS data
     */
    if ((NULL == (* txpRXS_val_ptr_ptr )) ||
        ((* txpRXS_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpRXS_len* sizeof(rowreq_ctx->data.txpRXS[0])))) {
        /*
         * allocate space for txpRXS data
         */
        (* txpRXS_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpRXS_len* sizeof(rowreq_ctx->data.txpRXS[0]));
        if(NULL == (* txpRXS_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpRXS)\n");
            return MFD_ERROR;
        }
    }
    (* txpRXS_val_ptr_len_ptr ) = rowreq_ctx->data.txpRXS_len* sizeof(rowreq_ctx->data.txpRXS[0]);
    memcpy( (* txpRXS_val_ptr_ptr ), rowreq_ctx->data.txpRXS, rowreq_ctx->data.txpRXS_len* sizeof(rowreq_ctx->data.txpRXS[0]) );

    return MFD_SUCCESS;
} /* txpRXS_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::txpAlrmRxEntry.txpPRBSERRDET
 * txpPRBSERRDET is subid 7 of txpAlrmRxEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.1.2.1.7
 * Description:
Line receiver PRBSERRDET (an error was detected by the PRBS error checker).
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpPRBSERRDET data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpPRBSERRDET_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpPRBSERRDET_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpPRBSERRDET.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpPRBSERRDET_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpPRBSERRDET_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpPRBSERRDET_get( txpAlrmRxTable_rowreq_ctx *rowreq_ctx, char **txpPRBSERRDET_val_ptr_ptr, size_t *txpPRBSERRDET_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpPRBSERRDET_val_ptr_ptr) && (NULL != *txpPRBSERRDET_val_ptr_ptr));
   netsnmp_assert( NULL != txpPRBSERRDET_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpAlrmRxTable:txpPRBSERRDET_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpPRBSERRDET data.
 * copy (* txpPRBSERRDET_val_ptr_ptr ) data and (* txpPRBSERRDET_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpPRBSERRDET data
     */
    if ((NULL == (* txpPRBSERRDET_val_ptr_ptr )) ||
        ((* txpPRBSERRDET_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpPRBSERRDET_len* sizeof(rowreq_ctx->data.txpPRBSERRDET[0])))) {
        /*
         * allocate space for txpPRBSERRDET data
         */
        (* txpPRBSERRDET_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpPRBSERRDET_len* sizeof(rowreq_ctx->data.txpPRBSERRDET[0]));
        if(NULL == (* txpPRBSERRDET_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpPRBSERRDET)\n");
            return MFD_ERROR;
        }
    }
    (* txpPRBSERRDET_val_ptr_len_ptr ) = rowreq_ctx->data.txpPRBSERRDET_len* sizeof(rowreq_ctx->data.txpPRBSERRDET[0]);
    memcpy( (* txpPRBSERRDET_val_ptr_ptr ), rowreq_ctx->data.txpPRBSERRDET, rowreq_ctx->data.txpPRBSERRDET_len* sizeof(rowreq_ctx->data.txpPRBSERRDET[0]) );

    return MFD_SUCCESS;
} /* txpPRBSERRDET_get */



/** @} */
