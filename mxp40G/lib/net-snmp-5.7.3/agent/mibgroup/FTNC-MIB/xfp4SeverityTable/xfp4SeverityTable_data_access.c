/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "../../xfp4SeverityTable.h"

#include "xfp4SeverityTable_data_access.h"

#include <stdio.h>
#include <sys/mman.h>
#include <sys/types.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <stdlib.h>

#include "../../../../lib/hl_configs/definitions.h"

void shm_handler_create(Monitor *pt_monitor_struct);
Monitor *pt_monitor_struct;


/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table xfp4SeverityTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::xfp4SeverityTable is subid 4 of xfpAlarms.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.1.1.3.5.4, length: 12
 */

/**
 * initialization for xfp4SeverityTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param xfp4SeverityTable_reg
 *        Pointer to xfp4SeverityTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int xfp4SeverityTable_init_data(xfp4SeverityTable_registration * xfp4SeverityTable_reg)
{
	DEBUGMSGTL(("verbose:xfp4SeverityTable:xfp4SeverityTable_init_data","called\n"));

	/*
	 * TODO:303:o: Initialize xfp4SeverityTable data.
	 */
	/*
	 ***************************************************
	 ***             START EXAMPLE CODE              ***
	 ***---------------------------------------------***/
	/*
	 * if you are the sole writer for the file, you could
	 * open it here. However, as stated earlier, we are assuming
	 * the worst case, which in this case means that the file is
	 * written to by someone else, and might not even exist when
	 * we start up. So we can't do anything here.
	 */
	/*
	 ***---------------------------------------------***
	 ***              END  EXAMPLE CODE              ***
	 ***************************************************/

	return MFD_SUCCESS;
} /* xfp4SeverityTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void xfp4SeverityTable_container_init(netsnmp_container **container_ptr_ptr, netsnmp_cache *cache)
{
	DEBUGMSGTL(("verbose:xfp4SeverityTable:xfp4SeverityTable_container_init","called\n"));

	if (NULL == container_ptr_ptr)
	{
		snmp_log(LOG_ERR, "bad container param to xfp4SeverityTable_container_init\n");
		return;
	}

	/*
	 * For advanced users, you can use a custom container. If you
	 * do not create one, one will be created for you.
	 */
	*container_ptr_ptr = NULL;

	if (NULL == cache)
	{
		snmp_log(LOG_ERR, "bad cache param to xfp4SeverityTable_container_init\n");
		return;
	}

	/*
	 * TODO:345:A: Set up xfp4SeverityTable cache properties.
	 *
	 * Also for advanced users, you can set parameters for the
	 * cache. Do not change the magic pointer, as it is used
	 * by the MFD helper. To completely disable caching, set
	 * cache->enabled to 0.
	 */
	cache->timeout = XFP4SEVERITYTABLE_CACHE_TIMEOUT; /* seconds */
} /* xfp4SeverityTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before xfp4SeverityTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void xfp4SeverityTable_container_shutdown(netsnmp_container *container_ptr)
{
	DEBUGMSGTL(("verbose:xfp4SeverityTable:xfp4SeverityTable_container_shutdown","called\n"));

	if (NULL == container_ptr)
	{
		snmp_log(LOG_ERR, "bad params to xfp4SeverityTable_container_shutdown\n");
		return;
	}

} /* xfp4SeverityTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement xfp4SeverityTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  xfp4SeverityTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int xfp4SeverityTable_container_load(netsnmp_container *container)
{
	xfp4SeverityTable_rowreq_ctx *rowreq_ctx;
	size_t count = 0;

	/*
	 * temporary storage for index values
	 */
	/*
	 * xfp4SeverityIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/a/w/e/R/d/h
	 */
	long xfp4SeverityIndex = 1;

	/*
	 * this example code is based on a data source that is a
	 * text file to be read and parsed.
	 */

	DEBUGMSGTL(("verbose:xfp4SeverityTable:xfp4SeverityTable_container_load","called\n"));

	/*
	 * TODO:352:M: |   |-> set indexes in new xfp4SeverityTable rowreq context.
	 * data context will be set from the param (unless NULL,
	 *      in which case a new data context will be allocated)
	 */
	rowreq_ctx = xfp4SeverityTable_allocate_rowreq_ctx(NULL);
	if (NULL == rowreq_ctx)
	{
		snmp_log(LOG_ERR, "memory allocation failed\n");
		return MFD_RESOURCE_UNAVAILABLE;
	}
	if (MFD_SUCCESS != xfp4SeverityTable_indexes_set(rowreq_ctx, xfp4SeverityIndex))
	{
		snmp_log(LOG_ERR, "error setting index while loading "
				"xfp4SeverityTable data.\n");
		xfp4SeverityTable_release_rowreq_ctx(rowreq_ctx);
	}

	/*
	 * TODO:352:r: |   |-> populate xfp4SeverityTable data context.
	 * Populate data context here. (optionally, delay until row prep)
	 */
	/*
	 * TRANSIENT or semi-TRANSIENT data:
	 * copy data or save any info needed to do it in row_prep.
	 */

	shm_handler_create(pt_monitor_struct);

	/*
	 * setup/save data for xfp4SeverityPresence
	 * xfp4SeverityPresence(2)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityPresence = pt_monitor_struct->xfp_struct.severity[3].presence;

	/*
	 * setup/save data for xfp4SeverityRxLoss
	 * xfp4SeverityRxLoss(3)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityRxLoss = pt_monitor_struct->xfp_struct.severity[3].rx_loss;

	/*
	 * setup/save data for xfp4SeverityReady
	 * xfp4SeverityReady(4)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityReady = pt_monitor_struct->xfp_struct.severity[3].ready;

	/*
	 * setup/save data for xfp4SeverityTxPower
	 * xfp4SeverityTxPower(5)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityTxPower = pt_monitor_struct->xfp_struct.severity[3].tx_power;

	/*
	 * setup/save data for xfp4SeverityRxPower
	 * xfp4SeverityRxPower(6)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityRxPower = pt_monitor_struct->xfp_struct.severity[3].rx_power;

	/*
	 * setup/save data for xfp4SeverityTemperature
	 * xfp4SeverityTemperature(7)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityTemperature = pt_monitor_struct->xfp_struct.severity[3].temperature;

	/*
	 * setup/save data for xfp4SeverityLowTxPower
	 * xfp4SeverityLowTxPower(8)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityLowTxPower = pt_monitor_struct->xfp_struct.severity[3].low_tx_power;

	/*
	 * setup/save data for xfp4SeverityHighTxPower
	 * xfp4SeverityHighTxPower(9)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityHighTxPower = pt_monitor_struct->xfp_struct.severity[3].high_tx_power;

	/*
	 * setup/save data for xfp4SeverityLowRxPower
	 * xfp4SeverityLowRxPower(10)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityLowRxPower = pt_monitor_struct->xfp_struct.severity[3].low_rx_power;

	/*
	 * setup/save data for xfp4SeverityHighRxPower
	 * xfp4SeverityHighRxPower(11)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityHighRxPower = pt_monitor_struct->xfp_struct.severity[3].high_rx_power;

	/*
	 * setup/save data for xfp4SeverityRxCdrLossOfLock
	 * xfp4SeverityRxCdrLossOfLock(12)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityRxCdrLossOfLock = pt_monitor_struct->xfp_struct.severity[3].rx_cdr_loss_of_lock;

	/*
	 * setup/save data for xfp4SeverityTxCdrLossOfLock
	 * xfp4SeverityTxCdrLossOfLock(13)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityTxCdrLossOfLock = pt_monitor_struct->xfp_struct.severity[3].tx_cdr_loss_of_lock;

	/*
	 * setup/save data for xfp4SeverityLaserFault
	 * xfp4SeverityLaserFault(14)/INTEGER32/ASN_INTEGER/long(long)//l/A/W/e/r/d/h
	 */
	/** no mapping */
	rowreq_ctx->data.xfp4SeverityLaserFault = pt_monitor_struct->xfp_struct.severity[3].laser_fault;

	/*
	 * insert into table container
	 */
	CONTAINER_INSERT(container, rowreq_ctx);

	DEBUGMSGT(("verbose:xfp4SeverityTable:xfp4SeverityTable_container_load", "inserted %d records\n", count));

	return MFD_SUCCESS;
} /* xfp4SeverityTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void xfp4SeverityTable_container_free(netsnmp_container *container)
{
	DEBUGMSGTL(("verbose:xfp4SeverityTable:xfp4SeverityTable_container_free","called\n"));

	/*
	 * TODO:380:M: Free xfp4SeverityTable container data.
	 */
} /* xfp4SeverityTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int xfp4SeverityTable_row_prep(xfp4SeverityTable_rowreq_ctx *rowreq_ctx)
{
	DEBUGMSGTL(("verbose:xfp4SeverityTable:xfp4SeverityTable_row_prep","called\n"));

	netsnmp_assert(NULL != rowreq_ctx);

	/*
	 * TODO:390:o: Prepare row for request.
	 * If populating row data was delayed, this is the place to
	 * fill in the row for this request.
	 */

	return MFD_SUCCESS;
} /* xfp4SeverityTable_row_prep */

/** @} */
