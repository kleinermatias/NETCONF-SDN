/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/** \page MFD helper for fanTable
 *
 * \section intro Introduction
 * Introductory text.
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "fanTable.h"

#include <net-snmp/agent/mib_modules.h>

#include "FTNC-MIB/fanTable/fanTable_interface.h"

const oid fanTable_oid[] = { FANTABLE_OID };
const int fanTable_oid_size = OID_LENGTH(fanTable_oid);

    fanTable_registration  fanTable_user_context;

void initialize_table_fanTable(void);
void shutdown_table_fanTable(void);


/**
 * Initializes the fanTable module
 */
void
init_fanTable(void)
{
    DEBUGMSGTL(("verbose:fanTable:init_fanTable","called\n"));

    /*
     * TODO:300:o: Perform fanTable one-time module initialization.
     */
     
    /*
     * here we initialize all the tables we're planning on supporting
     */
    if (should_init("fanTable"))
        initialize_table_fanTable();

} /* init_fanTable */

/**
 * Shut-down the fanTable module (agent is exiting)
 */
void
shutdown_fanTable(void)
{
    if (should_init("fanTable"))
        shutdown_table_fanTable();

}

/**
 * Initialize the table fanTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_fanTable(void)
{
    fanTable_registration * user_context;
    u_long flags;

    DEBUGMSGTL(("verbose:fanTable:initialize_table_fanTable","called\n"));

    /*
     * TODO:301:o: Perform fanTable one-time table initialization.
     */

    /*
     * TODO:302:o: |->Initialize fanTable user context
     * if you'd like to pass in a pointer to some data for this
     * table, allocate or set it up here.
     */
    /*
     * a netsnmp_data_list is a simple way to store void pointers. A simple
     * string token is used to add, find or remove pointers.
     */
    user_context = netsnmp_create_data_list("fanTable", NULL, NULL);
    
    /*
     * No support for any flags yet, but in the future you would
     * set any flags here.
     */
    flags = 0;
    
    /*
     * call interface initialization code
     */
    _fanTable_initialize_interface(user_context, flags);
} /* initialize_table_fanTable */

/**
 * Shutdown the table fanTable 
 */
void
shutdown_table_fanTable(void)
{
    /*
     * call interface shutdown code
     */
    _fanTable_shutdown_interface(&fanTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
fanTable_rowreq_ctx_init(fanTable_rowreq_ctx *rowreq_ctx,
                           void *user_init_ctx)
{
    DEBUGMSGTL(("verbose:fanTable:fanTable_rowreq_ctx_init","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:210:o: |-> Perform extra fanTable rowreq initialization. (eg DEFVALS)
     */

    return MFD_SUCCESS;
} /* fanTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void fanTable_rowreq_ctx_cleanup(fanTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:fanTable:fanTable_rowreq_ctx_cleanup","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:211:o: |-> Perform extra fanTable rowreq cleanup.
     */
} /* fanTable_rowreq_ctx_cleanup */

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
fanTable_pre_request(fanTable_registration * user_context)
{
    DEBUGMSGTL(("verbose:fanTable:fanTable_pre_request","called\n"));

    /*
     * TODO:510:o: Perform fanTable pre-request actions.
     */

    return MFD_SUCCESS;
} /* fanTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param user_context
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
fanTable_post_request(fanTable_registration * user_context, int rc)
{
    DEBUGMSGTL(("verbose:fanTable:fanTable_post_request","called\n"));

    /*
     * TODO:511:o: Perform fanTable post-request actions.
     */

    return MFD_SUCCESS;
} /* fanTable_post_request */


/** @{ */
