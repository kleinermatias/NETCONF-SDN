#ifndef _LOGGER_H_
#define _LOGGER_H_

#include "mond.h"

#include "logmsg.h"

int init_tmp(tmp_t *tmp);

int log_tmp(tmp_t *tmp);

int init_hmd(void);

int log_hmd(hmd_t *hmd);

int init_T41(void);

int log_otn(void);

int log_eth(void);

int init_xfp(xfp_t *xfp);

int log_xfp(void);

int init_qsfp(qsfp_t *qsfp);

int log_qsfp(qsfp_t *qsfp);

int init_txp(txp_t *txp);

int log_txp(void);

#endif /* _LOGGER_H_ */
