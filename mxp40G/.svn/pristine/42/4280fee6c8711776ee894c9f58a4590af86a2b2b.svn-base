/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/** \page MFD helper for txpBandTable
 *
 * \section intro Introduction
 * Introductory text.
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "txpBandTable.h"

#include <net-snmp/agent/mib_modules.h>

#include "FTNC-MIB/txpBandTable/txpBandTable_interface.h"

const oid txpBandTable_oid[] = { TXPBANDTABLE_OID };
const int txpBandTable_oid_size = OID_LENGTH(txpBandTable_oid);

    txpBandTable_registration  txpBandTable_user_context;

void initialize_table_txpBandTable(void);
void shutdown_table_txpBandTable(void);


/**
 * Initializes the txpBandTable module
 */
void
init_txpBandTable(void)
{
    DEBUGMSGTL(("verbose:txpBandTable:init_txpBandTable","called\n"));

    /*
     * TODO:300:o: Perform txpBandTable one-time module initialization.
     */
     
    /*
     * here we initialize all the tables we're planning on supporting
     */
    if (should_init("txpBandTable"))
        initialize_table_txpBandTable();

} /* init_txpBandTable */

/**
 * Shut-down the txpBandTable module (agent is exiting)
 */
void
shutdown_txpBandTable(void)
{
    if (should_init("txpBandTable"))
        shutdown_table_txpBandTable();

}

/**
 * Initialize the table txpBandTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_txpBandTable(void)
{
    txpBandTable_registration * user_context;
    u_long flags;

    DEBUGMSGTL(("verbose:txpBandTable:initialize_table_txpBandTable","called\n"));

    /*
     * TODO:301:o: Perform txpBandTable one-time table initialization.
     */

    /*
     * TODO:302:o: |->Initialize txpBandTable user context
     * if you'd like to pass in a pointer to some data for this
     * table, allocate or set it up here.
     */
    /*
     * a netsnmp_data_list is a simple way to store void pointers. A simple
     * string token is used to add, find or remove pointers.
     */
    user_context = netsnmp_create_data_list("txpBandTable", NULL, NULL);
    
    /*
     * No support for any flags yet, but in the future you would
     * set any flags here.
     */
    flags = 0;
    
    /*
     * call interface initialization code
     */
    _txpBandTable_initialize_interface(user_context, flags);
} /* initialize_table_txpBandTable */

/**
 * Shutdown the table txpBandTable 
 */
void
shutdown_table_txpBandTable(void)
{
    /*
     * call interface shutdown code
     */
    _txpBandTable_shutdown_interface(&txpBandTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
txpBandTable_rowreq_ctx_init(txpBandTable_rowreq_ctx *rowreq_ctx,
                           void *user_init_ctx)
{
    DEBUGMSGTL(("verbose:txpBandTable:txpBandTable_rowreq_ctx_init","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:210:o: |-> Perform extra txpBandTable rowreq initialization. (eg DEFVALS)
     */

    return MFD_SUCCESS;
} /* txpBandTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void txpBandTable_rowreq_ctx_cleanup(txpBandTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:txpBandTable:txpBandTable_rowreq_ctx_cleanup","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:211:o: |-> Perform extra txpBandTable rowreq cleanup.
     */
} /* txpBandTable_rowreq_ctx_cleanup */

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
txpBandTable_pre_request(txpBandTable_registration * user_context)
{
    DEBUGMSGTL(("verbose:txpBandTable:txpBandTable_pre_request","called\n"));

    /*
     * TODO:510:o: Perform txpBandTable pre-request actions.
     */

    return MFD_SUCCESS;
} /* txpBandTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param user_context
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
txpBandTable_post_request(txpBandTable_registration * user_context, int rc)
{
    DEBUGMSGTL(("verbose:txpBandTable:txpBandTable_post_request","called\n"));

    /*
     * TODO:511:o: Perform txpBandTable post-request actions.
     */

    return MFD_SUCCESS;
} /* txpBandTable_post_request */


/** @{ */
