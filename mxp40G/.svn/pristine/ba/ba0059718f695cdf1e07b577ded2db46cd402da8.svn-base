/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "../../mxpEnvTable.h"
#include "../../../../lib/hl_configs/definitions.h"

void shm_handler_create(Monitor *pt_monitor_struct);
Monitor *pt_monitor_struct;

#include <stdio.h>
#include <sys/mman.h>
#include <sys/types.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <stdlib.h>

#include "mxpEnvTable_data_access.h"

/** @ingroup interface
 * @addtogroup data_access data_access: Routines to access data
 *
 * These routines are used to locate the data used to satisfy
 * requests.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table mxpEnvTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::mxpEnvTable is subid 7 of mxpMIBObjects.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.2.7, length: 9
*/

/**
 * initialization for mxpEnvTable data access
 *
 * This function is called during startup to allow you to
 * allocate any resources you need for the data table.
 *
 * @param mxpEnvTable_reg
 *        Pointer to mxpEnvTable_registration
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : unrecoverable error.
 */
int
mxpEnvTable_init_data(mxpEnvTable_registration * mxpEnvTable_reg)
{
    DEBUGMSGTL(("verbose:mxpEnvTable:mxpEnvTable_init_data","called\n"));

    /*
     * TODO:303:o: Initialize mxpEnvTable data.
     */
    /*
    ***************************************************
    ***             START EXAMPLE CODE              ***
    ***---------------------------------------------***/
    /*
     * if you are the sole writer for the file, you could
     * open it here. However, as stated earlier, we are assuming
     * the worst case, which in this case means that the file is
     * written to by someone else, and might not even exist when
     * we start up. So we can't do anything here.
     */
    /*
    ***---------------------------------------------***
    ***              END  EXAMPLE CODE              ***
    ***************************************************/

    return MFD_SUCCESS;
} /* mxpEnvTable_init_data */

/**
 * container overview
 *
 */

/**
 * container initialization
 *
 * @param container_ptr_ptr A pointer to a container pointer. If you
 *        create a custom container, use this parameter to return it
 *        to the MFD helper. If set to NULL, the MFD helper will
 *        allocate a container for you.
 * @param  cache A pointer to a cache structure. You can set the timeout
 *         and other cache flags using this pointer.
 *
 *  This function is called at startup to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases. If no custom
 *  container is allocated, the MFD code will create one for your.
 *
 *  This is also the place to set up cache behavior. The default, to
 *  simply set the cache timeout, will work well with the default
 *  container. If you are using a custom container, you may want to
 *  look at the cache helper documentation to see if there are any
 *  flags you want to set.
 *
 * @remark
 *  This would also be a good place to do any initialization needed
 *  for you data source. For example, opening a connection to another
 *  process that will supply the data, opening a database, etc.
 */
void
mxpEnvTable_container_init(netsnmp_container **container_ptr_ptr,
                             netsnmp_cache *cache)
{
    DEBUGMSGTL(("verbose:mxpEnvTable:mxpEnvTable_container_init","called\n"));
    
    if (NULL == container_ptr_ptr) {
        snmp_log(LOG_ERR,"bad container param to mxpEnvTable_container_init\n");
        return;
    }

    /*
     * For advanced users, you can use a custom container. If you
     * do not create one, one will be created for you.
     */
    *container_ptr_ptr = NULL;

    if (NULL == cache) {
        snmp_log(LOG_ERR,"bad cache param to mxpEnvTable_container_init\n");
        return;
    }

    /*
     * TODO:345:A: Set up mxpEnvTable cache properties.
     *
     * Also for advanced users, you can set parameters for the
     * cache. Do not change the magic pointer, as it is used
     * by the MFD helper. To completely disable caching, set
     * cache->enabled to 0.
     */
    cache->timeout = MXPENVTABLE_CACHE_TIMEOUT; /* seconds */
} /* mxpEnvTable_container_init */

/**
 * container shutdown
 *
 * @param container_ptr A pointer to the container.
 *
 *  This function is called at shutdown to allow you to customize certain
 *  aspects of the access method. For the most part, it is for advanced
 *  users. The default code should suffice for most cases.
 *
 *  This function is called before mxpEnvTable_container_free().
 *
 * @remark
 *  This would also be a good place to do any cleanup needed
 *  for you data source. For example, closing a connection to another
 *  process that supplied the data, closing a database, etc.
 */
void
mxpEnvTable_container_shutdown(netsnmp_container *container_ptr)
{
    DEBUGMSGTL(("verbose:mxpEnvTable:mxpEnvTable_container_shutdown","called\n"));
    
    if (NULL == container_ptr) {
        snmp_log(LOG_ERR,"bad params to mxpEnvTable_container_shutdown\n");
        return;
    }

} /* mxpEnvTable_container_shutdown */

/**
 * load initial data
 *
 * TODO:350:M: Implement mxpEnvTable data load
 * This function will also be called by the cache helper to load
 * the container again (after the container free function has been
 * called to free the previous contents).
 *
 * @param container container to which items should be inserted
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_RESOURCE_UNAVAILABLE : Can't access data source
 * @retval MFD_ERROR                : other error.
 *
 *  This function is called to load the index(es) (and data, optionally)
 *  for the every row in the data set.
 *
 * @remark
 *  While loading the data, the only important thing is the indexes.
 *  If access to your data is cheap/fast (e.g. you have a pointer to a
 *  structure in memory), it would make sense to update the data here.
 *  If, however, the accessing the data invovles more work (e.g. parsing
 *  some other existing data, or peforming calculations to derive the data),
 *  then you can limit yourself to setting the indexes and saving any
 *  information you will need later. Then use the saved information in
 *  mxpEnvTable_row_prep() for populating data.
 *
 * @note
 *  If you need consistency between rows (like you want statistics
 *  for each row to be from the same time frame), you should set all
 *  data here.
 *
 */
int
mxpEnvTable_container_load(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:mxpEnvTable:mxpEnvTable_container_load","called\n"));

    mxpEnvTable_rowreq_ctx *rowreq_ctx;
    size_t                 count = 0;

    /*
     * temporary storage for index values
     */
        /*
         * mxpEnvIndex(1)/OCTETSTR/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h
         */
    char mxpEnvIndex[32];
    strcpy (mxpEnvIndex, "mxpEnvIndex");
    size_t mxpEnvIndex_len = strlen(mxpEnvIndex);
    
    shm_handler_create(pt_monitor_struct);

        /*
         * TODO:352:M: |   |-> set indexes in new mxpEnvTable rowreq context.
         * data context will be set from the param (unless NULL,
         *      in which case a new data context will be allocated)
         */
        rowreq_ctx = mxpEnvTable_allocate_rowreq_ctx(NULL);
        if (NULL == rowreq_ctx) {
            snmp_log(LOG_ERR, "memory allocation failed\n");
            return MFD_RESOURCE_UNAVAILABLE;
        }
        if(MFD_SUCCESS != mxpEnvTable_indexes_set(rowreq_ctx
                               , mxpEnvIndex, mxpEnvIndex_len
               )) {
            snmp_log(LOG_ERR,"error setting index while loading "
                     "mxpEnvTable data.\n");
            mxpEnvTable_release_rowreq_ctx(rowreq_ctx);
        }

        /*
         * TODO:352:r: |   |-> populate mxpEnvTable data context.
         * Populate data context here. (optionally, delay until row prep)
         */
    /*
     * TRANSIENT or semi-TRANSIENT data:
     * copy data or save any info needed to do it in row_prep.
     */
    /*
     * setup/save data for mxpTempT41Around
     * mxpTempT41Around(2)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for mxpTempT41Around data
     */
     
    char mxpTempT41Around[65535];
    sprintf(output_char, "%.2f", pt_monitor_struct->general_struct.cs6041_temperature[0]);
	strcpy (mxpTempT41Around, output_char);
	size_t mxpTempT41Around_len = strlen(mxpTempT41Around);
     
    rowreq_ctx->data.mxpTempT41Around_len = mxpTempT41Around_len* sizeof(mxpTempT41Around[0]);
    if ((NULL == rowreq_ctx->data.mxpTempT41Around) ||
        (rowreq_ctx->data.mxpTempT41Around_len <
         (mxpTempT41Around_len* sizeof(mxpTempT41Around[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (mxpTempT41Around)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.mxpTempT41Around, mxpTempT41Around, mxpTempT41Around_len* sizeof(mxpTempT41Around[0]) );
    
    /*
     * setup/save data for mxpTempT41TS0
     * mxpTempT41TS0(3)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for mxpTempT41TS0 data
     */
    
    char mxpTempT41TS0[65535];
    sprintf(output_char, "%.2f", pt_monitor_struct->general_struct.cs6041_temperature[1]);
	strcpy (mxpTempT41TS0, output_char);
	size_t mxpTempT41TS0_len = strlen(mxpTempT41TS0);
    
    rowreq_ctx->data.mxpTempT41TS0_len = mxpTempT41TS0_len* sizeof(mxpTempT41TS0[0]);
    if ((NULL == rowreq_ctx->data.mxpTempT41TS0) ||
        (rowreq_ctx->data.mxpTempT41TS0_len <
         (mxpTempT41TS0_len* sizeof(mxpTempT41TS0[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (mxpTempT41TS0)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.mxpTempT41TS0, mxpTempT41TS0, mxpTempT41TS0_len* sizeof(mxpTempT41TS0[0]) );
    
    /*
     * setup/save data for mxpTempT41TS1
     * mxpTempT41TS1(4)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for mxpTempT41TS1 data
     */

    char mxpTempT41TS1[65535];
    sprintf(output_char, "%.2f", pt_monitor_struct->general_struct.cs6041_temperature[2]);
	strcpy (mxpTempT41TS1, output_char);
	size_t mxpTempT41TS1_len = strlen(mxpTempT41TS1);

    rowreq_ctx->data.mxpTempT41TS1_len = mxpTempT41TS1_len* sizeof(mxpTempT41TS1[0]);    
    if ((NULL == rowreq_ctx->data.mxpTempT41TS1) ||
        (rowreq_ctx->data.mxpTempT41TS1_len <
         (mxpTempT41TS1_len* sizeof(mxpTempT41TS1[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (mxpTempT41TS1)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.mxpTempT41TS1, mxpTempT41TS1, mxpTempT41TS1_len* sizeof(mxpTempT41TS1[0]) );
    
    /*
     * setup/save data for mxpTempT41TS2
     * mxpTempT41TS2(5)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for mxpTempT41TS2 data
     */
    
    char mxpTempT41TS2[65535];
    sprintf(output_char, "%.2f", pt_monitor_struct->general_struct.cs6041_temperature[3]);
	strcpy (mxpTempT41TS2, output_char);
	size_t mxpTempT41TS2_len = strlen(mxpTempT41TS2);
    
    rowreq_ctx->data.mxpTempT41TS2_len = mxpTempT41TS2_len* sizeof(mxpTempT41TS2[0]);
    if ((NULL == rowreq_ctx->data.mxpTempT41TS2) ||
        (rowreq_ctx->data.mxpTempT41TS2_len <
         (mxpTempT41TS2_len* sizeof(mxpTempT41TS2[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (mxpTempT41TS2)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.mxpTempT41TS2, mxpTempT41TS2, mxpTempT41TS2_len* sizeof(mxpTempT41TS2[0]) );
    
    /*
     * setup/save data for mxpTempFPGA
     * mxpTempFPGA(6)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for mxpTempFPGA data
     */
    
    char mxpTempFPGA[65535];
    sprintf(output_char, "%.2f", pt_monitor_struct->general_struct.fpga_temperature);
	strcpy (mxpTempFPGA, output_char);
	size_t mxpTempFPGA_len = strlen(mxpTempFPGA);
    
    rowreq_ctx->data.mxpTempFPGA_len = mxpTempFPGA_len* sizeof(mxpTempFPGA[0]);
    if ((NULL == rowreq_ctx->data.mxpTempFPGA) ||
        (rowreq_ctx->data.mxpTempFPGA_len <
         (mxpTempFPGA_len* sizeof(mxpTempFPGA[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (mxpTempFPGA)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.mxpTempFPGA, mxpTempFPGA, mxpTempFPGA_len* sizeof(mxpTempFPGA[0]) );
    
    /*
     * setup/save data for mxpTempBOARD
     * mxpTempBOARD(7)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for mxpTempBOARD data
     */
    
    char mxpTempBOARD[65535];
    sprintf(output_char, "%d", pt_monitor_struct->general_struct.board_temperature);
	strcpy (mxpTempBOARD, output_char);
	size_t mxpTempBOARD_len = strlen(mxpTempBOARD);
    
    rowreq_ctx->data.mxpTempBOARD_len = mxpTempBOARD_len* sizeof(mxpTempBOARD[0]);
    if ((NULL == rowreq_ctx->data.mxpTempBOARD) ||
        (rowreq_ctx->data.mxpTempBOARD_len <
         (mxpTempBOARD_len* sizeof(mxpTempBOARD[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (mxpTempBOARD)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.mxpTempBOARD, mxpTempBOARD, mxpTempBOARD_len* sizeof(mxpTempBOARD[0]) );
    
    /*
     * setup/save data for mxpTempEdfa
     * mxpTempEdfa(8)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for mxpTempEdfa data
     */
    char mxpTempEdfa[65535];
    strcpy (mxpTempEdfa, pt_monitor_struct->edfa_struct.edfa_temperature);
    size_t mxpTempEdfa_len = strlen(mxpTempEdfa);
    
    rowreq_ctx->data.mxpTempEdfa_len = mxpTempEdfa_len* sizeof(mxpTempEdfa[0]);
    if ((NULL == rowreq_ctx->data.mxpTempEdfa) ||
        (rowreq_ctx->data.mxpTempEdfa_len <
         (mxpTempEdfa_len* sizeof(mxpTempEdfa[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (mxpTempEdfa)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.mxpTempEdfa, mxpTempEdfa, mxpTempEdfa_len* sizeof(mxpTempEdfa[0]) );
    
    /*
     * setup/save data for mxpHumedity
     * mxpHumidity(9)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/r/d/h
     */
    /** no mapping */
    /*
     * make sure there is enough space for mxpHumidity data
     */
    
    char mxpHumidity[65535];
	sprintf(output_char, "%d", pt_monitor_struct->general_struct.board_humidity);
	strcpy (mxpHumidity, output_char);
	size_t mxpHumidity_len = strlen(mxpHumidity);
    
    rowreq_ctx->data.mxpHumedity_len = mxpHumidity_len* sizeof(mxpHumidity[0]);
    if ((NULL == rowreq_ctx->data.mxpHumedity) ||
        (rowreq_ctx->data.mxpHumedity_len <
         (mxpHumidity_len* sizeof(mxpHumidity[0])))) {
        snmp_log(LOG_ERR,"not enough space for value (mxpHumidity)\n");
        return MFD_ERROR;
    }
    memcpy( rowreq_ctx->data.mxpHumedity, mxpHumidity, mxpHumidity_len* sizeof(mxpHumidity[0]) );

        /*
         * insert into table container
         */
        CONTAINER_INSERT(container, rowreq_ctx);
        ++count;

    DEBUGMSGT(("verbose:mxpEnvTable:mxpEnvTable_container_load",
               "inserted %d records\n", count));

    return MFD_SUCCESS;
} /* mxpEnvTable_container_load */

/**
 * container clean up
 *
 * @param container container with all current items
 *
 *  This optional callback is called prior to all
 *  item's being removed from the container. If you
 *  need to do any processing before that, do it here.
 *
 * @note
 *  The MFD helper will take care of releasing all the row contexts.
 *
 */
void
mxpEnvTable_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("verbose:mxpEnvTable:mxpEnvTable_container_free","called\n"));

    /*
     * TODO:380:M: Free mxpEnvTable container data.
     */
} /* mxpEnvTable_container_free */

/**
 * prepare row for processing.
 *
 *  When the agent has located the row for a request, this function is
 *  called to prepare the row for processing. If you fully populated
 *  the data context during the index setup phase, you may not need to
 *  do anything.
 *
 * @param rowreq_ctx pointer to a context.
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 */
int
mxpEnvTable_row_prep( mxpEnvTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:mxpEnvTable:mxpEnvTable_row_prep","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:390:o: Prepare row for request.
     * If populating row data was delayed, this is the place to
     * fill in the row for this request.
     */

    return MFD_SUCCESS;
} /* mxpEnvTable_row_prep */

/** @} */
