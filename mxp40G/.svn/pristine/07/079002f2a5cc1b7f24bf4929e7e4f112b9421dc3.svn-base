/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "../../txpPhyspadTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement txpPhyspadTable get routines.
 * TODO:240:M: Implement txpPhyspadTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table txpPhyspadTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::txpPhyspadTable is subid 5 of txpMiscellaneous.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.2.9.2.5, length: 11
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement txpPhyspadTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param txpPhyspadIndex_ptr
 * @param txpPhyspadIndex_ptr_len
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
txpPhyspadTable_indexes_set_tbl_idx(txpPhyspadTable_mib_index *tbl_idx, char *txpPhyspadIndex_val_ptr,  size_t txpPhyspadIndex_val_ptr_len)
{
    DEBUGMSGTL(("verbose:txpPhyspadTable:txpPhyspadTable_indexes_set_tbl_idx","called\n"));

    /* txpPhyspadIndex(1)/OCTETSTR/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/h */
    tbl_idx->txpPhyspadIndex_len = sizeof(tbl_idx->txpPhyspadIndex)/sizeof(tbl_idx->txpPhyspadIndex[0]); /* max length */
    /*
     * make sure there is enough space for txpPhyspadIndex data
     */
    if ((NULL == tbl_idx->txpPhyspadIndex) ||
        (tbl_idx->txpPhyspadIndex_len <
         (txpPhyspadIndex_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value (txpPhyspadIndex_val_ptr)\n");
        return MFD_ERROR;
    }
    tbl_idx->txpPhyspadIndex_len = txpPhyspadIndex_val_ptr_len;
    memcpy( tbl_idx->txpPhyspadIndex, txpPhyspadIndex_val_ptr, txpPhyspadIndex_val_ptr_len* sizeof(txpPhyspadIndex_val_ptr[0]) );
    

    return MFD_SUCCESS;
} /* txpPhyspadTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
txpPhyspadTable_indexes_set(txpPhyspadTable_rowreq_ctx *rowreq_ctx, char *txpPhyspadIndex_val_ptr,  size_t txpPhyspadIndex_val_ptr_len)
{
    DEBUGMSGTL(("verbose:txpPhyspadTable:txpPhyspadTable_indexes_set","called\n"));

    if(MFD_SUCCESS != txpPhyspadTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , txpPhyspadIndex_val_ptr, txpPhyspadIndex_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != txpPhyspadTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* txpPhyspadTable_indexes_set */


/*---------------------------------------------------------------------
 * FTNC-MIB::txpPhyspadEntry.txpLoss
 * txpLoss is subid 2 of txpPhyspadEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.2.5.1.2
 * Description:
txp40G pin loss input alarm.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpLoss data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpLoss_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpLoss_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpLoss.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpLoss_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpLoss_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpLoss_get( txpPhyspadTable_rowreq_ctx *rowreq_ctx, char **txpLoss_val_ptr_ptr, size_t *txpLoss_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpLoss_val_ptr_ptr) && (NULL != *txpLoss_val_ptr_ptr));
   netsnmp_assert( NULL != txpLoss_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpPhyspadTable:txpLoss_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpLoss data.
 * copy (* txpLoss_val_ptr_ptr ) data and (* txpLoss_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpLoss data
     */
    if ((NULL == (* txpLoss_val_ptr_ptr )) ||
        ((* txpLoss_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpLoss_len* sizeof(rowreq_ctx->data.txpLoss[0])))) {
        /*
         * allocate space for txpLoss data
         */
        (* txpLoss_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpLoss_len* sizeof(rowreq_ctx->data.txpLoss[0]));
        if(NULL == (* txpLoss_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpLoss)\n");
            return MFD_ERROR;
        }
    }
    (* txpLoss_val_ptr_len_ptr ) = rowreq_ctx->data.txpLoss_len* sizeof(rowreq_ctx->data.txpLoss[0]);
    memcpy( (* txpLoss_val_ptr_ptr ), rowreq_ctx->data.txpLoss, rowreq_ctx->data.txpLoss_len* sizeof(rowreq_ctx->data.txpLoss[0]) );

    return MFD_SUCCESS;
} /* txpLoss_get */

/*---------------------------------------------------------------------
 * FTNC-MIB::txpPhyspadEntry.txpInterrupt
 * txpInterrupt is subid 3 of txpPhyspadEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.43484.2.9.2.5.1.3
 * Description:
txp40G pin interrupt alarm.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Extract the current value of the txpInterrupt data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param txpInterrupt_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param txpInterrupt_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by txpInterrupt.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*txpInterrupt_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update txpInterrupt_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
txpInterrupt_get( txpPhyspadTable_rowreq_ctx *rowreq_ctx, char **txpInterrupt_val_ptr_ptr, size_t *txpInterrupt_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != txpInterrupt_val_ptr_ptr) && (NULL != *txpInterrupt_val_ptr_ptr));
   netsnmp_assert( NULL != txpInterrupt_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:txpPhyspadTable:txpInterrupt_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the txpInterrupt data.
 * copy (* txpInterrupt_val_ptr_ptr ) data and (* txpInterrupt_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for txpInterrupt data
     */
    if ((NULL == (* txpInterrupt_val_ptr_ptr )) ||
        ((* txpInterrupt_val_ptr_len_ptr ) <
         (rowreq_ctx->data.txpInterrupt_len* sizeof(rowreq_ctx->data.txpInterrupt[0])))) {
        /*
         * allocate space for txpInterrupt data
         */
        (* txpInterrupt_val_ptr_ptr ) = malloc(rowreq_ctx->data.txpInterrupt_len* sizeof(rowreq_ctx->data.txpInterrupt[0]));
        if(NULL == (* txpInterrupt_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory (rowreq_ctx->data.txpInterrupt)\n");
            return MFD_ERROR;
        }
    }
    (* txpInterrupt_val_ptr_len_ptr ) = rowreq_ctx->data.txpInterrupt_len* sizeof(rowreq_ctx->data.txpInterrupt[0]);
    memcpy( (* txpInterrupt_val_ptr_ptr ), rowreq_ctx->data.txpInterrupt, rowreq_ctx->data.txpInterrupt_len* sizeof(rowreq_ctx->data.txpInterrupt[0]) );

    return MFD_SUCCESS;
} /* txpInterrupt_get */



/** @} */
