/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "tpClient4SeverityTable.h"

#include <stdio.h>
#include <sys/mman.h>
#include <sys/types.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <stdlib.h>
#include "../../../../lib/hl_configs/definitions.h"

void shm_handler_create(Monitor *pt_monitor_struct);
Monitor *pt_monitor_struct;

/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table tpClient4SeverityTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * FTNC-MIB::tpClient4SeverityTable is subid 5 of tpAlarms.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * tpClient4SeverityTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * tpClient4SeverityTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
tpClient4SeverityTable_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup tpClient4SeverityTable undo.
     * set up tpClient4SeverityTable undo information, in preparation for a set.
     * Undo storage is in (* tpClient4SeveritySecmonLom_val_ptr )*
     */

    return rc;
} /* tpClient4SeverityTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
tpClient4SeverityTable_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> tpClient4SeverityTable undo.
     * tpClient4SeverityTable undo information, in response to a failed set.
     * Undo storage is in (* tpClient4SeveritySecmonLom_val_ptr )*
     */

    return rc;
} /* tpClient4SeverityTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
tpClient4SeverityTable_undo_cleanup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup tpClient4SeverityTable undo.
     * Undo storage is in (* tpClient4SeveritySecmonLom_val_ptr )*
     */

    return rc;
} /* tpClient4SeverityTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * tpClient4SeverityTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param tpClient4SeverityTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
tpClient4SeverityTable_commit( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit tpClient4SeverityTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_TPCLIENT4SEVERITYFRAMERFSMMS_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYFRAMERFSMMS_FLAG; /* clear tpClient4SeverityFramerFsmms */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityFramerFsmms.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityFramerFsmms commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityFramerFsmms
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYFRAMERFSMMS_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYFRAMERFSSUSTS_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYFRAMERFSSUSTS_FLAG; /* clear tpClient4SeverityFramerFssusts */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityFramerFssusts.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityFramerFssusts commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityFramerFssusts
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYFRAMERFSSUSTS_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYFRAMERSOOF_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYFRAMERSOOF_FLAG; /* clear tpClient4SeverityFramerSoof */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityFramerSoof.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityFramerSoof commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityFramerSoof
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYFRAMERSOOF_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYFRAMERSLOF_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYFRAMERSLOF_FLAG; /* clear tpClient4SeverityFramerSlof */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityFramerSlof.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityFramerSlof commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityFramerSlof
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYFRAMERSLOF_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYFRAMERSIF_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYFRAMERSIF_FLAG; /* clear tpClient4SeverityFramerSif */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityFramerSif.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityFramerSif commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityFramerSif
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYFRAMERSIF_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYFRAMERFST_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYFRAMERFST_FLAG; /* clear tpClient4SeverityFramerFst */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityFramerFst.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityFramerFst commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityFramerFst
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYFRAMERFST_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYLOSSTATLOSF_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYLOSSTATLOSF_FLAG; /* clear tpClient4SeverityLosstatLosf */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityLosstatLosf.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityLosstatLosf commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityLosstatLosf
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYLOSSTATLOSF_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYPATHMONACCSTAT_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYPATHMONACCSTAT_FLAG; /* clear tpClient4SeverityPathmonAccstat */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityPathmonAccstat.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityPathmonAccstat commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityPathmonAccstat
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYPATHMONACCSTAT_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYPATHMONDAIS_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYPATHMONDAIS_FLAG; /* clear tpClient4SeverityPathmonDais */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityPathmonDais.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityPathmonDais commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityPathmonDais
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYPATHMONDAIS_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYPATHMONDBDI_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYPATHMONDBDI_FLAG; /* clear tpClient4SeverityPathmonDbdi */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityPathmonDbdi.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityPathmonDbdi commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityPathmonDbdi
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYPATHMONDBDI_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYPATHMONDLCK_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYPATHMONDLCK_FLAG; /* clear tpClient4SeverityPathmonDlck */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityPathmonDlck.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityPathmonDlck commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityPathmonDlck
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYPATHMONDLCK_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYPATHMONMSIUNST_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYPATHMONMSIUNST_FLAG; /* clear tpClient4SeverityPathmonMsiunst */
       /*
        * TODO:482:o: |-> commit column tpClient4SeverityPathmonMsiunst.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeverityPathmonMsiunst commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeverityPathmonMsiunst
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYPATHMONMSIUNST_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYSECMONBIPSFS_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYSECMONBIPSFS_FLAG; /* clear tpClient4SeveritySecmonBipsfs */
       /*
        * TODO:482:o: |-> commit column tpClient4SeveritySecmonBipsfs.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeveritySecmonBipsfs commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeveritySecmonBipsfs
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYSECMONBIPSFS_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYSECMONDBDI_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYSECMONDBDI_FLAG; /* clear tpClient4SeveritySecmonDbdi */
       /*
        * TODO:482:o: |-> commit column tpClient4SeveritySecmonDbdi.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeveritySecmonDbdi commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeveritySecmonDbdi
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYSECMONDBDI_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYSECMONDIAE_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYSECMONDIAE_FLAG; /* clear tpClient4SeveritySecmonDiae */
       /*
        * TODO:482:o: |-> commit column tpClient4SeveritySecmonDiae.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeveritySecmonDiae commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeveritySecmonDiae
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYSECMONDIAE_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYSECMONFLOM_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYSECMONFLOM_FLAG; /* clear tpClient4SeveritySecmonFlom */
       /*
        * TODO:482:o: |-> commit column tpClient4SeveritySecmonFlom.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeveritySecmonFlom commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeveritySecmonFlom
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYSECMONFLOM_FLAG;
       }
    }

    if (save_flags & COLUMN_TPCLIENT4SEVERITYSECMONLOM_FLAG) {
       save_flags &= ~COLUMN_TPCLIENT4SEVERITYSECMONLOM_FLAG; /* clear tpClient4SeveritySecmonLom */
       /*
        * TODO:482:o: |-> commit column tpClient4SeveritySecmonLom.
        */
       //rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"tpClient4SeverityTable column tpClient4SeveritySecmonLom commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo tpClient4SeveritySecmonLom
             */
            rowreq_ctx->column_set_flags |= COLUMN_TPCLIENT4SEVERITYSECMONLOM_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* tpClient4SeverityTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * tpClient4SeverityTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param tpClient4SeverityTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
tpClient4SeverityTable_undo_commit( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo tpClient4SeverityTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* tpClient4SeverityTable_undo_commit */

/*
 * TODO:440:M: Implement tpClient4SeverityTable node value checks.
 * TODO:450:M: Implement tpClient4SeverityTable undo functions.
 * TODO:460:M: Implement tpClient4SeverityTable set functions.
 * TODO:480:M: Implement tpClient4SeverityTable commit functions.
 */
/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityFramerFsmms
 * tpClient4SeverityFramerFsmms is subid 2 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.2
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityFramerFsmms_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityFramerFsmms_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerFsmms_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFsmms_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityFramerFsmms value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityFramerFsmms value not illegal */
} /* tpClient4SeverityFramerFsmms_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityFramerFsmms_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFsmms_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityFramerFsmms undo.
     */
    /*
     * copy tpClient4SeverityFramerFsmms data
     * set rowreq_ctx->undo->tpClient4SeverityFramerFsmms from rowreq_ctx->data.tpClient4SeverityFramerFsmms
     */
    rowreq_ctx->undo->tpClient4SeverityFramerFsmms = rowreq_ctx->data.tpClient4SeverityFramerFsmms;


    return MFD_SUCCESS;
} /* tpClient4SeverityFramerFsmms_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityFramerFsmms_val
 *        A long containing the new value.
 */
int
tpClient4SeverityFramerFsmms_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerFsmms_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFsmms_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityFramerFsmms value.
     * set tpClient4SeverityFramerFsmms value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityFramerFsmms = tpClient4SeverityFramerFsmms_val;

    // Set severity on shared memory
    setTpClient4Severity("FramerFsmms", tpClient4SeverityFramerFsmms_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityFramerFsmms_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityFramerFsmms_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFsmms_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityFramerFsmms undo.
     */
    /*
     * copy tpClient4SeverityFramerFsmms data
     * set rowreq_ctx->data.tpClient4SeverityFramerFsmms from rowreq_ctx->undo->tpClient4SeverityFramerFsmms
     */
    rowreq_ctx->data.tpClient4SeverityFramerFsmms = rowreq_ctx->undo->tpClient4SeverityFramerFsmms;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityFramerFsmms_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityFramerFssusts
 * tpClient4SeverityFramerFssusts is subid 3 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.3
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityFramerFssusts_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityFramerFssusts_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerFssusts_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFssusts_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityFramerFssusts value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityFramerFssusts value not illegal */
} /* tpClient4SeverityFramerFssusts_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityFramerFssusts_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFssusts_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityFramerFssusts undo.
     */
    /*
     * copy tpClient4SeverityFramerFssusts data
     * set rowreq_ctx->undo->tpClient4SeverityFramerFssusts from rowreq_ctx->data.tpClient4SeverityFramerFssusts
     */
    rowreq_ctx->undo->tpClient4SeverityFramerFssusts = rowreq_ctx->data.tpClient4SeverityFramerFssusts;


    return MFD_SUCCESS;
} /* tpClient4SeverityFramerFssusts_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityFramerFssusts_val
 *        A long containing the new value.
 */
int
tpClient4SeverityFramerFssusts_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerFssusts_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFssusts_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityFramerFssusts value.
     * set tpClient4SeverityFramerFssusts value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityFramerFssusts = tpClient4SeverityFramerFssusts_val;

    // Set severity on shared memory
    setTpClient4Severity("FramerFssusts", tpClient4SeverityFramerFssusts_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityFramerFssusts_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityFramerFssusts_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFssusts_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityFramerFssusts undo.
     */
    /*
     * copy tpClient4SeverityFramerFssusts data
     * set rowreq_ctx->data.tpClient4SeverityFramerFssusts from rowreq_ctx->undo->tpClient4SeverityFramerFssusts
     */
    rowreq_ctx->data.tpClient4SeverityFramerFssusts = rowreq_ctx->undo->tpClient4SeverityFramerFssusts;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityFramerFssusts_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityFramerSoof
 * tpClient4SeverityFramerSoof is subid 4 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.4
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityFramerSoof_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityFramerSoof_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerSoof_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSoof_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityFramerSoof value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityFramerSoof value not illegal */
} /* tpClient4SeverityFramerSoof_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityFramerSoof_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSoof_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityFramerSoof undo.
     */
    /*
     * copy tpClient4SeverityFramerSoof data
     * set rowreq_ctx->undo->tpClient4SeverityFramerSoof from rowreq_ctx->data.tpClient4SeverityFramerSoof
     */
    rowreq_ctx->undo->tpClient4SeverityFramerSoof = rowreq_ctx->data.tpClient4SeverityFramerSoof;


    return MFD_SUCCESS;
} /* tpClient4SeverityFramerSoof_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityFramerSoof_val
 *        A long containing the new value.
 */
int
tpClient4SeverityFramerSoof_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerSoof_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSoof_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityFramerSoof value.
     * set tpClient4SeverityFramerSoof value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityFramerSoof = tpClient4SeverityFramerSoof_val;

    // Set severity on shared memory
    setTpClient4Severity("FramerSoof", tpClient4SeverityFramerSoof_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityFramerSoof_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityFramerSoof_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSoof_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityFramerSoof undo.
     */
    /*
     * copy tpClient4SeverityFramerSoof data
     * set rowreq_ctx->data.tpClient4SeverityFramerSoof from rowreq_ctx->undo->tpClient4SeverityFramerSoof
     */
    rowreq_ctx->data.tpClient4SeverityFramerSoof = rowreq_ctx->undo->tpClient4SeverityFramerSoof;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityFramerSoof_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityFramerSlof
 * tpClient4SeverityFramerSlof is subid 5 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.5
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityFramerSlof_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityFramerSlof_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerSlof_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSlof_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityFramerSlof value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityFramerSlof value not illegal */
} /* tpClient4SeverityFramerSlof_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityFramerSlof_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSlof_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityFramerSlof undo.
     */
    /*
     * copy tpClient4SeverityFramerSlof data
     * set rowreq_ctx->undo->tpClient4SeverityFramerSlof from rowreq_ctx->data.tpClient4SeverityFramerSlof
     */
    rowreq_ctx->undo->tpClient4SeverityFramerSlof = rowreq_ctx->data.tpClient4SeverityFramerSlof;


    return MFD_SUCCESS;
} /* tpClient4SeverityFramerSlof_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityFramerSlof_val
 *        A long containing the new value.
 */
int
tpClient4SeverityFramerSlof_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerSlof_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSlof_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityFramerSlof value.
     * set tpClient4SeverityFramerSlof value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityFramerSlof = tpClient4SeverityFramerSlof_val;

    // Set severity on shared memory
    setTpClient4Severity("FramerSlof", tpClient4SeverityFramerSlof_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityFramerSlof_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityFramerSlof_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSlof_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityFramerSlof undo.
     */
    /*
     * copy tpClient4SeverityFramerSlof data
     * set rowreq_ctx->data.tpClient4SeverityFramerSlof from rowreq_ctx->undo->tpClient4SeverityFramerSlof
     */
    rowreq_ctx->data.tpClient4SeverityFramerSlof = rowreq_ctx->undo->tpClient4SeverityFramerSlof;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityFramerSlof_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityFramerSif
 * tpClient4SeverityFramerSif is subid 6 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.6
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityFramerSif_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityFramerSif_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerSif_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSif_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityFramerSif value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityFramerSif value not illegal */
} /* tpClient4SeverityFramerSif_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityFramerSif_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSif_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityFramerSif undo.
     */
    /*
     * copy tpClient4SeverityFramerSif data
     * set rowreq_ctx->undo->tpClient4SeverityFramerSif from rowreq_ctx->data.tpClient4SeverityFramerSif
     */
    rowreq_ctx->undo->tpClient4SeverityFramerSif = rowreq_ctx->data.tpClient4SeverityFramerSif;


    return MFD_SUCCESS;
} /* tpClient4SeverityFramerSif_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityFramerSif_val
 *        A long containing the new value.
 */
int
tpClient4SeverityFramerSif_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerSif_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSif_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityFramerSif value.
     * set tpClient4SeverityFramerSif value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityFramerSif = tpClient4SeverityFramerSif_val;

    // Set severity on shared memory
    setTpClient4Severity("FramerSif", tpClient4SeverityFramerSif_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityFramerSif_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityFramerSif_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerSif_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityFramerSif undo.
     */
    /*
     * copy tpClient4SeverityFramerSif data
     * set rowreq_ctx->data.tpClient4SeverityFramerSif from rowreq_ctx->undo->tpClient4SeverityFramerSif
     */
    rowreq_ctx->data.tpClient4SeverityFramerSif = rowreq_ctx->undo->tpClient4SeverityFramerSif;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityFramerSif_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityFramerFst
 * tpClient4SeverityFramerFst is subid 7 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.7
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityFramerFst_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityFramerFst_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerFst_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFst_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityFramerFst value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityFramerFst value not illegal */
} /* tpClient4SeverityFramerFst_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityFramerFst_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFst_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityFramerFst undo.
     */
    /*
     * copy tpClient4SeverityFramerFst data
     * set rowreq_ctx->undo->tpClient4SeverityFramerFst from rowreq_ctx->data.tpClient4SeverityFramerFst
     */
    rowreq_ctx->undo->tpClient4SeverityFramerFst = rowreq_ctx->data.tpClient4SeverityFramerFst;


    return MFD_SUCCESS;
} /* tpClient4SeverityFramerFst_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityFramerFst_val
 *        A long containing the new value.
 */
int
tpClient4SeverityFramerFst_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityFramerFst_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFst_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityFramerFst value.
     * set tpClient4SeverityFramerFst value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityFramerFst = tpClient4SeverityFramerFst_val;

    // Set severity on shared memory
    setTpClient4Severity("FramerFst", tpClient4SeverityFramerFst_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityFramerFst_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityFramerFst_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityFramerFst_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityFramerFst undo.
     */
    /*
     * copy tpClient4SeverityFramerFst data
     * set rowreq_ctx->data.tpClient4SeverityFramerFst from rowreq_ctx->undo->tpClient4SeverityFramerFst
     */
    rowreq_ctx->data.tpClient4SeverityFramerFst = rowreq_ctx->undo->tpClient4SeverityFramerFst;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityFramerFst_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityLosstatLosf
 * tpClient4SeverityLosstatLosf is subid 8 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.8
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityLosstatLosf_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityLosstatLosf_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityLosstatLosf_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityLosstatLosf_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityLosstatLosf value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityLosstatLosf value not illegal */
} /* tpClient4SeverityLosstatLosf_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityLosstatLosf_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityLosstatLosf_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityLosstatLosf undo.
     */
    /*
     * copy tpClient4SeverityLosstatLosf data
     * set rowreq_ctx->undo->tpClient4SeverityLosstatLosf from rowreq_ctx->data.tpClient4SeverityLosstatLosf
     */
    rowreq_ctx->undo->tpClient4SeverityLosstatLosf = rowreq_ctx->data.tpClient4SeverityLosstatLosf;


    return MFD_SUCCESS;
} /* tpClient4SeverityLosstatLosf_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityLosstatLosf_val
 *        A long containing the new value.
 */
int
tpClient4SeverityLosstatLosf_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityLosstatLosf_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityLosstatLosf_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityLosstatLosf value.
     * set tpClient4SeverityLosstatLosf value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityLosstatLosf = tpClient4SeverityLosstatLosf_val;

    // Set severity on shared memory
    setTpClient4Severity("LosstatLosf", tpClient4SeverityLosstatLosf_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityLosstatLosf_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityLosstatLosf_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityLosstatLosf_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityLosstatLosf undo.
     */
    /*
     * copy tpClient4SeverityLosstatLosf data
     * set rowreq_ctx->data.tpClient4SeverityLosstatLosf from rowreq_ctx->undo->tpClient4SeverityLosstatLosf
     */
    rowreq_ctx->data.tpClient4SeverityLosstatLosf = rowreq_ctx->undo->tpClient4SeverityLosstatLosf;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityLosstatLosf_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityPathmonAccstat
 * tpClient4SeverityPathmonAccstat is subid 9 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.9
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityPathmonAccstat_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityPathmonAccstat_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityPathmonAccstat_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonAccstat_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityPathmonAccstat value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityPathmonAccstat value not illegal */
} /* tpClient4SeverityPathmonAccstat_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityPathmonAccstat_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonAccstat_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityPathmonAccstat undo.
     */
    /*
     * copy tpClient4SeverityPathmonAccstat data
     * set rowreq_ctx->undo->tpClient4SeverityPathmonAccstat from rowreq_ctx->data.tpClient4SeverityPathmonAccstat
     */
    rowreq_ctx->undo->tpClient4SeverityPathmonAccstat = rowreq_ctx->data.tpClient4SeverityPathmonAccstat;


    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonAccstat_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityPathmonAccstat_val
 *        A long containing the new value.
 */
int
tpClient4SeverityPathmonAccstat_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityPathmonAccstat_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonAccstat_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityPathmonAccstat value.
     * set tpClient4SeverityPathmonAccstat value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityPathmonAccstat = tpClient4SeverityPathmonAccstat_val;

    // Set severity on shared memory
    setTpClient4Severity("PathmonAccstat", tpClient4SeverityPathmonAccstat_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonAccstat_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityPathmonAccstat_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonAccstat_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityPathmonAccstat undo.
     */
    /*
     * copy tpClient4SeverityPathmonAccstat data
     * set rowreq_ctx->data.tpClient4SeverityPathmonAccstat from rowreq_ctx->undo->tpClient4SeverityPathmonAccstat
     */
    rowreq_ctx->data.tpClient4SeverityPathmonAccstat = rowreq_ctx->undo->tpClient4SeverityPathmonAccstat;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonAccstat_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityPathmonDais
 * tpClient4SeverityPathmonDais is subid 10 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.10
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityPathmonDais_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityPathmonDais_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityPathmonDais_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDais_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityPathmonDais value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityPathmonDais value not illegal */
} /* tpClient4SeverityPathmonDais_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityPathmonDais_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDais_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityPathmonDais undo.
     */
    /*
     * copy tpClient4SeverityPathmonDais data
     * set rowreq_ctx->undo->tpClient4SeverityPathmonDais from rowreq_ctx->data.tpClient4SeverityPathmonDais
     */
    rowreq_ctx->undo->tpClient4SeverityPathmonDais = rowreq_ctx->data.tpClient4SeverityPathmonDais;


    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonDais_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityPathmonDais_val
 *        A long containing the new value.
 */
int
tpClient4SeverityPathmonDais_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityPathmonDais_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDais_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityPathmonDais value.
     * set tpClient4SeverityPathmonDais value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityPathmonDais = tpClient4SeverityPathmonDais_val;

    // Set severity on shared memory
    setTpClient4Severity("PathmonDais", tpClient4SeverityPathmonDais_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonDais_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityPathmonDais_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDais_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityPathmonDais undo.
     */
    /*
     * copy tpClient4SeverityPathmonDais data
     * set rowreq_ctx->data.tpClient4SeverityPathmonDais from rowreq_ctx->undo->tpClient4SeverityPathmonDais
     */
    rowreq_ctx->data.tpClient4SeverityPathmonDais = rowreq_ctx->undo->tpClient4SeverityPathmonDais;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonDais_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityPathmonDbdi
 * tpClient4SeverityPathmonDbdi is subid 11 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.11
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityPathmonDbdi_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityPathmonDbdi_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityPathmonDbdi_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDbdi_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityPathmonDbdi value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityPathmonDbdi value not illegal */
} /* tpClient4SeverityPathmonDbdi_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityPathmonDbdi_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDbdi_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityPathmonDbdi undo.
     */
    /*
     * copy tpClient4SeverityPathmonDbdi data
     * set rowreq_ctx->undo->tpClient4SeverityPathmonDbdi from rowreq_ctx->data.tpClient4SeverityPathmonDbdi
     */
    rowreq_ctx->undo->tpClient4SeverityPathmonDbdi = rowreq_ctx->data.tpClient4SeverityPathmonDbdi;


    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonDbdi_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityPathmonDbdi_val
 *        A long containing the new value.
 */
int
tpClient4SeverityPathmonDbdi_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityPathmonDbdi_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDbdi_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityPathmonDbdi value.
     * set tpClient4SeverityPathmonDbdi value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityPathmonDbdi = tpClient4SeverityPathmonDbdi_val;

    // Set severity on shared memory
    setTpClient4Severity("PathmonDbdi", tpClient4SeverityPathmonDbdi_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonDbdi_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityPathmonDbdi_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDbdi_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityPathmonDbdi undo.
     */
    /*
     * copy tpClient4SeverityPathmonDbdi data
     * set rowreq_ctx->data.tpClient4SeverityPathmonDbdi from rowreq_ctx->undo->tpClient4SeverityPathmonDbdi
     */
    rowreq_ctx->data.tpClient4SeverityPathmonDbdi = rowreq_ctx->undo->tpClient4SeverityPathmonDbdi;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonDbdi_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityPathmonDlck
 * tpClient4SeverityPathmonDlck is subid 12 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.12
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityPathmonDlck_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityPathmonDlck_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityPathmonDlck_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDlck_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityPathmonDlck value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityPathmonDlck value not illegal */
} /* tpClient4SeverityPathmonDlck_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityPathmonDlck_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDlck_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityPathmonDlck undo.
     */
    /*
     * copy tpClient4SeverityPathmonDlck data
     * set rowreq_ctx->undo->tpClient4SeverityPathmonDlck from rowreq_ctx->data.tpClient4SeverityPathmonDlck
     */
    rowreq_ctx->undo->tpClient4SeverityPathmonDlck = rowreq_ctx->data.tpClient4SeverityPathmonDlck;


    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonDlck_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityPathmonDlck_val
 *        A long containing the new value.
 */
int
tpClient4SeverityPathmonDlck_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityPathmonDlck_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDlck_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityPathmonDlck value.
     * set tpClient4SeverityPathmonDlck value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityPathmonDlck = tpClient4SeverityPathmonDlck_val;

    // Set severity on shared memory
    setTpClient4Severity("PathmonDlck", tpClient4SeverityPathmonDlck_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonDlck_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityPathmonDlck_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonDlck_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityPathmonDlck undo.
     */
    /*
     * copy tpClient4SeverityPathmonDlck data
     * set rowreq_ctx->data.tpClient4SeverityPathmonDlck from rowreq_ctx->undo->tpClient4SeverityPathmonDlck
     */
    rowreq_ctx->data.tpClient4SeverityPathmonDlck = rowreq_ctx->undo->tpClient4SeverityPathmonDlck;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonDlck_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeverityPathmonMsiunst
 * tpClient4SeverityPathmonMsiunst is subid 13 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.13
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeverityPathmonMsiunst_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeverityPathmonMsiunst_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityPathmonMsiunst_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonMsiunst_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeverityPathmonMsiunst value.
     */

    return MFD_SUCCESS; /* tpClient4SeverityPathmonMsiunst value not illegal */
} /* tpClient4SeverityPathmonMsiunst_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeverityPathmonMsiunst_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonMsiunst_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeverityPathmonMsiunst undo.
     */
    /*
     * copy tpClient4SeverityPathmonMsiunst data
     * set rowreq_ctx->undo->tpClient4SeverityPathmonMsiunst from rowreq_ctx->data.tpClient4SeverityPathmonMsiunst
     */
    rowreq_ctx->undo->tpClient4SeverityPathmonMsiunst = rowreq_ctx->data.tpClient4SeverityPathmonMsiunst;


    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonMsiunst_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeverityPathmonMsiunst_val
 *        A long containing the new value.
 */
int
tpClient4SeverityPathmonMsiunst_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeverityPathmonMsiunst_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonMsiunst_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeverityPathmonMsiunst value.
     * set tpClient4SeverityPathmonMsiunst value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeverityPathmonMsiunst = tpClient4SeverityPathmonMsiunst_val;

    // Set severity on shared memory
    setTpClient4Severity("PathmonMsiunst", tpClient4SeverityPathmonMsiunst_val);

    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonMsiunst_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeverityPathmonMsiunst_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeverityPathmonMsiunst_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeverityPathmonMsiunst undo.
     */
    /*
     * copy tpClient4SeverityPathmonMsiunst data
     * set rowreq_ctx->data.tpClient4SeverityPathmonMsiunst from rowreq_ctx->undo->tpClient4SeverityPathmonMsiunst
     */
    rowreq_ctx->data.tpClient4SeverityPathmonMsiunst = rowreq_ctx->undo->tpClient4SeverityPathmonMsiunst;

    
    return MFD_SUCCESS;
} /* tpClient4SeverityPathmonMsiunst_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeveritySecmonBipsfs
 * tpClient4SeveritySecmonBipsfs is subid 14 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.14
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeveritySecmonBipsfs_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeveritySecmonBipsfs_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeveritySecmonBipsfs_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonBipsfs_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeveritySecmonBipsfs value.
     */

    return MFD_SUCCESS; /* tpClient4SeveritySecmonBipsfs value not illegal */
} /* tpClient4SeveritySecmonBipsfs_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeveritySecmonBipsfs_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonBipsfs_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeveritySecmonBipsfs undo.
     */
    /*
     * copy tpClient4SeveritySecmonBipsfs data
     * set rowreq_ctx->undo->tpClient4SeveritySecmonBipsfs from rowreq_ctx->data.tpClient4SeveritySecmonBipsfs
     */
    rowreq_ctx->undo->tpClient4SeveritySecmonBipsfs = rowreq_ctx->data.tpClient4SeveritySecmonBipsfs;


    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonBipsfs_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeveritySecmonBipsfs_val
 *        A long containing the new value.
 */
int
tpClient4SeveritySecmonBipsfs_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeveritySecmonBipsfs_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonBipsfs_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeveritySecmonBipsfs value.
     * set tpClient4SeveritySecmonBipsfs value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeveritySecmonBipsfs = tpClient4SeveritySecmonBipsfs_val;

    // Set severity on shared memory
    setTpClient4Severity("SecmonBipsfs", tpClient4SeveritySecmonBipsfs_val);

    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonBipsfs_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeveritySecmonBipsfs_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonBipsfs_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeveritySecmonBipsfs undo.
     */
    /*
     * copy tpClient4SeveritySecmonBipsfs data
     * set rowreq_ctx->data.tpClient4SeveritySecmonBipsfs from rowreq_ctx->undo->tpClient4SeveritySecmonBipsfs
     */
    rowreq_ctx->data.tpClient4SeveritySecmonBipsfs = rowreq_ctx->undo->tpClient4SeveritySecmonBipsfs;

    
    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonBipsfs_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeveritySecmonDbdi
 * tpClient4SeveritySecmonDbdi is subid 15 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.15
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeveritySecmonDbdi_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeveritySecmonDbdi_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeveritySecmonDbdi_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonDbdi_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeveritySecmonDbdi value.
     */

    return MFD_SUCCESS; /* tpClient4SeveritySecmonDbdi value not illegal */
} /* tpClient4SeveritySecmonDbdi_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeveritySecmonDbdi_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonDbdi_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeveritySecmonDbdi undo.
     */
    /*
     * copy tpClient4SeveritySecmonDbdi data
     * set rowreq_ctx->undo->tpClient4SeveritySecmonDbdi from rowreq_ctx->data.tpClient4SeveritySecmonDbdi
     */
    rowreq_ctx->undo->tpClient4SeveritySecmonDbdi = rowreq_ctx->data.tpClient4SeveritySecmonDbdi;


    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonDbdi_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeveritySecmonDbdi_val
 *        A long containing the new value.
 */
int
tpClient4SeveritySecmonDbdi_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeveritySecmonDbdi_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonDbdi_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeveritySecmonDbdi value.
     * set tpClient4SeveritySecmonDbdi value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeveritySecmonDbdi = tpClient4SeveritySecmonDbdi_val;

    // Set severity on shared memory
    setTpClient4Severity("SecmonDbdi", tpClient4SeveritySecmonDbdi_val);

    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonDbdi_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeveritySecmonDbdi_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonDbdi_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeveritySecmonDbdi undo.
     */
    /*
     * copy tpClient4SeveritySecmonDbdi data
     * set rowreq_ctx->data.tpClient4SeveritySecmonDbdi from rowreq_ctx->undo->tpClient4SeveritySecmonDbdi
     */
    rowreq_ctx->data.tpClient4SeveritySecmonDbdi = rowreq_ctx->undo->tpClient4SeveritySecmonDbdi;

    
    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonDbdi_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeveritySecmonDiae
 * tpClient4SeveritySecmonDiae is subid 16 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.16
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeveritySecmonDiae_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeveritySecmonDiae_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeveritySecmonDiae_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonDiae_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeveritySecmonDiae value.
     */

    return MFD_SUCCESS; /* tpClient4SeveritySecmonDiae value not illegal */
} /* tpClient4SeveritySecmonDiae_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeveritySecmonDiae_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonDiae_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeveritySecmonDiae undo.
     */
    /*
     * copy tpClient4SeveritySecmonDiae data
     * set rowreq_ctx->undo->tpClient4SeveritySecmonDiae from rowreq_ctx->data.tpClient4SeveritySecmonDiae
     */
    rowreq_ctx->undo->tpClient4SeveritySecmonDiae = rowreq_ctx->data.tpClient4SeveritySecmonDiae;


    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonDiae_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeveritySecmonDiae_val
 *        A long containing the new value.
 */
int
tpClient4SeveritySecmonDiae_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeveritySecmonDiae_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonDiae_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeveritySecmonDiae value.
     * set tpClient4SeveritySecmonDiae value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeveritySecmonDiae = tpClient4SeveritySecmonDiae_val;

    // Set severity on shared memory
    setTpClient4Severity("SecmonDiae", tpClient4SeveritySecmonDiae_val);

    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonDiae_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeveritySecmonDiae_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonDiae_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeveritySecmonDiae undo.
     */
    /*
     * copy tpClient4SeveritySecmonDiae data
     * set rowreq_ctx->data.tpClient4SeveritySecmonDiae from rowreq_ctx->undo->tpClient4SeveritySecmonDiae
     */
    rowreq_ctx->data.tpClient4SeveritySecmonDiae = rowreq_ctx->undo->tpClient4SeveritySecmonDiae;

    
    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonDiae_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeveritySecmonFlom
 * tpClient4SeveritySecmonFlom is subid 17 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.17
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeveritySecmonFlom_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeveritySecmonFlom_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeveritySecmonFlom_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonFlom_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeveritySecmonFlom value.
     */

    return MFD_SUCCESS; /* tpClient4SeveritySecmonFlom value not illegal */
} /* tpClient4SeveritySecmonFlom_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeveritySecmonFlom_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonFlom_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeveritySecmonFlom undo.
     */
    /*
     * copy tpClient4SeveritySecmonFlom data
     * set rowreq_ctx->undo->tpClient4SeveritySecmonFlom from rowreq_ctx->data.tpClient4SeveritySecmonFlom
     */
    rowreq_ctx->undo->tpClient4SeveritySecmonFlom = rowreq_ctx->data.tpClient4SeveritySecmonFlom;


    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonFlom_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeveritySecmonFlom_val
 *        A long containing the new value.
 */
int
tpClient4SeveritySecmonFlom_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeveritySecmonFlom_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonFlom_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeveritySecmonFlom value.
     * set tpClient4SeveritySecmonFlom value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeveritySecmonFlom = tpClient4SeveritySecmonFlom_val;

    // Set severity on shared memory
    setTpClient4Severity("SecmonFlom", tpClient4SeveritySecmonFlom_val);

    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonFlom_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeveritySecmonFlom_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonFlom_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeveritySecmonFlom undo.
     */
    /*
     * copy tpClient4SeveritySecmonFlom data
     * set rowreq_ctx->data.tpClient4SeveritySecmonFlom from rowreq_ctx->undo->tpClient4SeveritySecmonFlom
     */
    rowreq_ctx->data.tpClient4SeveritySecmonFlom = rowreq_ctx->undo->tpClient4SeveritySecmonFlom;

    
    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonFlom_undo */

/*---------------------------------------------------------------------
 * FTNC-MIB::tpClient4SeverityEntry.tpClient4SeveritySecmonLom
 * tpClient4SeveritySecmonLom is subid 18 of tpClient4SeverityEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.43484.1.1.3.6.5.1.18
 * Description:
alarm severity
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param tpClient4SeveritySecmonLom_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * tpClient4SeverityTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
tpClient4SeveritySecmonLom_check_value( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeveritySecmonLom_val)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonLom_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid tpClient4SeveritySecmonLom value.
     */

    return MFD_SUCCESS; /* tpClient4SeveritySecmonLom value not illegal */
} /* tpClient4SeveritySecmonLom_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (tpClient4SeverityTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * tpClient4SeverityTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
tpClient4SeveritySecmonLom_undo_setup( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonLom_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup tpClient4SeveritySecmonLom undo.
     */
    /*
     * copy tpClient4SeveritySecmonLom data
     * set rowreq_ctx->undo->tpClient4SeveritySecmonLom from rowreq_ctx->data.tpClient4SeveritySecmonLom
     */
    rowreq_ctx->undo->tpClient4SeveritySecmonLom = rowreq_ctx->data.tpClient4SeveritySecmonLom;


    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonLom_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param tpClient4SeveritySecmonLom_val
 *        A long containing the new value.
 */
int
tpClient4SeveritySecmonLom_set( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx, long tpClient4SeveritySecmonLom_val )
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonLom_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set tpClient4SeveritySecmonLom value.
     * set tpClient4SeveritySecmonLom value in rowreq_ctx->data
     */
    rowreq_ctx->data.tpClient4SeveritySecmonLom = tpClient4SeveritySecmonLom_val;

    // Set severity on shared memory
    setTpClient4Severity("SecmonLom", tpClient4SeveritySecmonLom_val);

    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonLom_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
tpClient4SeveritySecmonLom_undo( tpClient4SeverityTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:tpClient4SeverityTable:tpClient4SeveritySecmonLom_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up tpClient4SeveritySecmonLom undo.
     */
    /*
     * copy tpClient4SeveritySecmonLom data
     * set rowreq_ctx->data.tpClient4SeveritySecmonLom from rowreq_ctx->undo->tpClient4SeveritySecmonLom
     */
    rowreq_ctx->data.tpClient4SeveritySecmonLom = rowreq_ctx->undo->tpClient4SeveritySecmonLom;
    
    return MFD_SUCCESS;
} /* tpClient4SeveritySecmonLom_undo */


void setTpClient4Severity(char *alarm, long severity_value)
{
	DEBUGMSGTL(("verbose:setTpClient4Severity:setTpClient4Severity","called\n"));

	shm_handler_create(pt_monitor_struct);

	if (strcmp(alarm, "FramerFsmms") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].FramerFsmms = severity_value;
	}
	else if (strcmp(alarm, "FramerFssusts") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].FramerFssusts = severity_value;
	}
	else if (strcmp(alarm, "FramerFst") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].FramerFst = severity_value;
	}
	else if (strcmp(alarm, "FramerSif") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].FramerSif = severity_value;
	}
	else if (strcmp(alarm, "FramerSlof") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].FramerSlof = severity_value;
	}
	else if (strcmp(alarm, "FramerSoof") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].FramerSoof = severity_value;
	}
	else if (strcmp(alarm, "LosstatLosf") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].LosstatLosf = severity_value;
	}
	else if (strcmp(alarm, "PathmonAccstat") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].PathmonAccstat = severity_value;
	}
	else if (strcmp(alarm, "PathmonDais") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].PathmonDais = severity_value;
	}
	else if (strcmp(alarm, "PathmonDbdi") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].PathmonDbdi = severity_value;
	}
	else if (strcmp(alarm, "PathmonDlck") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].PathmonDlck = severity_value;
	}
	else if (strcmp(alarm, "PathmonMsiunst") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].PathmonMsiunst = severity_value;
	}
	else if (strcmp(alarm, "SecmonBipsfs") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].SecmonBipsfs = severity_value;
	}
	else if (strcmp(alarm, "SecmonDbdi") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].SecmonDbdi = severity_value;
	}
	else if (strcmp(alarm, "SecmonDiae") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].SecmonDiae = severity_value;
	}
	else if (strcmp(alarm, "SecmonFlom") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].SecmonFlom = severity_value;
	}
	else if (strcmp(alarm, "SecmonLom") == 0)
	{
		pt_monitor_struct->tp_defects_struct.severity.severity_client[3].SecmonLom = severity_value;
	}

	// Set dirty flag
	pt_monitor_struct->sev_dirty_flag = 1;
}

/** @} */
